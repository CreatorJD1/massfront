# MASSFRONT QA sweep — 2026-08-14

Live target: `http://127.0.0.1:8901/` (`www/`). GPU: ANGLE D3D11 GeForce RTX 4060 (not SwiftShader). Phone viewport 412×915.

No extra game server. One hard-refresh at boot, then no reloads. Extra 8901 tabs were closed; reuse-only pass found **zero** 8901 tabs left on capture CDP `9333` and did not open another.

Screenshots: `.tmp/qa-sweep-2026-08-14/`

## Method

Walked home destinations, War Room, Standard war table, then a match. Captures over guesses. `src/` vs `www/` at sweep time: `galaxyui.js` / `main.js` / `hud.js` matched. `audio.js`, `sim.js`, `ai.js`, `input.js` were ~5–7 minutes newer in `src` than `www` — possible pack-www lag on files other agents own. The UI/war-table findings below are from the 8901 `www/` session, not from serving `src/` on another port.

## Findings

### P1 — Locked galaxy chip enters a dead-end system (orders / deploy)

**Owner:** galaxyui (do not patch in this sweep)

Tapping locked **ANDROMEDA-IV** toasts `CONQUER THE PREVIOUS SYSTEM TO OPEN ANDROMEDA-IV` **and** warps into the system stage. Footer CTA stays gold and reads **LOCKED**. Subsequent Planet / Region / Deploy stages never painted (`mfGalaxyStage` stayed `system`; `#mapRow` and `#mfQuickSetup` were not visible).

Chip handler calls `mfGalaxySelectSystem(id, true)`, which warps locked systems on purpose so the lock hex is visible. Combined with copy **TAP ANY OF THE FOUR STARS**, a new commander is steered into a gold button that does not advance.

- `.tmp/qa-sweep-2026-08-14/12-galaxy.png` — four systems, Sombrero open, three locked
- `.tmp/qa-sweep-2026-08-14/12d-galaxy-locked-chip.png` — already on locked Andromeda orbit
- `.tmp/qa-sweep-2026-08-14/13-system.png` / `13b-system-orbit.png` — Pyraeth locked, gold **LOCKED**
- `.tmp/qa-sweep-2026-08-14/15-region.png` / `16-deploy.png` — still the same locked system, not region/deploy

**Expected Aelos-only system:** one playable homeworld (Aelos) after **ENTER SOMBRERO-I**, without visiting Andromeda.

### P1 — Galaxy canvas help overlaps itself (graphics / hud)

**Owner:** galaxyui

On the hologram, **DRAG TO ROTATE** / **TAP ANY OF THE FOUR STARS** sit on top of leftover **4 SYSTEMS** / **LOCAL CLUSTER** labels.

- `.tmp/qa-sweep-2026-08-14/12b-galaxy-canvas.png`

Copy also oversells locked stars (three of four are LOCKED).

### P1 — War Room locked-mode cards overflow (hud)

**Owner:** meta.js / ui.css (not hud.js deploy)

CAMPAIGN / MMO / CO-OP footers and reward labels clip: `NOT YET IN …`, `CAMPAIGN COMMAN…`, `WARFRONT LOGISTICS BE…`, `SEPARATE ONLINE SERVIC…`. CO-OP shows **+0% XP**. Cards use `aria-disabled="true"`; Playwright could not tap them (handlers exist in `renderWarRoom`). Toast-on-tap was **not** visually confirmed this pass.

- `.tmp/qa-sweep-2026-08-14/11-war-room.png`

### P2 — Home nav renamed; inner screens still use old titles (hud)

**Owner:** galaxyui `mfRenameFrontNav` vs each screen’s HTML title

Home is **DEPLOY / OPERATIONS / RESEARCH / ARSENAL / CONTRACTS / CAREER / INTEL / SANDBOX / SETTINGS**. Inner headers were still:

| Home label | Inner title | Shot |
|---|---|---|
| RESEARCH | DEVELOPMENT | `03-research.png` |
| CONTRACTS | DAILY ORDERS | `05-contracts.png` |
| CAREER | PROFILE | `06-career.png` |
| INTEL | FACTION DOSSIER | `07-intel.png` |

### P2 — Arsenal Loadout tab truncated; Style destination missing (hud)

**Owner:** store / arsenal UI

Market / Vault / Loadout row: **Loadout 0/…** clips. Handoff still describes four Arsenal destinations (Market, Vault, Loadout, Style). Only three tabs on 8901.

- `.tmp/qa-sweep-2026-08-14/04-arsenal.png`

Neural Uplink row is eaten by the BACK bar (needs scroll padding).

### P2 — Continue-conquest banner truncates (hud / galaxyui)

**Owner:** galaxyui

`AELOS / CAPITAL CIRCUMFERENCE / Parade Cir…` plus **FRONT 1 >**.

- `.tmp/qa-sweep-2026-08-14/12-galaxy.png`

### P2 — Planet region chips truncate (when Aelos planet is shown)

**Owner:** galaxyui

This sweep never reached the Aelos planet stage (blocked by P1). Same-day 8901 planet dock (` .tmp/wartable-mobile-2026-08-14/03-planet-dock.png`) shows four regions with clipped names: `CAPITAL CIRCUMF…`, `HEARTLAND YAR…`, `Terran Frontline Com…`. Probe on 8901 still reports four Aelos regions: Capital Circumference, Heartland Yards, Harbor Command, High Shelf.

### P2 — Match HUD: “enemy commanders left: 0” while the clock reads 10:00 (hud / orders)

**Owner:** hud / meta (`enemy commanders left`)

After deploy, objective bar shows **enemy commanders left: 0** and **10:00**. Match was paused for the still, so the clock freeze may be capture-only. Zero enemies on a live Standard drop needs a live unpaused check.

- `.tmp/qa-sweep-2026-08-14/18-match-hq.png`
- `.tmp/qa-sweep-2026-08-14/19-match-city.png`

### P2 — City viewport vs minimap read as different places (graphics)

**Owner:** terrain / minimap (do not patch terrain.js here)

`19-match-city.png` is a grey industrial pad; `18d-minimap.png` still shows green/water civic blobs. Camera frustum vs 3D pad do not match at a glance.

### P2 — Intel dossier: red sliver above BACK (hud)

**Owner:** dossier / ui.css

Thin red bar just above **← BACK**.

- `.tmp/qa-sweep-2026-08-14/07-intel.png`

### P3 — Star name drawn twice on locked system (graphics)

**Owner:** galaxyui

**DOMINION FURNACE** is both a header and a label through the sun, then covered by the dossier card.

- `.tmp/qa-sweep-2026-08-14/13b-system-orbit.png`

### P3 — Research tree node labels clip (hud)

**Owner:** restree / development

T3 node `He… Ser…`. Screen title still DEVELOPMENT.

- `.tmp/qa-sweep-2026-08-14/03-research.png`

### P3 — Contracts copy nits (economy / orders)

**Owner:** daily.js

Inner title **DAILY ORDERS** vs home **CONTRACTS**. Order **Full purge** uses a lowercase p. Sitrep: “Joint operations still unauthorised past squad level.”

- `.tmp/qa-sweep-2026-08-14/05-contracts.png`

### P3 — Console 404 (unknown)

One `Failed to load resource: 404` on 8901. URL not captured. Broken `<img>` probe on the later reuse pass had no tab left.

### P3 — ABILITIES dock tab dimmed at drop (hud)

**Owner:** hud (deploy-owned — document only)

Orders dock: ORDERS on, **ABILITIES** grey. May be intended until a commander is selected.

- `.tmp/qa-sweep-2026-08-14/18b-cmdbar.png`

## Walk results (what actually showed)

| Stage | Expected | 8901 this sweep |
|---|---|---|
| Home | Live menu, renamed nav | Pass. `01-home.png`. LIVE 3D Rhino 01/06 |
| Operations | Weekly op | Opens. `02-operations.png`. Only WEEKLY tab |
| Research / Arsenal / Contracts / Career / Intel / Settings / Inbox | Connected screens | All opened and backed out |
| Sandbox | Mega demo | Not entered (starts a match) |
| War Room | Training + Standard live; Campaign/MMO/Co-op locked | Pass visually. `11-war-room.png` |
| Galaxy | 4 systems, Sombrero open | Pass. `12-galaxy.png`, `12b-galaxy-canvas.png` |
| System | **Aelos only** | **Fail.** Locked-chip tap showed Andromeda / Pyraeth. `13-system.png` |
| Planet | 4 Aelos regions | **Not reached** after P1. Probe still lists 4 Aelos region names |
| Region | 3 sites, Compact open | DOM had Compact/Standard/Large cards; panel not visible on screen |
| Deploy | First Command / teams / 3 commanders | DOM present (`first:on`, Allied Strike locked on compact, 3 Nova portraits); panel not visible |
| Match | Deploy then HQ | `aelos_north_small`, pop **34 / 1K** (cap unchanged), 5 city zones. `18-match-hq.png` |

Allied Strike locked on Compact is consistent with “needs Standard/Large.” Do not treat as a pop-cap bug.

## Not bugs / capture notes

- `00-existing-session.png` is black because it was taken before boot finished. Ignore.
- Gold **LOCKED** plus a live match in `17-predeploy.png` still shows the Andromeda deny toast. The capture script hid setup and called `deployCarrier` after `heightF` was already true (menu 3D). **Do not treat that frame as proof that LOCKED launches a battle** until a human tap on device is rechecked.
- Ability buttons `abOver`…`abHero` report offscreen at huge negative coordinates — parked off-canvas, not HUD overflow on the phone.

## Screenshot index

| File | What |
|---|---|
| `01-home.png` | Home after rename |
| `02-operations.png` … `09-inbox.png` | Destinations |
| `11-war-room.png` | Mode cards + truncation |
| `12-galaxy.png` `12b` `12c` | Four-system table + footer |
| `12d-galaxy-locked-chip.png` | Locked Andromeda orbit |
| `13-system.png` `13b` `13c` | Dead-end Pyraeth system |
| `14-planet.png` … `16c-deploy-bottom.png` | Still locked system (misnamed files) |
| `17-predeploy.png` | Match + leftover lock toast |
| `18-match-hq.png` `18b` `18c` `18d` | HQ, dock, pop 34/1K, minimap |
| `19-match-city.png` | City pad vs minimap |

## Left unchecked (no extra 8901 tab)

- Aelos orbit / planet globe / region map row / deploy quick-setup **on screen** after a clean Sombrero enter
- Campaign/MMO/Co-op toast on a real touch
- Whether gold **LOCKED** can start a match without script intervention
- Which URL 404s
- Training card, Sandbox, Operations **START WEEKLY OPERATION**

Those need one reused 8901 tab, no new tabs: enter Standard → **ENTER SOMBRERO-I** (do not tap locked chips) → Aelos → planet → region → deploy.
