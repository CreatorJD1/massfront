# MASSFRONT verify — 2026-08-14

## Addendum — leftover systems (16:10)

Leftover-systems agent. Source edits in `galaxyui.js`, `tutorial.js`, `hud.js`,
`daily.js`, `ui.css`. `node tools/bundle.mjs` → **75** sources, clean.
`node tools/pack-www.mjs` after that.

**No full 14-check rewalk.** A sibling crater-water recapture was holding the
8901 tab (`?craterw=`). One hard-refresh was deferred so that capture was not
loop-refreshed. Locked-chip / Standard-medium / retreat+STOP were already PASS
on the 15:44 recheck and were not touched.

Source-only leftover closes (see `docs/MASTER_PLAN_2026-08-14.md` §1):
galaxy chrome + help under hologram; continue-conquest short path; training
skip made explicit; minimap `willReadFrequently`; inbound-commander HUD flash;
`Full Purge` title.

`gl.js` Canvas2D hint sites stay with the graphics sibling.

## Addendum — FAIL 7 / 13 recheck (15:44)

Packed `http://127.0.0.1:8901/` only. ANGLE D3D11 RTX 4060. Cleared `massfront-updates` IDB. One tab, closed after the last still (`8901_LEFT 0`). No feature rewrite. Stills: `.tmp/verify-2026-08-14-recheck/`. Probes: `probes.json`, `recapture.json`.

| # | Morning | Recheck | Notes |
|---|---|---|---|
| 7 | FAIL | **PASS** | Double-tap empty ground: toast `➤ RETREAT — dropping combat` + cyan route (`07-retreat.png`, `07-retreat-toast.png`; `moveFx` kind 1). Stop writes selInfo **STOP**; Hold writes **HOLD** (live `selInfo` on Commander/Constructor, then Striker/Rhino). |
| 13 | FAIL | **PASS** (shader) / **unproven still** (foam) | Settings → Display renderer badge **OK**, no `ERR water FS` / `half` reserved-word FAULT. `waterProg` linked, `GL_PROG_ERRORS` empty, `aelos_north_medium` river (115 wet samples), `wfxWakeN=1`. Clean wake/foam photograph did not land: first wet stills were under the home overlay; the recapture hit **MISSION FAILED** after the commander died. Not treated as a one-line product bug. |

---

Live only: `http://127.0.0.1:8901/` (packed `www/`). GPU: ANGLE D3D11 GeForce RTX 4060 (not SwiftShader). Phone viewport 412×915, one capture tab, closed after the last probe.

Report only. No feature rewrites. `tacticons.png` 404 is by design and is not a fail.

Screenshots: `.tmp/verify-2026-08-14/`  
Probes: `.tmp/verify-2026-08-14/report.json`, `probes.json`, `probes-continue.json`

## Method

Two 8901 sessions on the same GPU capture Chrome:

1. **War-table walk** (14:24). Real home destinations, War Room → Standard → galaxy → locked Andromeda tap → ENTER SOMBRERO-I → Aelos system → planet → region → deploy (HARD + enemy) → match. This is the evidence for items 1–6, 8–11.
2. **Packaged recheck** (14:54). Cleared `massfront-updates` IndexedDB so boot ran packaged `www/`, not an OTA blob. Confirmed home rename, Settings → Display, then a Standard `aelos_north_medium` drop for items 12–14 and a second try at item 7.

An earlier 14:43 walk painted black boot frames and threw `Identifier 'WFX_N' has already been declared` / `terrVerts is not defined`. That session is **not** used as product evidence. After the IDB clear, packaged boot was clean (`terrVerts` present, start button **DEPLOY**, no page errors). Treat the 14:43 collision as a capture-profile / mid-pack race, not as the packed APK path.

`pack-www` wipes `www/`. Mid-pack 404s were retried. Live `8901` served `www/` (repo `docs/` and `experimental/` 404).

## Score

**12 PASS · 2 FAIL**

| # | Check | Result |
|---|---|---|
| 1 | Galaxy: four systems, three locked, locked tap stays | **PASS** |
| 2 | System: Aelos only, cyan orbit | **PASS** |
| 3 | Planet: four regions, no moons | **PASS** |
| 4 | Deploy: enemy faction + difficulty apply; Standard `aelos_north_medium`; 12 sites | **PASS** |
| 5 | No 15 Hz material flicker | **PASS** |
| 6 | Buildings not black hulls; pavement not pink | **PASS** |
| 7 | Double-tap retreats; Stop stands; HUD STOP vs HOLD | **FAIL** |
| 8 | Hazard chip; titles match nav; Arsenal 4 tabs incl STYLE | **PASS** |
| 9 | Minimap box matches 3D camera | **PASS** |
| 10 | Commander VO; Brood screeches, not human | **PASS** |
| 11 | Pop `n / 1K` | **PASS** |
| 12 | MEDIUM not slower than HIGH (`dprCap` 1.25, World V2 off) | **PASS** |
| 13 | Water: ocean swell / river flow / shore foam / ship bob | **FAIL** |
| 14 | Screen Grade default NEUTRAL (no canvas CSS crush) | **PASS** |

---

### 1 Galaxy — PASS

`.tmp/verify-2026-08-14/07-galaxy.png`, `07b-galaxy-canvas.png`, `08-galaxy-after-locked-tap.png`

Four systems on the hologram: **SOMBRERO-I OPEN**, **ANDROMEDA-IV / ORION ARC / HELIOS CORE LOCKED**. Footer stayed **▶ ENTER SOMBRERO-I**. Chips: Sombrero `0/12 SECURED`, three `SYSTEM LOCKED`.

Locked Andromeda tap: stage remained `galaxy`, toast `CONQUER THE PREVIOUS SYSTEM TO OPEN ANDROMEDA-IV`, active system still `sombrero`. Did not warp into a gold LOCKED orbit (the 14:00 QA-sweep P1).

---

### 2 System Aelos / cyan orbit — PASS

`.tmp/verify-2026-08-14/09-system.png`, `09b-system-orbit.png`

Sombrero orbit: one playable target `aelos`. Dossier **HOMEWORLD AELOS**, **4 REGIONS · 12 SITES**. One bright cyan ellipse; lore rocks sit on dim grey rings. Footer **▶ ENTER AELOS**. `mfSystemTargets` = `['aelos']`.

---

### 3 Planet four regions, no moons — PASS

`.tmp/verify-2026-08-14/10-planet.png`, `10b-planet-globe.png`

Globe **AELOS** with four region marks: Capital Circumference, Heartland Yards, Harbor Command, High Shelf. No moons. Conquest chip **0 / 12**. Primer copy: four regions, tap to descend.

---

### 4 Deploy apply + Standard medium + 12 sites — PASS

`.tmp/verify-2026-08-14/11-region.png`, `12-deploy.png`, `12b-deploy-config.png`, `14-match.png`

Region row: Compact / **STANDARD · 2.6 KM Civic Grid** / Large locked. `curMap` **`aelos_north_medium`**, size `standard`. Aelos has 12 site IDs (4 regions × 3). Deploy tags: **2.6 KM**, **RIVER + NAVAL**, twelve-sites copy.

Applied **HARD** on the global diff row and enemy AI slot. After drop: `aiFactionSel` **legion**, `difficulty` **2**, match live.

---

### 5 No 15 Hz material flicker — PASS

`.tmp/verify-2026-08-14/17-flicker-frame.png`, `18-base-albedo.png`

HQ crop sampled across frames: worst mean Δ 1.47, `pctBig` 0.09%. Hulls stay panelled; no army-wide atlas strobe. Ad upload is on texture unit 7 in packed `adboards.js`.

---

### 6 Buildings / pavement — PASS

`.tmp/verify-2026-08-14/14-match.png`, `18-base-albedo.png`, `16-hud-hazard.png`

HQ / factory / reactor sit on **grey** civic pads with charcoal hulls and cyan lamps. Probe: 3 buildings, mean luminance 91.6, near-black 0%, magenta 0%. Not black unlit hulls, not pink pavement.

---

### 7 Double-tap / Stop / STOP vs HOLD — FAIL

`.tmp/verify-2026-08-14/20-retreat.png`, `16-hud-hazard.png`

The orders dock **is** labelled **STOP** (hand icon), not HOLD. That is the only part that showed.

Live double-tap on empty ground did not produce a retreat toast (`CLAIM MASS` stayed on the rail). `selInfo` never read STOP or HOLD. A second in-match probe found no selectable unit (`orders.ok: false`) after a 1-second commander death on the fast drop. **Stop-stands vs idle-chase was not proven on 8901.**

---

### 8 Hazard / titles / Arsenal STYLE — PASS

`.tmp/verify-2026-08-14/00-home.png`, `00b-home-recheck.png`, `01-research.png`, `02-contracts.png`, `03-career.png`, `04-intel.png`, `05-arsenal.png`, `16-hud-hazard.png`

- Hazard chip **◎ CLEAR SKIES** on the match HUD (`aelos_north_medium`).
- Home nav: **DEPLOY / OPERATIONS / RESEARCH / ARSENAL / CONTRACTS / CAREER / INTEL / SANDBOX / SETTINGS**.
- Inner titles match those labels (no leftover DEVELOPMENT / DAILY ORDERS / PROFILE / FACTION DOSSIER).
- Arsenal tabs: **MARKET · VAULT · LOADOUT · STYLE**.

---

### 9 Minimap vs 3D camera — PASS

`.tmp/verify-2026-08-14/19-minimap.png`, `19b-minimap-context.png`, `14-match.png`

White frustum on the command map sits on the HQ pad the 3D camera is looking at. Unproject error max **8.9** world units on a 3200 map (~0.3%).

---

### 10 Commander VO / Brood screech — PASS

`.tmp/verify-2026-08-14/22-vo-state.png`

Nova `VO_LAST`: `kind: 'speech'`, `human: true`, slot `vo_nova_attack`. Brood: `kind: 'screech'`, `human: false`. `speechSynthesis` count stayed 0.

---

### 11 Pop n / 1K — PASS

`.tmp/verify-2026-08-14/15-pop-chip.png`, `14-match.png`, `18-base-albedo.png`

Chip **2 / 1K** at drop (later HQ probe **34 / 1K**). Cap 1000. Not 4K.

---

### 12 MEDIUM vs HIGH — PASS

`.tmp/verify-2026-08-14/12-high.png`, `12-medium.png` (Mission Failed overlay; numbers are from rAF, not those stills)

| Preset | median ms | p95 | `worldV2` | `dprCap` | `aoSamples` |
|---|---|---|---|---|---|
| HIGH | 12.5 | 37.6 | true | 0 | 12 |
| MEDIUM | 8.3 | 29.2 | **false** | **1.25** | 4 |

MEDIUM is cheaper on the same RTX 4060. Playwright `deviceScaleFactor` still reports CSS DPR 2; the fillrate cap is `GFX.dprCap`.

---

### 13 Water — FAIL

`.tmp/verify-2026-08-14/14d-settings-display.png` (renderer FAULT), `13-water.png` (Mission Failed overlay; not a water still)

`aelos_north_medium` is authored **river** (65 wet samples, hydro=1, ocean=0). CPU `waterSurfaceY` moved ~0.20 over 480 ms; naval `unitGroundY` bobbed the same amount. Foam colour is authored `[150,205,205]`.

Packed Settings → Display reports:

`ERR water FS: 'half' : Illegal use of reserved word` — fragment shader not compiled, water program **FAULT**.

So GPU ocean swell / river flow / shore foam are **not drawing** on this 8901 GPU path. CPU bob math is not a pass for the four visual asks.

---

### 14 Screen Grade NEUTRAL — PASS

`.tmp/verify-2026-08-14/14d-settings-display.png`, `14e-grade-row.png`, `00-home.png`, `14-match.png`

Settings → Display **Screen Grade = NEUTRAL**. Copy: “No screen filter.” `META.settings.screenGrade === 'neutral'`, `#gl` computed `filter: none`. Match stills keep shadow detail; no CSS contrast crush.

---

## Boot / load notes (not in the 14)

- Pre-alpha intro can auto-dismiss before the first still. Account gate can swallow `#startBtn` until `#apCloseBtn` / `apClose()`. After a clean packaged boot, `#startBtn` is **DEPLOY** and opens the War Room.
- Packaged boot after IDB clear: no `WFX_N` collision, `terrVerts` live, water **mesh** ok, water **FS** faulted (item 13).
- War Room locked cards still clip (`NOT YET IN PLAY`, `CAMPAIGN COMMAND INTEL`, `WARFRONT LOGISTICS BEACON`). P2, same as the morning QA sweep.

## Screenshot index (evidence used)

| File | Used for |
|---|---|
| `00-home.png` `00b-home-recheck.png` | Nav rename, grade look |
| `01-research.png` `02-contracts.png` `03-career.png` `04-intel.png` `05-arsenal.png` | Titles + STYLE tab |
| `06-war-room.png` | Standard live, three modes locked |
| `07-galaxy.png` `07b-galaxy-canvas.png` `08-galaxy-after-locked-tap.png` | Item 1 |
| `09-system.png` `09b-system-orbit.png` | Item 2 |
| `10-planet.png` `10b-planet-globe.png` | Item 3 |
| `11-region.png` `12-deploy.png` `12b-deploy-config.png` | Item 4 |
| `14-match.png` `15-pop-chip.png` `16-hud-hazard.png` | Items 6, 8, 11 |
| `17-flicker-frame.png` `18-base-albedo.png` | Items 5, 6 |
| `19-minimap.png` `19b-minimap-context.png` | Item 9 |
| `20-retreat.png` | Item 7 (fail evidence) |
| `22-vo-state.png` | Item 10 |
| `14d-settings-display.png` `14e-grade-row.png` | Items 13 (water FAULT), 14 |
