# v1.33.74 SYSTEM — publication record

## Authority and scope

The owner accepted the proposed repair-and-publish scope with “Repair those ans publish the uodate”: v1.33.74 SYSTEM, Hugging Face Stable (`CREATORJD/massfront-releases`), its established Cloudflare delivery mirror, signed Android installer, and exact packed browser/PWA (`CREATORJD/massfront-playtest`). Required multiplayer backend repair is included. No source archive, native iOS build, or monolithic Galactic optional-pack upload is authorized by this slice. Failed preparation does not consume another number.

Canonical Local checkout verified at `C:/Users/Jason/Documents/Codex/2026-08-01/massfront-rts-mobile-game-for-apple`; HEAD `d180831ca4eb3caffd8bb34dcf20004dbcd86b7f`, existing `cursor/strip-mass-node-bloom` branch. Existing shared changes are preserved.

## Status

**PUBLISHED AND ACTIVATED: v1.33.74 SYSTEM on Stable**, closed out on 2026-09-04 PDT (`2026-09-05T02:54:35.539Z` final hosted-browser acceptance). All required gates passed: final live multiplayer, offline PWA, actual shipped-.73 OTA interruption/install/restart/rollback, real gameplay GL context recovery, live endpoint equivalence/ranges, and fresh hosted browser startup. The first immutable .74 candidate remains preserved and unactivated; only `candidate-r2` was activated. Channel receipts below, not the historical preparation entries, determine publication state.

Prior Stable manifest root: `ec25c9e3f44b6a1f70f308057caef6e35379f3fb45dde6d7983892d331d97263`.

## Published channels

| Channel | Final receipt |
| --- | --- |
| Canonical Local source | Version 1.33.74; existing checkout/branch and unrelated changes preserved. No source archive uploaded and no Git commit created. |
| Packed preview | `www/`, served at `http://127.0.0.1:8901/`; packed-directory SHA-256 `7ef85e4c8ef651d6e58a30e669770cd837de9a117a33329b44bb9c7534668fa6`. Root entry/boot/SW/compatibility HTTP bytes matched. |
| HF Stable + Cloudflare OTA | `https://massfront-update.jasondixon1994.workers.dev/update.json`; HF `CREATORJD/massfront-releases` activation commit `788af33658f1e65ce669c88e7f4cf343c61ba45b`. `update.json`, `MASSFRONT-update.json`, and `update-v1.33.74.json` moved together after guarded prior-root checks. Final root `93b2006041491075e5f99733cecfc0bdbde4240650019655e2e83c1859a5ad8f`; category `system`, 114 files / 96,859,456 bytes. |
| Signed Android installer | `releases/MASSFRONT-v1.33.74-candidate-r2-mobile-install.apk`, 136,345,250 bytes, SHA-256 `18a540c415e5b227f8240de351d3973bd8f738e4ddd000fc33c9461299bfaddf`. v2/v3 signatures, shrink and alignment passed; established certificate SHA-256 `d61aaf77c171f0f1e7841394eb0adaed196e146ad90226a0f07854c29ee073f0`. All 851 shipped www files matched APK public bytes. |
| Browser/PWA Space | `CREATORJD/massfront-playtest`, commit `aed627e1ac2055a3ac2d82904c64265211cf4f36`, static RUNNING. Exact packed www upload, previous README/config preserved, no deletion. Live URL `https://creatorjd-massfront-playtest.static.hf.space/`. |
| Required multiplayer backend | `massfront-auth` deployment `36a81088-3c7e-4f5d-aa3f-7ae114d9667d`, final real baseline + delayed replay 14/14 each. No D1 schema migration or feature-flag reduction. |

Pinned installer: [MASSFRONT v1.33.74 Android installer](https://huggingface.co/datasets/CREATORJD/massfront-releases/resolve/ed860aed17b0771a014af02b880ce32b1cb77a7b/MASSFRONT-v1.33.74-candidate-r2-mobile-install.apk?download=true).

`node tools/verify-release-channels.mjs --version 1.33.74` and `node tools/probe-payload-cors.mjs --no-browser` both passed after activation. Local, HF resolve/raw and Worker ordered inventories, roots and chunk tables agree; all six range-bearing entries passed direct 206/CORS/preflight checks. The post-activation plain-file CORS probe samples 2/108 plain entries; complete byte/hash verification occurred independently before both activations. A subsequent live 4 MiB chunk of the largest artifact matched its hash, and the pinned APK returned HTTP 200 with matching size/hash metadata. Previous .73 artifacts and rollback manifests remain preserved.

Native iOS/IPA and collaborator source archive: intentionally skipped under the owner's standing scope. Apple remains supported through browser/PWA delivery, not a native iOS release.

## Repairs and evidence

- Publisher now separates local preparation, immutable upload, complete redirect-free recovery verification, and guarded activation. Both full and delta inventories are verified; expected-prior identity prevents overwriting an intervening release. HF aliases move in one guarded commit. Previous artifacts and rollback pointers are retained.
- Multiplayer retains bounded exact tick frames, pauses authority for a disconnected/synchronizing seat, resumes from the last actually applied tick, reconciles sequences and pending upgrades, and requires readiness before accepting new orders. Stale socket callbacks cannot terminate a newer connection. Exact replay duplicates cannot charge twice.
- Server unit checks: replay 16/16 and social 437/437. Client replay and upgrade authority regressions pass; building upgrade contract 15/15. Other building, production, utility, Brood terrain and crowd contracts pass.
- Local real Worker/D1/WebSocket test: `.tmp/match-replay-live/mtnorqkl857a9a.json`, 13 checks passed, both QA accounts removed.
- Deployed `massfront-auth` version `ee7da60d-9f02-4608-bc4b-a079b7ffe2fb`; previous deployment `163f4d25-afb5-4ea8-9a29-13358a879bfc` retained for rollback. No D1 migration or feature-flag reduction.
- Production real Worker/D1/WebSocket test: `.tmp/match-replay-live/mtnoxwxzfd133a.json`, 13 checks passed, two isolated QA accounts removed after identity verification. Public social route checks 9/9 passed. Real-time matches, lobby launch, invites, online count and chat are enabled; individual presence remains disabled.
- First production fixture attempt timed out awaiting one acknowledgement; `.tmp/match-replay-live/mtnovrukd98051.json` is preserved. Submitting each fixture order immediately after its own next received tick passed without changing the server's input window. This is not evidence for arbitrary high-latency connections.

## Explicit limits

- Restoring an already started room after Worker eviction/deployment lacks a durable emitted-frame journal. It ends with `recovery_history_unavailable` rather than replaying an unproven snapshot. Fresh rooms remain available. A lost initial start frame followed by a nonzero resume cursor also fails closed.
- This release must not advertise the 2,500-body performance target as achieved: the prior desktop-profile p99 was 101.3 ms against a 33.3 ms target. Broader high-unit optimization remains open.
- Desktop hardware browser verification is not physical Galaxy S25/S24 or Safari device certification. Optional massive-content transfer acceptance on those devices remains open.
- Locked Stage 10 models are not regenerated. The Brood fix uses the existing accepted albedo and normal/roughness assets.
- Cosmetic known issue: the OTA rollback button uses the running `APP_VERSION` at `src/updater.js:3076`, so an upgraded .73 installer can display “packaged v1.33.74.” Actual offline rollback restored the unchanged .73 payload correctly. Correct the label in a later batched UI change; no extra version is consumed for it.

## Final acceptance receipts

- **Actual shipped APK to OTA: PASS**, `.tmp/prepared-ota-upgrade/ota-73-to-74-r2-final-20260905-a/evidence.json`, completed `2026-09-05T02:32:01.661Z`. Exact original .73 APK size/hash and extracted public bytes were preserved; the old updater validated the real prepared .74 manifest. Cancellation/restart retained a verified 4 MiB chunk, and resume did not request it again. All 114 artifacts / 96,859,456 bytes staged with exact hashes; real apply/restart executed all 114, confirmed probation, and produced the final `ad9c15…` compatibility tuple. Both offline restarts, offline hash/decode of both Brood textures absent from the old installer, and real offline rollback to .73 passed. Zero page errors, missing files, or offline payload requests; stable source and released freeze. Root inspected installed and rollback screenshots. This is the actual APK public payload in hardware Chromium, not a physical native-bridge test.
- **Actual graphics recovery: PASS 31/31**, `.tmp/gl-probe-recovery/report.json`. Normal visible controls traversed galaxy, system, planet, region and deployment into a live unpaused offline match. One real `WEBGL_lose_context` loss held simulation while RAF continued; one restore rebuilt fresh linked programs, buffers and textures, preserved CPU terrain, applied the intended one-step quality fallback, and automatically resumed simulation and rendering. Nonempty rendered output, no unexpected shader/GL/page/console errors, stable final packed-www identity, six valid captures, and released freeze. Root inspected loss and restored-battle screenshots. Earlier launcher/account fixture failures remain preserved separately and are not counted as passes.
- **Delivery preflight: PASS**, `.tmp/ota-delivery-repair/update-v1.33.74-prepared-evidence.json`: all 114 files, all 130 chunk hashes, browser CORS/preflight and endpoint ranges verified before activation. Prepared JSON SHA-256 `9017ede72d4f63c400ac9fafc00b076a367cdbdd8b7a323b4dcbfead0d90f8c9`. At `2026-09-05T02:34:17.176Z`, Worker and HF prior aliases still matched .73/root `ec25c9…`; final pinned APK remote metadata matched local bytes.
- **Live hosted browser: PASS**, `.tmp/live-space-release/release-v13374-r2-hosted-accepted/evidence.json`, finished `2026-09-05T02:54:35.539Z`. Fresh isolated AMD 610M D3D11 Chromium, no prior cache/OTA and service workers deliberately blocked for hosted-byte proof. All 12 critical HTTP checks and 117 browser-loaded files, including all 112 runtime scripts, matched final www bytes. Four real UI clicks reached the unobstructed menu; version/compatibility matched .74/ad9c15…, no horizontal document overflow, zero page/HTTP/request errors, stable source and released freeze. Root inspected the live menu screenshot. This proves hosted boot/menu, not a full multiplayer/Apple device certification.
- **Hosted verifier history preserved:** `release-v13374-r2-final` rejected HF's exact 101-byte HTML variable insertion despite a byte-correct pinned upload. `release-v13374-r2-hosted-final` exposed four real HTTP 429s and an intro-control race. A five-minute quiet interval respected host limits without changing identity or bypassing throttling; `release-v13374-r2-hosted-reset-final` had no 429 but rejected legitimate HF CDN redirects and stopped after an already-effective launcher click. The final verifier accepts only the exact observed HTML insertion and proven known-www-to-exact-HF-CDN redirect chain, enforcing every request hop before send and hashing final script responses. Adversarial tests reject altered game bytes, unknown insertion/offset, wrong host/protocol/repository/path/provenance. It re-observes real UI transitions without forcing state and retains strict error/429 gates. These were verifier-only changes, with no new upload or release number. Transient host throttling was observed; one clean final run does not promise unlimited hosting capacity.

## Preparation history (superseded by final receipts above)

### Final r2 preparation and verification

- Final immutable HF upload completed: OTA commit `aec376bc7e079483ac256d2eb44c04bccee31be3`, installer/pinned commit `ed860aed17b0771a014af02b880ce32b1cb77a7b`, namespace `v1.33.74-candidate-r2/`. All 114 files were downloaded and byte/hash verified, with first/last advertised ranges checked. No manifest was activated by this upload. Independent ZIP inspection confirms all 851 shipped `www` files match the final APK byte-for-byte (no missing/different files); `.gitkeep` is excluded.
- Final production acceptance on the `ad9c15…` tuple: `.tmp/match-replay-live/release-v13374-r2-final-baseline-20260905T021556Z.json` and `release-v13374-r2-final-delay80-20260905T021700Z.json`, each PASS 14/14 without retries. Both use the actual deployed Worker/D1/WebSocket service and actual source consumers. The delayed case held sends 80/87 ms, with queued-to-ack 163/171 ms, and preserved exact targets. Exact/empty-frame replay, pending upgrade preservation, exactly-once owner charges, convergence, contiguous continuation and grace expiry passed. All four QA accounts were identity-checked and deleted. Normal registration limits were allowed to expire; none were bypassed or changed.

The preparation hashes immediately below describe the intermediate local r2 before the offline boot fix. It was not uploaded. **Final r2** uses canonical manifest `ad9c15db798d9ca4733e0104cc500d8ecb7f8cd3b52302aaa7ac16b150570274`, the same balance root, and APK SHA-256 `18a540c415e5b227f8240de351d3973bd8f738e4ddd000fc33c9461299bfaddf` (136,345,250 bytes). Full OTA is 96,859,456 bytes across 114 artifacts; release manifest root `93b2006041491075e5f99733cecfc0bdbde4240650019655e2e83c1859a5ad8f`. PrepareOnly and subsequent UploadOnly rebuilds have identical APK hashes and canonical/transport validation passes.

- Offline-origin-denial verification exposed an existing `boot.js` Promise rejection: synchronous `try/catch` did not handle `reg.update()` rejecting while offline. The fix handles that operation's rejection, records `__mfPwaDiag.updateError`, still attempts updates, and preserves independent storage work. A focused test reproduced the original failure, then passed offline reject/success/synchronous throw/registration failure/storage failure/unrelated rejection cases.
- Final PWA `.tmp/pwa-shell/release-v13374-r2-final/evidence.json`: PASS, zero page errors, source stable, real AMD 610M hardware. Two navigations came from the service worker with all 112 scripts cached, failed uncached fetches, and zero successful origin responses after cutoff. Portrait and landscape screenshots inspected. Chromium's `navigator.onLine` remained true under CDP emulation, so origin denial and actual fetch/navigation evidence prove offline operation; the hint was not spoofed. Physical Apple/Safari/installed-standalone acceptance is not claimed.
- A byte comparison against both building and Brood evidence found exactly three changed paths out of 273 recorded inputs: root/packed `boot.js` and the regenerated packed compatibility descriptor. The remaining 270 source/package inputs are identical. Earlier visual/command evidence supports those unchanged subsystems, **not** whole-final-package identity; final boot and recovery are separately rerun.
- Initial GL recovery verifier reached hardware probe recovery but its older menu route stopped at the newly introduced launcher. `.tmp/gl-probe-recovery-failed-v13374-r2-launcher/report.json` preserves the failure. The verifier now serves/hashes `www/` and uses the real offline launcher/onboarding/deployment controls; graphics acceptance awaits its rerun.

- Canonical packaged/OTA multiplayer identity: manifest `9c15efc3f765a3a6280d31a1e075097060a509177f4a8f47778bda0b377dd49a`, balance `589728025661bfca37b7c2460dac69bde7fd0833b340b168e66de8d8b3d8418a`. Actual emitted files pass separate transport-byte checks.
- Prepared installer `releases/MASSFRONT-v1.33.74-candidate-r2-mobile-install.apk`: 136,345,250 bytes, SHA-256 `37653c4fc03932c8b8c6340c55c6d1c898ac65aa6e23f5b913ccf84a3e6bb647`. Build, shrink, signing and alignment passed; this is not yet publication.
- Final multiplayer deployment `36a81088-3c7e-4f5d-aa3f-7ae114d9667d`: .74 negotiates 2–18 ticks/default 8, while legacy clients retain 2–3/default 2. Server replay checks 17/17 and social 437/437 pass. Local real Worker/D1 run `.tmp/match-replay-live/mtnpph755e882e.json` passes all 14 checks with 120 ms intentional outbound delay.
- Production r2 run `.tmp/match-replay-live/mtnpt71s56bbaa.json` admitted both commands at 42–43 ms acknowledgement latency. It exposed an incorrect verifier assertion: B legitimately executed before A's withheld tick, but the assertion required zero charges for both owners. Corrected to require zero A-owner charges while A is pending; later exactly-two-total and convergence assertions remain unchanged. Failure and successful QA cleanup are preserved.
- Follow-up `.tmp/match-replay-live/mtnpyz9z540421.json` received registration HTTP 429 before creating any account. The existing 8/hour anti-abuse policy remains unchanged. Final production runs must wait for normal reset; this is not a multiplayer pass.
- Eight layouts/scales and real building command/production/four-faction activity/utility checks: `.tmp/building-command/release-v13374-r2/evidence.json`, PASS, source stable, no page errors. Root inspected portrait, 200% landscape and faction screenshots. The faction-FX fixture recolors existing buildings, so its old hardscape pads are not foundation evidence.
- Separate Brood placement through `addBld`, including neutral nest: `.tmp/environment-upgrade/brood-after-release-v13374-r2/evidence.json`, PASS. All sites have organic coverage, the neutral nest preserves authoritative height/pathing/hardscape, and foundation refresh creates no flood. Medium/High/Cinematic close/tactical captures preserve paused terrain; root visually confirmed organic foundations and connecting veins.
- First PWA verifier run `.tmp/pwa-shell/release-v13374-r2/evidence.json` failed before loading the game because its DNS isolation rule omitted numeric loopback exclusions. Harness rule corrected while keeping external-network isolation; failure remains preserved, not counted as application acceptance.

### First candidate retained, not released

HF OTA commit `b5d2d51585af3bd4b3788eb63ed1f6129f44a070`; installer/pinned commit `333e29aaeb43ce05e7ccb03677746cb2db37e9bf`. The 114-artifact candidate is 96,859,111 bytes. Its APK is 136,345,250 bytes, SHA-256 `c1ce6871bba49d092e163b665030e9039fa862dfcb2bed0393a333366353d41d`. Signing/alignment passed with the established certificate. All 851 actual www files inside the APK matched their local bytes; two extra generated Cordova bridge files are native-only, and `.gitkeep` is not shipped. This candidate is **not** the final release.

Cross-channel compatibility was repaired and its exact build test passed: shared canonical manifest `9cf409f1e8ada18b8f9dd21648f497a9254bdda33138035119ea358158951d3c`, balance `589728025661bfca37b7c2460dac69bde7fd0833b340b168e66de8d8b3d8418a`; emitted-byte transport fingerprints remain distinct and verified. Active OTA target-version classification is fixed. These hashes will change with the latency repair.

Final-tuple live test `.tmp/match-replay-live/mtnpd1m243294e.json` failed with `stale_target_tick`: B submitted tick 1 + 3 after only 2 ms, but the server was at tick 4 when it rejected the command, 104 ms after submission. Both QA accounts were removed. This is a real timing-window limit, not a reason to retry until a pass or silently widen test tolerances. Legacy clients must retain their old negotiated window; the new .74 client/server window must be explicitly tested before activation. Prior candidate manifests/descriptors are also preserved under `.tmp/release74-first-candidate/`.

## Ownership and continuation

- Integration/root: canonical scope/version checks, `boot.js` offline Promise repair, release metadata/packaging integration, final activation, and this ledger. Shared gameplay/graphics/UI batch files were preserved and verified, not replaced by a shadow checkout.
- Delivery lane: `tools/publish-hf-release.ps1`, `tools/mirror-release-to-cloudflare.mjs`, `tools/repoint-manifests-to-mirror.mjs`, release delivery/activation contracts and tests, and `tools/verify-prepared-ota-upgrade.mjs`; complete immutable/mirror and actual-old-APK transfer evidence. Root alone activated public pointers.
- Multiplayer/PWA lane: `cloudflare/massfront-auth/src/index.js` replay/input-window integration and test, client authority/compatibility integration, `tools/verify-match-replay-live.mjs`, `tools/verify-pwa-shell.mjs`, and `tools/verify-live-space-release.mjs`. Production QA identities were cleaned up; no further deployment is pending.
- Graphics verification lane: `tools/verify-gl-probe-recovery.mjs` packaged real-entry/recovery acceptance. Final report and current www fingerprint agree.

This release slice is complete; the broader 18-stage plan is not. Next safe work is a separately scoped high-unit-performance pass and the known cosmetic rollback label, with source-matched evidence and a later batched release. Physical mobile/WebKit certification, durable room recovery after backend eviction, and massive optional-pack acceptance remain open. Do not increment or publish another release just because a verifier fixture failed.

All verification browsers and cooperative freezes were released. The owned local Worker/D1 test server was stopped; preview `127.0.0.1:8901` remains available. Final `git diff --check` passed (only normal LF/CRLF advisory warnings), and the local signed APK hash was rechecked unchanged after publication. No delegate has further writes or deployment work outstanding.
