import crypto from 'node:crypto';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

/* Fail-closed acceptance gate for the locked Stage 10 model-preparation pack. */

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const CATALOG_FILE = path.join(ROOT, 'tmp', 'stage10-model-review', 'catalog.json');
const OUT = path.join(ROOT, 'tmp', 'stage10-model-repair');
const REPAIR_REPORT_ROOT = path.join(OUT, 'reports');
const PBR_REPORT_ROOT = path.join(OUT, 'pbr-reports');
const PBR_MODEL_ROOT = path.join(OUT, 'pbr-models');
const RENDER_ROOT = path.join(OUT, 'renders');
const ZFIGHT_ROOT = path.join(OUT, 'z-fighting-reports');
const REPAIR_SCRIPT = path.join(ROOT, 'tools', 'blender', 'repair-stage10-model-pack.py');
const PBR_SCRIPT = path.join(ROOT, 'tools', 'blender', 'texture-stage10-model-pack.py');
const RENDER_SCRIPT = path.join(ROOT, 'tools', 'blender', 'render-stage10-repaired-model-pack.py');
const ZFIGHT_SCRIPT = path.join(ROOT, 'tools', 'blender', 'audit-stage10-z-fighting.py');
const RASTER_CLEANUP_SCRIPT = path.join(ROOT, 'tools', 'blender', 'stage10_raster_cleanup.py');
const FROZEN_REPAIR_SCRIPT_SHA256 = 'ef9cb66d9f41a0a42c7439b20d31aa43b3ae56e2c2127ba5afd7fedf71a9f54d';
const FROZEN_RASTER_CLEANUP_SCRIPT_SHA256 = '1d2dec9d59864a218786fa56b5d9df44d74ac5f430dcd76e6d890da2fce1e72b';
const FULL_MATERIAL = Object.freeze({models:327,slots:1926,uniqueNonempty:149,uniqueWithUnnamed:150,unnamedSlots:60,coverageSha256:'e57e5524f908ceda81f1640099ee0623df190c0b4357cba5dcb22af77736f447'});
const EXPECTED = Object.freeze({world:320,spline:7,candidates:327,excluded:0,included:327,repairPipeline:18,zFightingAudit:3,pbrPipeline:1,renderer:2,renderResolution:768});
const REPAIR_LOCKED = new Map();
const EXCLUSIONS = new Map();
const DELETED_SPLINE_PROPS = [
  'MF_PROP_CARGODEPOT_01','MF_PROP_CRYSTAL_01','MF_PROP_DEPOSIT_01','MF_PROP_GEYSER_01',
  'MF_PROP_ICESPIRE_01','MF_PROP_MOUNTAIN_02','MF_PROP_RELIC_01','MF_PROP_ROCK_01',
  'MF_PROP_ROCKARCH_01','MF_PROP_TREE_01','MF_PROP_TREE_02','MF_PROP_WRECK_01',
];
const REPAIR_STATUSES = new Set(['READY_FOR_TEXTURE_GENERATION','UV_READY_GEOMETRY_REVIEW','RECONSTRUCTION_REQUIRED']);
const REPAIR_ELIGIBLE_STATUSES = Object.freeze(['READY_FOR_TEXTURE_GENERATION','UV_READY_GEOMETRY_REVIEW']);
const REQUIRED_REPAIR_PROOFS = Object.freeze(['MATERIAL_SEMANTIC','RASTER_CLEANUP','TILED_UV_CONSTRUCTION','PRE_EXPORT_TILED_UV','POST_EXPORT_OUTPUT_TILED_UV','POST_EXPORT_TEXTURE_SOURCE_TILED_UV']);
const PRODUCTION_SET_POLICY = Object.freeze({schema:'MassfrontStage10ProductionSetPolicyV1',repairEligibleStatuses:REPAIR_ELIGIBLE_STATUSES,repairQuarantineStatus:'RECONSTRUCTION_REQUIRED',requiredRepairProofs:REQUIRED_REPAIR_PROOFS,downstreamFailureDisposition:'QUARANTINED_NO_PROMOTION',canonicalSourcesUntouched:true});
const SHA_PATTERN = /^[0-9a-f]{64}$/;
const GL_REPEAT = 10497;
const CUBIC_STRETCH_LIMIT = Math.sqrt(3)+0.01;
const TEXEL_SCALE_ERROR_LIMIT = 0.005;
const SOURCE_SLIVER_QUALITY_LIMIT = 1.0e-4;
const TILED_UV_METRIC_METHOD = 'WORLD_TRIANGLE_TANGENT_JACOBIAN_PROJECTED_WORLD_AREA_FLOOR';
const TILED_UV_ANCHOR_METHOD = 'PER_POLYGON_INTEGRAL_REPEAT_TILE_ANCHOR';
const TILED_UV_AGGREGATE_METHOD = 'BLENDER_WORLD_CUBIC_UV_CONSTRUCTION_AND_JACOBIAN_V1';
const TILED_UV_WORLD_DOUBLE_AREA_FLOOR = 1.0e-12;
const TILED_UV_PROJECTED_FLOOR_METHOD = 'WORLD_DOUBLE_AREA_FLOOR_TIMES_ABS_DOMINANT_NORMAL_COMPONENT';
const TILED_UV_POST_INFERENCE_METHOD = 'ALL_LOOP_PROJECTED_MINUS_STORED_UNIQUE_INTEGER_WITH_FLOAT32_ULP_BOUND';
const TILED_UV_POST_BOUND_METHOD = 'HIERARCHICAL_FLOAT32_AFFINE_FORWARD_ERROR_PLUS_UV_AND_GLTF_V_FLIP_ULPS';
const TILED_UV_POST_BOUND_FORMULA = '((PROPAGATED_INPUT_ULPS+COEFFICIENT_ULPS+GAMMA_7_TIMES_SUM_ABS_AFFINE_TERMS+COMPOSITION_REALIZATION_DELTA)/TILE_METRES)+PROJECTED_UV_ULP+STORED_UV_ULP+OPTIONAL_GLTF_V_FLIP_ULP';
const TILED_UV_PRE_INFERENCE_METHOD = 'NOT_APPLICABLE_PRE_EXPORT_DETERMINISTIC_FLOOR_FIRST_LOOP';
const TILED_UV_PRE_BOUND_METHOD = 'EXACT_PRE_EXPORT_FLOAT32_STORAGE_REPLAY';
const TILED_UV_PRE_BOUND_FORMULA = 'NOT_APPLICABLE_PRE_EXPORT';
const RAW_GLTF_AUDIT_METHOD = 'GLB_ACCESSOR_TRIANGLE_INDEX_WORLD_V1';
const CONTENT_EQUIVALENT_REBIND_SCHEMA = 'MassfrontStage10PbrContentEquivalentRebindV1';
const CONTENT_EQUIVALENT_REBIND_METHOD = 'BYTE_IDENTICAL_REPAIR_INPUT_AND_PBR_OUTPUT_FRESH_CURRENT_VALIDATION';
const CONTENT_EQUIVALENT_REBIND_LEGACY_PIPELINE_SHA256 = '1da057bd3a81467ba81205a97c149310c45142bd017ccdd2423fe3a25b116601';
const CONTENT_EQUIVALENT_REBIND_POLICY = Object.freeze({schema:CONTENT_EQUIVALENT_REBIND_SCHEMA,method:CONTENT_EQUIVALENT_REBIND_METHOD,legacyPipelineScriptSha256:CONTENT_EQUIVALENT_REBIND_LEGACY_PIPELINE_SHA256,requested:true});
const RASTER_ACCEPTANCE = Object.freeze({method:'SIGNED_SAME_WINDING_PAIR_LOCAL_AFFINE_DEPTH_BAND',signedSameWinding:true,normalAngleDegrees:0.5,normalDotMinimum:Math.cos(0.5*Math.PI/180),planeDistanceM:5.0e-4,minimumOverlapAreaM2:1.0e-6,exactDuplicatesIgnoreAreaFloor:true,minimumWidthExemption:false,cleanupDepthGuardOnly:true,cleanupGuardScope:'MUTATION_DEPTH_AND_FLOAT32_REMOVAL_BOUNDARY_ONLY',adjacentEdgeExclusion:'SAME_OBJECT_EXACT_TWO_ENDPOINT_EDGE_WITH_STRICT_OPPOSITE_SIDE',aggregateCoherentCrossingResolution:'IMMUTABLE_SOURCE_LINEAGE_PAIR_STABLE_OWNER_WITHIN_ACCEPTED_PAIR_LOCAL_COVERAGE',aggregateCoherentCrossingExpandsAcceptedCoverage:false,subsequentPassReconstructionPlane:'IMMUTABLE_SOURCE_LINEAGE_PLANE_WITH_CANONICAL_EDGES',subsequentPassReconstructionExpandsAcceptedCoverage:false,sameLineagePartitionRealization:'TOPOLOGY_ACCEPTED_PAIR_TRIGGERED_ADJACENT_FLOAT32_OMITTED_AXIS_ULP',sameLineagePartitionRealizationMergesProjectedVertices:false,sameLineagePartitionRealizationExpandsAcceptedCoverage:false,maximumCleanupPasses:6,pairCountMustStrictlyDecrease:false,cleanupProgressRule:'POSITIVE_ACCEPTED_MASK_SOURCE_SURFACE_INTERSECTION_OR_SAME_LINEAGE_OR_EXACT_DUPLICATE_CONSOLIDATION_WITH_PAIR_DECREASE',repeatedGeometryStateAllowed:false});
const LINEAGE_PROOF_POLICY = Object.freeze({method:'IMMUTABLE_SOURCE_TRIANGLE_PLANE_UNION_AND_CONSTRAINED_RETRIANGULATION',lineage:'INITIAL_OWNER_AND_SOURCE_TRIANGLE_ID',scope:'IN_MEMORY_CLEANUP_AID_NOT_REQUIRED_BY_FINAL_ACCEPTANCE',carriedAcrossRetriangulation:true,sameOwnerRequired:true,sameMaterialRequired:true,globalMinimumWidthExemption:false,finalArtifactAcceptanceDependsOnLineage:false});
const RASTER_DEPENDENCY = Object.freeze({shapelyVersion:'2.1.2',geosVersion:'3.13.1',requiredShapelyVersion:'2.1.2',requiredGeosVersion:'3.13.1',passed:true});
const PBR_RASTER_POLICY = Object.freeze({alphaMode:'OPAQUE',doubleSided:false,backfaceCulling:true});
const RASTER_BOUNDS_DRIFT_LIMIT = 5.01e-4;

let checkCount=0, failedCount=0;
const failureSamples={};
const stageCounts={};
function short(value){const text=typeof value==='string'?value:JSON.stringify(value);return text&&text.length>420?text.slice(0,417)+'...':text;}
function check(condition,code,details=undefined){checkCount++;const stage=String(code).split('.')[0],counts=stageCounts[stage]||={checks:0,failed:0};counts.checks++;if(condition)return true;failedCount++;counts.failed++;const samples=failureSamples[stage]||=[];if(samples.length<10)samples.push(details===undefined?code:code+': '+short(details));return false;}
function stableValue(value){if(Array.isArray(value))return value.map(stableValue);if(value&&typeof value==='object'&&!Buffer.isBuffer(value))return Object.fromEntries(Object.keys(value).sort().map(key=>[key,stableValue(value[key])]));return value;}
function stable(value){return JSON.stringify(stableValue(value));}
function equal(actual,expected,code){return check(stable(actual)===stable(expected),code,{actual,expected});}
function sha256Buffer(buffer){return crypto.createHash('sha256').update(buffer).digest('hex');}
function sha256File(file){return fs.existsSync(file)&&fs.statSync(file).isFile()?sha256Buffer(fs.readFileSync(file)):null;}
function canonicalHash(value){return sha256Buffer(Buffer.from(stable(value),'utf8'));}
function posix(file){return path.relative(ROOT,file).split(path.sep).join('/');}
function isWithin(file,parent){const relative=path.relative(path.resolve(parent),path.resolve(file));return relative===''||(!relative.startsWith('..'+path.sep)&&relative!=='..'&&!path.isAbsolute(relative));}
function safeId(value){return String(value).replaceAll('/','_').replaceAll('\\','_');}
function sum(values){return values.reduce((total,value)=>total+value,0);}
function sorted(values){return [...values].sort((a,b)=>String(a).localeCompare(String(b)));}
function setEqual(actual,expected,code){return equal(sorted(actual),sorted(expected),code);}
function finiteNumber(value){return typeof value==='number'&&Number.isFinite(value);}
function nearNumber(actual,expected,absolute=1e-6,relative=0){return finiteNumber(actual)&&finiteNumber(expected)&&Math.abs(actual-expected)<=Math.max(absolute,relative*Math.max(Math.abs(actual),Math.abs(expected)));}
function nearArray(actual,expected,absolute=1e-6,relative=0){return Array.isArray(actual)&&Array.isArray(expected)&&actual.length===expected.length&&actual.every((value,index)=>nearNumber(value,expected[index],absolute,relative));}
function canonicalTextBinding(textValue,value,hash){if(typeof textValue!=='string'||!SHA_PATTERN.test(hash||'')||sha256Buffer(Buffer.from(textValue,'utf8'))!==hash)return false;try{return stable(JSON.parse(textValue))===stable(value);}catch{return false;}}
function materialSemanticItemGreen(report){const contract=report?.materialSemanticContract||{},names=contract.originalMaterialNames;if(!Array.isArray(names)||names.some(name=>typeof name!=='string'))return false;const unique=[...new Set(names)].sort(),resolutions=contract.resolutions;if(contract.schema!=='MassfrontStage10MaterialSemanticContractV1'||contract.policy!=='EXPLICIT_SEMANTIC_PRIORITY_THEN_STABLE_OWNER'||contract.unnamedPolicy!=='LOWEST_PRIORITY_STABLE_OWNER'||contract.preflightScope!=='ORIGINAL_GLB_JSON_MATERIAL_DEFINITIONS'||contract.passed!==true||contract.sourceSha256!==report.sourceSha256||contract.materialSlotCount!==names.length||contract.uniqueIncludingUnnamed!==unique.length||contract.uniqueNonemptyNames!==unique.filter(Boolean).length||contract.unnamedSlots!==names.filter(name=>!name).length||!Array.isArray(resolutions)||resolutions.length!==unique.length)return false;for(let index=0;index<unique.length;index++){const resolution=resolutions[index];if(resolution?.name!==unique[index]||!Number.isInteger(resolution.rank)||typeof resolution.semantic!=='string'||!resolution.semantic||resolution.semantic==='UNKNOWN'||resolution.unnamedStablePolicy!==!unique[index])return false;}return contract.materialInventorySha256===canonicalHash({key:report.key,sourceSha256:report.sourceSha256,materialNames:names});}
function aggregateCoherentProofGreen(cleanup){
  const proof=cleanup?.lineageProof||{},passes=cleanup?.stabilizationPasses;
  const countFields=['aggregateCoherentLineagePairGroups','aggregateCoherentCrossingGroups','aggregateMixedSignedWinnerGroups','aggregateCoherentPairAssignments'];
  if(!Array.isArray(passes))return false;
  const counts=Object.fromEntries(countFields.map(field=>[field,0]));let coverageArea=0,maximumDelta=0;
  for(const pass of passes){
    const conflicts=pass?.conflicts||{};
    if(countFields.some(field=>!Number.isInteger(conflicts[field])||conflicts[field]<0)||!finiteNumber(conflicts.aggregateCoherentPairLocalCoverageAreaSumM2)||conflicts.aggregateCoherentPairLocalCoverageAreaSumM2<0||!finiteNumber(conflicts.aggregateCoherentMaximumCoverageDeltaM2)||conflicts.aggregateCoherentMaximumCoverageDeltaM2<0||conflicts.aggregateCoherentCoveragePreserved!==true||conflicts.aggregateCoherentCrossingGroups>conflicts.aggregateCoherentLineagePairGroups||conflicts.aggregateMixedSignedWinnerGroups>conflicts.aggregateCoherentLineagePairGroups||conflicts.aggregateCoherentPairAssignments<conflicts.aggregateCoherentLineagePairGroups)return false;
    for(const field of countFields)counts[field]+=conflicts[field];
    coverageArea+=conflicts.aggregateCoherentPairLocalCoverageAreaSumM2;maximumDelta=Math.max(maximumDelta,conflicts.aggregateCoherentMaximumCoverageDeltaM2);
  }
  return countFields.every(field=>cleanup[field]===counts[field]&&proof[field]===counts[field])&&nearNumber(cleanup.aggregateCoherentPairLocalCoverageAreaSumM2,coverageArea,1e-12,1e-12)&&nearNumber(proof.aggregateCoherentPairLocalCoverageAreaSumM2,coverageArea,1e-12,1e-12)&&nearNumber(cleanup.aggregateCoherentMaximumCoverageDeltaM2,maximumDelta,1e-12,1e-12)&&nearNumber(proof.aggregateCoherentMaximumCoverageDeltaM2,maximumDelta,1e-12,1e-12)&&cleanup.aggregateCoherentCoveragePreserved===true&&proof.aggregateCoherentCoveragePreserved===true&&proof.aggregateCoherentCrossingExpandsAcceptedCoverage===false&&cleanup.proofExpectations?.aggregateCoherentCrossingExpandsAcceptedCoverage===false&&cleanup.proofExpectations?.aggregateCoherentCoveragePreserved===true;
}
function immutablePlanePartitionProofGreen(cleanup){
  const lineage=cleanup?.lineageProof||{},partition=cleanup?.float32PartitionRealizationProof,objects=cleanup?.objects,passes=cleanup?.stabilizationPasses,expectations=cleanup?.proofExpectations||{};
  if(!partition||!Array.isArray(objects)||!Array.isArray(passes)||stable(lineage.float32PartitionRealization)!==stable(partition))return false;
  let immutableCount=0,immutableArea=0;
  for(const pass of passes){const conflicts=pass?.conflicts||{},count=conflicts.immutablePlaneSubsequentPassReconstructions,area=conflicts.immutablePlaneSubsequentPassReconstructionAreaM2;if(!Number.isInteger(count)||count<0||!finiteNumber(area)||area<0||conflicts.immutablePlaneSubsequentPassAcceptedCoverageExpanded!==false)return false;immutableCount+=count;immutableArea+=area;}
  if(lineage.subsequentPassReconstructionPlane!=='IMMUTABLE_SOURCE_LINEAGE_PLANE_WITH_CANONICAL_EDGES'||cleanup.immutablePlaneSubsequentPassReconstructions!==immutableCount||lineage.immutablePlaneSubsequentPassReconstructions!==immutableCount||cleanup.immutablePlaneSubsequentPassReconstructionAreaM2!==immutableArea||lineage.immutablePlaneSubsequentPassReconstructionAreaM2!==immutableArea||cleanup.immutablePlaneSubsequentPassAcceptedCoverageExpanded!==false||lineage.immutablePlaneSubsequentPassAcceptedCoverageExpanded!==false||expectations.subsequentPassReconstructionExpandsAcceptedCoverage!==false)return false;
  const integers=['float32PartitionRealizationEnabledConsolidations','float32PartitionRealizationApplications','float32PartitionRealizationAdjustments','float32PartitionRealizationAcceptedPairsBefore','float32PartitionRealizationAcceptedPairsAfter','float32PartitionRealizationDegenerateTriangles','float32PartitionRealizationInvertedTriangles','float32PartitionRealizationInternalDuplicateFaces','float32PartitionRealizationInternalEdgeViolations','float32PartitionRealizationNearDuplicateSharedEndpointViolations','lineageScopedSharedMeshVertexIndexReuses'];
  const numerics=['float32PartitionRealizationMaximumProjectedOverlapAreaM2','float32PartitionRealizationTotalProjectedOverlapAreaM2','float32PartitionRealizationMaximumProjectedSymmetricDifferenceM2','float32PartitionRealizationTotalProjectedSymmetricDifferenceM2','float32PartitionRealizationMaximumCoverageExpansionAreaM2','float32PartitionRealizationTotalCoverageExpansionAreaM2','float32PartitionRealizationMaximumPreexistingSymmetricDifferenceM2','float32PartitionRealizationMaximumPreexistingExpansionAreaM2','float32PartitionRealizationMaximumSourcePlaneDriftM','float32PartitionRealizationMaximumWorldShiftM'];
  for(const item of objects){if(!item||integers.some(field=>!Number.isInteger(item[field])||item[field]<0)||numerics.some(field=>!finiteNumber(item[field])||item[field]<0)||item.float32PartitionRealizationApplications>item.float32PartitionRealizationEnabledConsolidations)return false;if(item.float32PartitionRealizationApplications>0&&(item.float32PartitionRealizationAdjustments<=0||item.lineageScopedSharedMeshVertexIndexReuses<=0))return false;}
  const applied=objects.filter(item=>item.float32PartitionRealizationApplications>0),maximum=field=>Math.max(0,...applied.map(item=>item[field])),total=field=>sum(applied.map(item=>item[field]));
  const expected={
    method:'TOPOLOGY_ACCEPTED_PAIR_TRIGGERED_ADJACENT_FLOAT32_OMITTED_AXIS_ULP',
    enabledConsolidations:sum(objects.map(item=>item.float32PartitionRealizationEnabledConsolidations)),applications:sum(objects.map(item=>item.float32PartitionRealizationApplications)),adjustments:sum(objects.map(item=>item.float32PartitionRealizationAdjustments)),acceptedPairsBefore:sum(objects.map(item=>item.float32PartitionRealizationAcceptedPairsBefore)),acceptedPairsAfter:sum(objects.map(item=>item.float32PartitionRealizationAcceptedPairsAfter)),
    allFreshScansZero:applied.every(item=>item.float32PartitionRealizationAllFreshScansZero===true),allDistinctProjectedVerticesPreserved:applied.every(item=>item.float32PartitionRealizationAllDistinctProjectedVerticesPreserved===true),allMergesProjectedVerticesFalse:applied.every(item=>item.float32PartitionRealizationAllMergesProjectedVerticesFalse===true),
    maximumProjectedOverlapAreaM2:maximum('float32PartitionRealizationMaximumProjectedOverlapAreaM2'),totalProjectedOverlapAreaM2:total('float32PartitionRealizationTotalProjectedOverlapAreaM2'),maximumProjectedSymmetricDifferenceM2:maximum('float32PartitionRealizationMaximumProjectedSymmetricDifferenceM2'),totalProjectedSymmetricDifferenceM2:total('float32PartitionRealizationTotalProjectedSymmetricDifferenceM2'),maximumCoverageExpansionAreaM2:maximum('float32PartitionRealizationMaximumCoverageExpansionAreaM2'),totalCoverageExpansionAreaM2:total('float32PartitionRealizationTotalCoverageExpansionAreaM2'),
    acceptedCoverageExpanded:applied.some(item=>item.float32PartitionRealizationAcceptedCoverageExpanded===true),allExpandsAcceptedCoverageFalse:applied.every(item=>item.float32PartitionRealizationAllExpandsAcceptedCoverageFalse===true),allPreexistingCanonicalEdgeCoverageDeltasUnchanged:applied.every(item=>item.float32PartitionRealizationAllPreexistingCoverageDeltasUnchanged===true),maximumPreexistingCanonicalEdgeSymmetricDifferenceM2:maximum('float32PartitionRealizationMaximumPreexistingSymmetricDifferenceM2'),maximumPreexistingCanonicalEdgeExpansionAreaM2:maximum('float32PartitionRealizationMaximumPreexistingExpansionAreaM2'),
    degenerateTriangles:sum(applied.map(item=>item.float32PartitionRealizationDegenerateTriangles)),invertedTriangles:sum(applied.map(item=>item.float32PartitionRealizationInvertedTriangles)),internalDuplicateFaces:sum(applied.map(item=>item.float32PartitionRealizationInternalDuplicateFaces)),internalEdgeViolations:sum(applied.map(item=>item.float32PartitionRealizationInternalEdgeViolations)),nearDuplicateIntendedSharedEndpointViolations:sum(applied.map(item=>item.float32PartitionRealizationNearDuplicateSharedEndpointViolations)),
    allRepresentationalBoundsPassed:applied.every(item=>item.float32PartitionRealizationAllRepresentationalBoundsPassed===true),allActualDriftBoundsPassed:applied.every(item=>item.float32PartitionRealizationAllActualDriftBoundsPassed===true),maximumSourcePlaneDriftM:maximum('float32PartitionRealizationMaximumSourcePlaneDriftM'),maximumWorldShiftM:maximum('float32PartitionRealizationMaximumWorldShiftM'),sharedMeshVertexIndexReuses:sum(objects.map(item=>item.lineageScopedSharedMeshVertexIndexReuses)),sharedMeshVertexIndexReusePassed:objects.filter(item=>item.mutated).every(item=>item.sharedMeshVertexIndexReusePassed===true),
  };
  expected.passed=expected.acceptedPairsAfter===0&&expected.allFreshScansZero&&expected.allDistinctProjectedVerticesPreserved&&expected.allMergesProjectedVerticesFalse&&expected.maximumProjectedOverlapAreaM2===0&&expected.totalProjectedOverlapAreaM2===0&&expected.maximumProjectedSymmetricDifferenceM2===0&&expected.totalProjectedSymmetricDifferenceM2===0&&expected.maximumCoverageExpansionAreaM2===0&&expected.totalCoverageExpansionAreaM2===0&&!expected.acceptedCoverageExpanded&&expected.allExpandsAcceptedCoverageFalse&&expected.allPreexistingCanonicalEdgeCoverageDeltasUnchanged&&expected.degenerateTriangles===0&&expected.invertedTriangles===0&&expected.internalDuplicateFaces===0&&expected.internalEdgeViolations===0&&expected.nearDuplicateIntendedSharedEndpointViolations===0&&expected.allRepresentationalBoundsPassed&&expected.allActualDriftBoundsPassed&&expected.maximumSourcePlaneDriftM<=RASTER_BOUNDS_DRIFT_LIMIT&&expected.sharedMeshVertexIndexReusePassed;
  const expectedExpectations={float32PartitionRealizationPassed:true,float32PartitionProjectedOverlapAreaM2:0,float32PartitionProjectedSymmetricDifferenceM2:0,float32PartitionCoverageExpansionAreaM2:0,float32PartitionAcceptedPairsAfter:0,float32PartitionSharedMeshVertexIndexReusePassed:true,float32PartitionPreexistingCanonicalEdgeCoverageDeltaUnchanged:true};
  return stable(partition)===stable(expected)&&expected.passed===true&&Object.entries(expectedExpectations).every(([field,value])=>expectations[field]===value);
}
function lineageProofGreen(cleanup){
  const proof=cleanup?.lineageProof||{},objects=cleanup?.objects;
  const numeric=['maximumSourcePlaneMeasuredStoredDriftM','maximumSourcePlaneArithmeticGuardM','maximumSourcePlaneMeasuredStoredDriftPlusArithmeticGuardM','maximumSourcePlaneRepresentationalErrorBoundM'];
  if(!Array.isArray(objects)||objects.some(item=>!item||!Number.isInteger(item.lineageConsolidationCount)||item.lineageConsolidationCount<0||!Number.isInteger(item.sourcePlaneProofConsolidationCount)||item.sourcePlaneProofConsolidationCount<0||item.sourcePlaneProofConsolidationCount>item.lineageConsolidationCount||numeric.some(field=>!finiteNumber(item[field])||item[field]<0)||item.maximumSourcePlaneMeasuredStoredDriftPlusArithmeticGuardM>RASTER_BOUNDS_DRIFT_LIMIT||item.allSourcePlaneWorldOriginCancellationAvoided!==true))return false;
  const count=sum(objects.map(item=>item.lineageConsolidationCount)),proofCount=sum(objects.map(item=>item.sourcePlaneProofConsolidationCount));
  const maxima=Object.fromEntries(numeric.map(field=>[field,Math.max(0,...objects.map(item=>item[field]))]));
  const cancellation=objects.filter(item=>item.sourcePlaneProofConsolidationCount).every(item=>item.allSourcePlaneWorldOriginCancellationAvoided===true);
  return Object.entries(LINEAGE_PROOF_POLICY).every(([field,value])=>proof[field]===value)&&Number.isInteger(proof.sameLineagePairsConsolidated)&&proof.sameLineagePairsConsolidated>=0&&Number.isInteger(proof.sameLineageCoherentCrossingPairsConsolidated)&&proof.sameLineageCoherentCrossingPairsConsolidated>=0&&proof.lineageConsolidationCount===count&&proof.sourcePlaneProofConsolidationCount===proofCount&&proofCount<=count&&Object.entries(maxima).every(([field,value])=>proof[field]===value)&&proof.sourcePlaneActualStoredDriftLimitM===RASTER_BOUNDS_DRIFT_LIMIT&&proof.maximumSourcePlaneMeasuredStoredDriftPlusArithmeticGuardM<=proof.sourcePlaneActualStoredDriftLimitM&&proof.sourcePlaneActualStoredDriftProofPassed===true&&proof.sourcePlaneRepresentationalErrorBoundIsInformational===true&&proof.allSourcePlaneWorldOriginCancellationAvoided===cancellation&&cancellation===true&&aggregateCoherentProofGreen(cleanup)&&immutablePlanePartitionProofGreen(cleanup);
}
function rasterCleanupProgressGreen(cleanup){
  const passes=cleanup?.stabilizationPasses,sequence=cleanup?.pairCountSequence,count=cleanup?.stabilizationPassCount;
  const topNumbers=['surfaceAreaBeforeM2','surfaceAreaAfterM2','surfaceAreaRemovedM2','recordLocalRemovalIntersectionSurfaceAreaSumM2','lineageRemovalIntersectionSurfaceAreaSumM2','positiveRemovalEvidenceAreaSumM2'];
  if(cleanup?.schema!=='MassfrontStage10RasterCleanupV2'||stable(cleanup.contract)!==stable(RASTER_ACCEPTANCE)||cleanup.passed!==true||cleanup.acceptedPairsAfter!==0||cleanup.exactDuplicatePairsAfter!==0||cleanup.remainingPairs!==0||cleanup.stabilized!==true||cleanup.progressRule!==RASTER_ACCEPTANCE.cleanupProgressRule||cleanup.progressValidatedPasses!==true||cleanup.repeatedGeometryStateDetected!==false||!Number.isInteger(count)||count<0||count>RASTER_ACCEPTANCE.maximumCleanupPasses||!Array.isArray(passes)||passes.length!==count||!Array.isArray(sequence)||sequence.length!==count+1||sequence.some(value=>!Number.isInteger(value)||value<0)||sequence[0]!==cleanup.acceptedPairsBefore||sequence.at(-1)!==0||topNumbers.some(field=>!finiteNumber(cleanup[field]))||!nearNumber(cleanup.surfaceAreaRemovedM2,cleanup.surfaceAreaBeforeM2-cleanup.surfaceAreaAfterM2,1e-12,1e-12)||!SHA_PATTERN.test(cleanup.initialGeometryStateSha256||'')||!SHA_PATTERN.test(cleanup.finalGeometryStateSha256||''))return false;
  const states=[cleanup.initialGeometryStateSha256],nonDecreasing=[];let previousSurface=cleanup.surfaceAreaBeforeM2;const sums={recordLocalRemovalIntersectionSurfaceAreaSumM2:0,lineageRemovalIntersectionSurfaceAreaSumM2:0,positiveRemovalEvidenceAreaSumM2:0};
  for(let index=0;index<passes.length;index++){
    const pass=passes[index],before=sequence[index],after=sequence[index+1];
    const numeric=['surfaceAreaBeforeM2','surfaceAreaAfterM2','surfaceAreaRemovedM2','recordLocalRemovalIntersectionSurfaceAreaM2','lineageRemovalIntersectionSurfaceAreaM2','positiveRemovalEvidenceAreaSumM2'];
    const strictlyDecreased=after<before,positiveRemoval=finiteNumber(pass?.positiveRemovalEvidenceAreaSumM2)&&pass.positiveRemovalEvidenceAreaSumM2>0,consolidation=pass?.consolidationPairProgress===true,expectedConsolidation=strictlyDecreased&&((pass?.conflicts?.sameLineagePairsConsolidated||0)>0||(pass?.conflicts?.exactDuplicateConflicts||0)>0);
    if(!pass||pass.pass!==index+1||pass.pairsBefore!==before||pass.pairsAfter!==after||pass.geometryStateSha256Before!==states.at(-1)||!SHA_PATTERN.test(pass.geometryStateSha256After||'')||numeric.some(field=>!finiteNumber(pass[field]))||pass.strictlyDecreased!==strictlyDecreased||pass.surfaceAreaStrictlyDecreased!==(pass.surfaceAreaRemovedM2>0)||pass.positiveAcceptedMaskRemoval!==positiveRemoval||pass.consolidationPairProgress!==expectedConsolidation||pass.progressRuleSatisfied!==(positiveRemoval||consolidation)||pass.progressRuleSatisfied!==true||(consolidation&&!strictlyDecreased)||(!strictlyDecreased&&!positiveRemoval)||!nearNumber(pass.surfaceAreaBeforeM2,previousSurface,1e-12,1e-12)||!nearNumber(pass.surfaceAreaRemovedM2,pass.surfaceAreaBeforeM2-pass.surfaceAreaAfterM2,1e-12,1e-12)||!nearNumber(pass.positiveRemovalEvidenceAreaSumM2,pass.recordLocalRemovalIntersectionSurfaceAreaM2+pass.lineageRemovalIntersectionSurfaceAreaM2,1e-12,1e-12))return false;
    if(states.includes(pass.geometryStateSha256After))return false;
    states.push(pass.geometryStateSha256After);
    previousSurface=pass.surfaceAreaAfterM2;sums.recordLocalRemovalIntersectionSurfaceAreaSumM2+=pass.recordLocalRemovalIntersectionSurfaceAreaM2;sums.lineageRemovalIntersectionSurfaceAreaSumM2+=pass.lineageRemovalIntersectionSurfaceAreaM2;sums.positiveRemovalEvidenceAreaSumM2+=pass.positiveRemovalEvidenceAreaSumM2;
    if(after>=before)nonDecreasing.push(index+1);
  }
  return states.at(-1)===cleanup.finalGeometryStateSha256&&nearNumber(previousSurface,cleanup.surfaceAreaAfterM2,1e-12,1e-12)&&Object.entries(sums).every(([field,value])=>nearNumber(cleanup[field],value,1e-12,1e-12))&&stable(cleanup.nonDecreasingPairPasses)===stable(nonDecreasing)&&cleanup.strictlyDecreasingPasses===(nonDecreasing.length===0);
}
function readJson(file,label){if(!check(fs.existsSync(file)&&fs.statSync(file).isFile(),label+'.exists',posix(file)))return null;try{return JSON.parse(fs.readFileSync(file,'utf8'));}catch(error){check(false,label+'.json',error.message);return null;}}
function walkFiles(directory,predicate=()=>true){if(!fs.existsSync(directory)||!fs.statSync(directory).isDirectory())return[];const files=[];const walk=current=>{for(const entry of fs.readdirSync(current,{withFileTypes:true})){const full=path.join(current,entry.name);if(entry.isDirectory())walk(full);else if(predicate(full))files.push(full);}};walk(directory);return files.sort((a,b)=>a.localeCompare(b));}
function resolveRef(reference,boundary,code){const valid=typeof reference==='string'&&reference.length>0&&!path.isAbsolute(reference)&&!reference.includes('\\')&&reference===path.posix.normalize(reference)&&!reference.startsWith('../')&&reference!=='..';check(valid,code+'.relative',reference);const resolved=valid?path.resolve(ROOT,reference):ROOT;check(valid&&isWithin(resolved,boundary),code+'.boundary',reference);return resolved;}
function verifyFileRecord(reference,expectedHash,expectedBytes,boundary,code){const file=resolveRef(reference,boundary,code);const exists=check(fs.existsSync(file)&&fs.statSync(file).isFile(),code+'.exists',reference);check(SHA_PATTERN.test(expectedHash||''),code+'.hash-format',expectedHash);if(exists){check(sha256File(file)===expectedHash,code+'.hash',reference);if(expectedBytes!==undefined)check(fs.statSync(file).size===expectedBytes,code+'.bytes',reference);}return file;}
function countsBy(items,field){const result={};for(const item of items){const key=String(item[field]);result[key]=(result[key]||0)+1;}return Object.fromEntries(Object.entries(result).sort(([a],[b])=>a.localeCompare(b)));}
function familyRepairCounts(items){const result={};for(const item of items){const family=result[item.family]||={total:0,statuses:{}};family.total++;family.statuses[item.status]=(family.statuses[item.status]||0)+1;}return result;}
function materialSemanticSummaryGreen(summaryContract,catalogContract,reports){
  if(!summaryContract||summaryContract.schema!=='MassfrontStage10MaterialSemanticContractV1'||summaryContract.policy!=='EXPLICIT_SEMANTIC_PRIORITY_THEN_STABLE_OWNER'||summaryContract.unnamedPolicy!=='LOWEST_PRIORITY_STABLE_OWNER'||summaryContract.preflightScope!=='EVERY_SELECTED_CANONICAL_GLB_BEFORE_ITEM_ONE'||summaryContract.passed!==true||summaryContract.allSelectedModelsPassed!==true||summaryContract.fullCatalogCountsMatched!==true)return false;
  const byKey=new Map(reports.map(report=>[report.key,report])),allNames=[],resolutionByName=new Map(),coverage=[];
  for(const item of catalogContract.included){const report=byKey.get(item.key);if(!report||!materialSemanticItemGreen(report))return false;const names=report.materialSemanticContract.originalMaterialNames;allNames.push(...names);coverage.push({key:report.key,sourceSha256:report.sourceSha256,materialNames:names});for(const resolution of report.materialSemanticContract.resolutions){const previous=resolutionByName.get(resolution.name);if(previous&&stable(previous)!==stable(resolution))return false;resolutionByName.set(resolution.name,resolution);}}
  const resolutions=[...resolutionByName.values()].sort((a,b)=>a.name<b.name?-1:a.name>b.name?1:0),unique=[...new Set(allNames)];
  return summaryContract.selectedModelCount===reports.length&&summaryContract.selectedModelCount===FULL_MATERIAL.models&&summaryContract.selectedMaterialSlotCount===allNames.length&&summaryContract.selectedMaterialSlotCount===FULL_MATERIAL.slots&&summaryContract.uniqueIncludingUnnamed===unique.length&&summaryContract.uniqueIncludingUnnamed===FULL_MATERIAL.uniqueWithUnnamed&&summaryContract.uniqueNonemptyNames===unique.filter(Boolean).length&&summaryContract.uniqueNonemptyNames===FULL_MATERIAL.uniqueNonempty&&summaryContract.unnamedSlots===allNames.filter(name=>!name).length&&summaryContract.unnamedSlots===FULL_MATERIAL.unnamedSlots&&summaryContract.fullCatalogExpectedUniqueNonemptyNames===summaryContract.uniqueNonemptyNames&&summaryContract.fullCatalogExpectedUniqueIncludingUnnamed===summaryContract.uniqueIncludingUnnamed&&summaryContract.fullCatalogExpectedUnnamedSlots===summaryContract.unnamedSlots&&stable(summaryContract.resolutions)===stable(resolutions)&&summaryContract.materialCoverageSha256===canonicalHash(coverage)&&summaryContract.materialCoverageSha256===FULL_MATERIAL.coverageSha256;
}

function parseGlb(file,code){
  if(!check(fs.existsSync(file)&&fs.statSync(file).isFile(),code+'.exists',posix(file)))return null;
  const buffer=fs.readFileSync(file);if(!check(buffer.length>=20,code+'.minimum-size',buffer.length))return null;
  check(buffer.toString('ascii',0,4)==='glTF',code+'.magic');check(buffer.readUInt32LE(4)===2,code+'.version',buffer.readUInt32LE(4));check(buffer.readUInt32LE(8)===buffer.length,code+'.declared-size');
  let offset=12,document=null,binary=Buffer.alloc(0);
  try{while(offset+8<=buffer.length){const length=buffer.readUInt32LE(offset),kind=buffer.readUInt32LE(offset+4),start=offset+8,end=start+length;if(end>buffer.length)throw new Error('truncated GLB chunk');if(kind===0x4e4f534a)document=JSON.parse(buffer.subarray(start,end).toString('utf8').replace(/[\0\s]+$/u,''));else if(kind===0x004e4942)binary=buffer.subarray(start,end);offset=end;}if(!document)throw new Error('missing JSON chunk');check(offset===buffer.length,code+'.chunk-boundary');return{document,binary,buffer};}catch(error){check(false,code+'.parse',error.message);return null;}
}
function parseGlbSemantic(file){
  try{const buffer=fs.readFileSync(file);if(buffer.length<20||buffer.toString('ascii',0,4)!=='glTF'||buffer.readUInt32LE(4)!==2||buffer.readUInt32LE(8)!==buffer.length)return null;let offset=12,document=null,binary=Buffer.alloc(0);while(offset+8<=buffer.length){const length=buffer.readUInt32LE(offset),kind=buffer.readUInt32LE(offset+4),start=offset+8,end=start+length;if(end>buffer.length)return null;if(kind===0x4e4f534a)document=JSON.parse(buffer.subarray(start,end).toString('utf8').replace(/[\0\s]+$/u,''));else if(kind===0x004e4942)binary=buffer.subarray(start,end);offset=end;}return offset===buffer.length&&document?{document,binary,buffer}:null;}catch{return null;}
}
function pngDimensionsBuffer(buffer){if(buffer.length<24||!buffer.subarray(0,8).equals(Buffer.from([137,80,78,71,13,10,26,10])))return null;return[buffer.readUInt32BE(16),buffer.readUInt32BE(20)];}
function pngDimensions(file){return fs.existsSync(file)&&fs.statSync(file).isFile()?pngDimensionsBuffer(fs.readFileSync(file)):null;}
function glbImages(parsed,glbFile,boundary,code){
  if(!parsed)return[];const{document,binary}=parsed,images=[];
  for(const[index,image]of(document.images||[]).entries()){
    let bytes=null,storage=null;
    if(Number.isInteger(image.bufferView)){const view=(document.bufferViews||[])[image.bufferView];check(!!view&&(view.buffer??0)===0,code+'.image-buffer-view',index);if(view){const start=view.byteOffset||0;bytes=binary.subarray(start,start+view.byteLength);storage='BUFFER_VIEW';}}
    else if(typeof image.uri==='string'&&image.uri.startsWith('data:')){const comma=image.uri.indexOf(',');try{bytes=Buffer.from(image.uri.slice(comma+1),image.uri.slice(0,comma).includes(';base64')?'base64':'utf8');}catch{bytes=null;}storage='DATA_URI';}
    else if(typeof image.uri==='string'){const external=path.resolve(path.dirname(glbFile),decodeURIComponent(image.uri));check(isWithin(external,boundary),code+'.image-uri-boundary',image.uri);if(fs.existsSync(external)&&fs.statSync(external).isFile())bytes=fs.readFileSync(external);storage='EXTERNAL_URI';}
    check(Buffer.isBuffer(bytes)&&bytes.length>0,code+'.image-bytes',index);const dimensions=bytes?pngDimensionsBuffer(bytes):null;check(!!dimensions,code+'.image-png',index);
    images.push({index,name:image.name??null,mimeType:image.mimeType??null,storage,sha256:bytes?sha256Buffer(bytes):null,bytes:bytes?.length||0,dimensions,width:dimensions?.[0]??null,height:dimensions?.[1]??null,bitDepth:bytes&&dimensions?bytes[24]:null,channels:bytes&&dimensions?(bytes[25]===6?'RGBA':bytes[25]===2?'RGB':'OTHER'):null});
  }return images;
}
function nodeMeshLabels(document){const labels=new Map();for(const node of document.nodes||[]){if(!Number.isInteger(node.mesh))continue;const list=labels.get(node.mesh)||[];list.push({text:[node.name,document.meshes?.[node.mesh]?.name].filter(Boolean).join(' '),extras:node.extras||{}});labels.set(node.mesh,list);}return labels;}
function nonRenderRecord(record){if(record.extras?.mf_stage10_render===true)return false;if(record.extras?.mf_stage10_render===false)return true;const role=String(record.extras?.mf_role||'').toUpperCase(),tokens=new Set((record.text||'').toUpperCase().split(/[^A-Z0-9]+/).filter(Boolean));return['COLLISION','NAV','NAVMESH','NAVIGATION','PROOF','EVIDENCE'].some(token=>tokens.has(token))||['COLLISION','SIMPLIFIED_COLLISION','NAVIGATION_PROXY','WALKABLE_DECK','PROOF_ONLY','EVIDENCE_ONLY'].includes(role)||(tokens.has('FLOOR')&&['STUDIO','SHADOW','REVIEW'].some(token=>tokens.has(token)));}
function renderPrimitives(document,requireProof=false){const labels=nodeMeshLabels(document),records=[];for(const[meshIndex,mesh]of(document.meshes||[]).entries()){const nodeRecords=labels.get(meshIndex)||[{text:mesh.name||'',extras:{}}],proof=nodeRecords.some(record=>record.extras?.mf_stage10_render===true),excluded=nodeRecords.every(nonRenderRecord);if((requireProof&&!proof)||(!requireProof&&excluded))continue;for(const[primitiveIndex,primitive]of(mesh.primitives||[]).entries())records.push({meshIndex,primitiveIndex,mesh,primitive});}return records;}
function verifyUvGlb(file,code,requireProof=false){const parsed=parseGlb(file,code);if(!parsed)return null;const primitives=renderPrimitives(parsed.document,requireProof);check(primitives.length>0,code+'.render-primitives');for(const record of primitives){const attributes=record.primitive.attributes||{};check(Number.isInteger(attributes.POSITION),code+'.position',record.mesh.name);check(Number.isInteger(attributes.NORMAL),code+'.normal',record.mesh.name);check(Number.isInteger(attributes.TEXCOORD_0),code+'.texcoord0',record.mesh.name);check(Number.isInteger(attributes.TEXCOORD_1),code+'.texcoord1',record.mesh.name);}return parsed;}
function loadReports(directory,schema,code,omitSummary=false,exclude=()=>false){check(fs.existsSync(directory)&&fs.statSync(directory).isDirectory(),code+'.directory',posix(directory));const files=walkFiles(directory,file=>file.toLowerCase().endsWith('.json')&&(!omitSummary||path.basename(file)!=='summary.json')&&!exclude(file)),reports=[],paths=new Map();for(const file of files){const report=readJson(file,code+'.item');if(!report)continue;check(report.schema===schema,code+'.schema',{file:posix(file),schema:report.schema});check(typeof report.key==='string'&&report.key.length>0,code+'.key',posix(file));if(paths.has(report.key))check(false,code+'.duplicate-key',report.key);else paths.set(report.key,file);reports.push(report);}return{reports,paths,files};}

function proofResult(){return{ok:true,reasons:[]};}
function rejectProof(result,code,details=undefined){result.ok=false;result.reasons.push(details===undefined?code:{code,details});}
function requireProof(result,condition,code,details=undefined){if(!condition)rejectProof(result,code,details);}
function isNonnegativeInteger(value){return Number.isInteger(value)&&value>=0;}
function isSortedUniqueStrings(values,allowEmpty=false){return Array.isArray(values)&&(allowEmpty||values.length>0)&&values.every(value=>typeof value==='string'&&value.length>0)&&stable(values)===stable(sorted(new Set(values)));}
function isSha(value){return typeof value==='string'&&SHA_PATTERN.test(value);}
function proofHashHasPythonFormat(record,field='canonicalProofSha256'){return record&&isSha(record[field]);}
function exactJsProofHash(record,field='canonicalProofSha256'){if(!record||!isSha(record[field]))return false;const payload={...record};delete payload[field];return canonicalHash(payload)===record[field];}
function metricDistributionGreen(distribution){return distribution&&['min','p50','p95','p99','max'].every(field=>finiteNumber(distribution[field]))&&distribution.min<=distribution.p50&&distribution.p50<=distribution.p95&&distribution.p95<=distribution.p99&&distribution.p99<=distribution.max;}

// Python and JavaScript spell finite exponents differently in canonical JSON
// (`e-08` versus `e-8`). Float-bearing Python hashes are therefore treated as
// producer bindings and their payloads are independently checked semantically;
// only integer/string accessor proofs are safely rehashed here.
function tiledUvConstructionContractResult(contract){
  const result=proofResult(),fixtures=contract?.fixtures||{},micro=fixtures.ruinedBunkerMicroTriangle3067||{},collapse=fixtures.ruinedBunkerExportCollapseTriangle2532||{},affine=fixtures.rotatedHierarchyAffineCancellation||{};
  requireProof(result,contract?.schema==='MassfrontStage10TiledUvConstructionContractV1','CONTRACT_SCHEMA');
  requireProof(result,contract?.method===TILED_UV_ANCHOR_METHOD&&contract?.metricMethod===TILED_UV_METRIC_METHOD,'CONTRACT_METHODS');
  requireProof(result,contract?.passed===true&&contract?.tileMetres===4,'CONTRACT_NOT_GREEN');
  requireProof(result,contract?.worldDoubleAreaFloor===TILED_UV_WORLD_DOUBLE_AREA_FLOOR,'WORLD_AREA_FLOOR_CHANGED',contract?.worldDoubleAreaFloor);
  requireProof(result,contract?.sourceSliverQualityLimit===SOURCE_SLIVER_QUALITY_LIMIT&&contract?.texelScaleErrorLimit===TEXEL_SCALE_ERROR_LIMIT&&nearNumber(contract?.cubicStretchLimit,CUBIC_STRETCH_LIMIT,1e-15,1e-15),'CONTRACT_LIMITS');
  requireProof(result,micro.passed===true&&finiteNumber(micro.worldDoubleArea)&&finiteNumber(micro.projectedDoubleArea)&&finiteNumber(micro.expectedAreaScale)&&finiteNumber(micro.projectedDoubleAreaFloor)&&micro.worldDoubleArea>TILED_UV_WORLD_DOUBLE_AREA_FLOOR&&micro.projectedDoubleArea>micro.projectedDoubleAreaFloor&&nearNumber(micro.projectedDoubleAreaFloor,TILED_UV_WORLD_DOUBLE_AREA_FLOOR*Math.abs(micro.expectedAreaScale),1e-24,1e-12)&&micro.oldUnscaledWorldFloorWouldReject===true,'DIMENSIONAL_FLOOR_FIXTURE');
  requireProof(result,collapse.passed===true&&collapse.integralAnchor===-8&&collapse.unanchoredValuesDistinctBeforeExport===true&&collapse.unanchoredValuesCollapseAfterExportFlip===true&&collapse.anchoredValuesDistinctAfterExportFlip===true&&collapse.anchoredValuesDistinctAfterRoundtrip===true,'FLOAT32_COLLAPSE_FIXTURE');
  for(const field of['exactCollapsedUvRejected','nonIntegralAnchorRejected','loopInconsistentPostExportArtifactRejected','affineOutOfBoundResidualRejected','scaleDistortionAboveFrozenLimitRejected'])requireProof(result,fixtures[field]===true,'NEGATIVE_FIXTURE_'+field);
  requireProof(result,affine.passed===true&&finiteNumber(affine.observedResidual)&&finiteNumber(affine.oldFinalResultUlpBound)&&finiteNumber(affine.affineProjectedGeometryBound)&&finiteNumber(affine.derivedTotalBound)&&affine.observedResidual>affine.oldFinalResultUlpBound&&affine.observedResidual<=affine.derivedTotalBound&&affine.affineProjectedGeometryBound>0&&affine.derivedTotalBound>=affine.affineProjectedGeometryBound&&affine.oldFinalResultUlpWouldReject===true&&affine.affineForwardErrorAccepts===true&&affine.operationCountPerAffineDot===7&&affine.boundFormula===TILED_UV_POST_BOUND_FORMULA,'AFFINE_FORWARD_ERROR_FIXTURE');
  requireProof(result,proofHashHasPythonFormat(contract,'canonicalContractSha256'),'CONTRACT_HASH_FORMAT');
  return result;
}

function tiledUvAnchorProofResult(proof,postExport){
  const result=proofResult(),zeroFields=['nonIntegralAnchorViolations','storageRealizationViolations','postExportInferredAnchorAmbiguityViolations','postExportInferredAnchorBoundViolations','postExportInferredAnchorLoopConsistencyViolations','moduloPhaseViolations','derivativeBoundViolations'];
  requireProof(result,proof?.schema==='MassfrontStage10TiledUvConstructionProofV1'&&proof?.method===TILED_UV_ANCHOR_METHOD,'ANCHOR_SCHEMA_METHOD');
  requireProof(result,proof?.present===true&&proof?.passed===true&&proof?.tileMetres===4&&proof?.anchorUnits==='INTEGER_UV_REPEAT_TILES','ANCHOR_NOT_GREEN');
  requireProof(result,isNonnegativeInteger(proof?.polygonCount)&&proof.polygonCount>0&&isNonnegativeInteger(proof?.skippedDegeneratePolygons)&&isNonnegativeInteger(proof?.nonzeroAnchorPolygons)&&proof.nonzeroAnchorPolygons<=proof.polygonCount&&isNonnegativeInteger(proof?.loopCount)&&proof.loopCount>=3*proof.polygonCount&&isNonnegativeInteger(proof?.edgeComponentCount)&&proof.edgeComponentCount>0,'ANCHOR_COVERAGE');
  requireProof(result,zeroFields.every(field=>proof?.[field]===0)&&Array.isArray(proof?.postExportInferredAnchorViolationSamples)&&proof.postExportInferredAnchorViolationSamples.length===0,'ANCHOR_VIOLATIONS');
  requireProof(result,proof?.allOffsetsIntegral===true&&proof?.integralTranslationPreservesModuloOneByConstruction===true&&proof?.storedCoordinatesMatchFloat32Realization===true&&proof?.moduloOnePhasePreserved===true&&proof?.derivativesWithinStoredFloatBounds===true,'ANCHOR_INVARIANTS');
  requireProof(result,[proof?.maxModuloPhaseError,proof?.maxModuloPhaseErrorBound,proof?.maxDerivativeComponentError,proof?.maxDerivativeComponentErrorBound].every(value=>finiteNumber(value)&&value>=0)&&proof.maxModuloPhaseError<=proof.maxModuloPhaseErrorBound&&proof.maxDerivativeComponentError<=proof.maxDerivativeComponentErrorBound,'ANCHOR_ERROR_BOUNDS');
  if(postExport){
    requireProof(result,proof?.storageRealization==='BLENDER_GLTF_FLOAT32_V_FLIP_ROUNDTRIP'&&proof?.postExportAnchorInferenceMethod===TILED_UV_POST_INFERENCE_METHOD&&proof?.postExportInferenceBoundMethod===TILED_UV_POST_BOUND_METHOD&&proof?.postExportInferenceBoundFormula===TILED_UV_POST_BOUND_FORMULA,'POST_ANCHOR_METHOD');
    requireProof(result,proof?.postExportAffineDotOperationCount===7&&proof?.postExportAffineUnitRoundoff===2**-24&&Number.isInteger(proof?.postExportMaxAffineHierarchyDepth)&&proof.postExportMaxAffineHierarchyDepth>=1&&proof?.postExportInferredAnchorPolygons===proof?.polygonCount,'POST_ANCHOR_FORWARD_ERROR');
  }else{
    requireProof(result,proof?.storageRealization==='BLENDER_FLOAT32_UV_LAYER'&&proof?.postExportAnchorInferenceMethod===TILED_UV_PRE_INFERENCE_METHOD&&proof?.postExportInferenceBoundMethod===TILED_UV_PRE_BOUND_METHOD&&proof?.postExportInferenceBoundFormula===TILED_UV_PRE_BOUND_FORMULA,'PRE_ANCHOR_METHOD');
    requireProof(result,proof?.postExportAffineDotOperationCount===0&&proof?.postExportAffineUnitRoundoff===0&&proof?.postExportMaxAffineHierarchyDepth===0&&proof?.postExportInferredAnchorPolygons===0,'PRE_ANCHOR_FORWARD_ERROR');
  }
  requireProof(result,proofHashHasPythonFormat(proof),'ANCHOR_HASH_FORMAT');
  return result;
}

function tiledUvMetricResult(metrics){
  const result=proofResult(),integerFields=['projectedFloorEvaluatedTriangles','trianglesMeasured','degenerateWorldTriangles','zeroUvAreaTriangles','exactZeroUvAreaTriangles','projectedFloorFailureTriangles','nonFiniteTriangles'];
  requireProof(result,metrics?.present===true&&metrics?.method===TILED_UV_METRIC_METHOD&&metrics?.valid===true,'METRIC_SCHEMA_METHOD');
  requireProof(result,metrics?.tileMetres===4&&metrics?.targetUvUnitsPerMetre===0.25&&metrics?.worldDoubleAreaFloor===TILED_UV_WORLD_DOUBLE_AREA_FLOOR&&metrics?.worldDoubleAreaFloorUnchanged===true,'METRIC_WORLD_FLOOR');
  requireProof(result,metrics?.projectedDoubleAreaFloorMethod===TILED_UV_PROJECTED_FLOOR_METHOD&&metrics?.projectedFloorIsDimensionalConversionNotToleranceRelaxation===true,'METRIC_PROJECTED_FLOOR_METHOD');
  requireProof(result,integerFields.every(field=>isNonnegativeInteger(metrics?.[field]))&&metrics.trianglesMeasured>0&&metrics.projectedFloorEvaluatedTriangles>=metrics.trianglesMeasured,'METRIC_COVERAGE');
  requireProof(result,finiteNumber(metrics?.minimumProjectedDoubleAreaFloor)&&finiteNumber(metrics?.maximumProjectedDoubleAreaFloor)&&metrics.minimumProjectedDoubleAreaFloor>0&&metrics.minimumProjectedDoubleAreaFloor<=metrics.maximumProjectedDoubleAreaFloor&&metrics.maximumProjectedDoubleAreaFloor<=TILED_UV_WORLD_DOUBLE_AREA_FLOOR,'METRIC_PROJECTED_FLOOR_RANGE');
  requireProof(result,metrics?.zeroUvAreaTriangles===0&&metrics?.exactZeroUvAreaTriangles===0&&metrics?.projectedFloorFailureTriangles===0&&metrics?.nonFiniteTriangles===0,'METRIC_ZERO_FAILURES');
  requireProof(result,metrics?.finite===true&&metrics?.nonZero===true&&metrics?.scaleConsistent===true&&metrics?.withinCubicStretchBound===true,'METRIC_INVARIANTS');
  requireProof(result,metrics?.sourceSliverQualityLimit===SOURCE_SLIVER_QUALITY_LIMIT&&metrics?.texelScaleErrorLimit===TEXEL_SCALE_ERROR_LIMIT&&nearNumber(metrics?.cubicStretchLimit,Number(CUBIC_STRETCH_LIMIT.toFixed(7)),1e-15),'METRIC_LIMITS');
  for(const field of['stretchRatio','areaScale','areaScaleRelativeError','maxSingularValueError'])requireProof(result,metricDistributionGreen(metrics?.[field]),'METRIC_DISTRIBUTION_'+field);
  requireProof(result,finiteNumber(metrics?.p95StretchRatio)&&finiteNumber(metrics?.maxStretchRatio)&&finiteNumber(metrics?.p95AreaScaleRelativeError)&&finiteNumber(metrics?.maxAreaScaleRelativeError)&&nearNumber(metrics.p95StretchRatio,metrics.stretchRatio?.p95,1e-7)&&nearNumber(metrics.maxStretchRatio,metrics.stretchRatio?.max,1e-7)&&nearNumber(metrics.p95AreaScaleRelativeError,metrics.areaScaleRelativeError?.p95,1e-7)&&nearNumber(metrics.maxAreaScaleRelativeError,metrics.areaScaleRelativeError?.max,1e-7)&&metrics.maxStretchRatio<=CUBIC_STRETCH_LIMIT&&metrics.maxAreaScaleRelativeError<=TEXEL_SCALE_ERROR_LIMIT&&metrics.maxSingularValueError?.max<=TEXEL_SCALE_ERROR_LIMIT,'METRIC_BOUNDS');
  return result;
}

function exportedAccessorProofResult(proof){
  const result=proofResult();
  requireProof(result,proof?.schema==='MassfrontStage10ExportedTexcoordChannelProofV1'&&proof?.method==='GLB_NODE_EXTRAS_PLUS_PRIMITIVE_ATTRIBUTE_INDEX_BINDING'&&proof?.passed===true,'ACCESSOR_SCHEMA_METHOD');
  requireProof(result,stable(proof?.authoredLayerOrder)===stable(['UV_GEN','UVMap_Tile'])&&stable(proof?.gltfChannelOrder)===stable(['TEXCOORD_0','TEXCOORD_1'])&&proof?.freshBlenderImportLayerIndexForTexcoord1===1&&proof?.requiredAccessorComponentType===5126&&proof?.requiredTexcoordAccessorType==='VEC2'&&proof?.texcoordCountsMustMatchPosition===true&&proof?.accessorsMustBeDistinct===true,'ACCESSOR_CONTRACT');
  requireProof(result,isSortedUniqueStrings(proof?.expectedRenderMeshes)&&stable(proof.expectedRenderMeshes)===stable(proof?.actualRenderMeshes)&&proof?.exactRenderMeshMembership===true,'ACCESSOR_MEMBERSHIP');
  requireProof(result,Array.isArray(proof?.records)&&proof.records.length===proof.expectedRenderMeshes.length&&stable(proof.records.map(record=>record.name))===stable(proof.expectedRenderMeshes),'ACCESSOR_RECORDS');
  for(const record of proof?.records||[]){
    requireProof(result,record?.primarySemantic==='UV_GEN'&&record?.secondarySemantic==='UVMap_Tile'&&Number.isInteger(record?.meshIndex)&&record.meshIndex>=0&&Number.isInteger(record?.primitiveCount)&&record.primitiveCount>0&&Array.isArray(record?.primitives)&&record.primitives.length===record.primitiveCount,'ACCESSOR_MESH_RECORD',record?.name);
    for(const[primitiveIndex,primitive]of(record?.primitives||[]).entries()){
      const accessors=[primitive?.positionAccessor,primitive?.texcoord0Accessor,primitive?.texcoord1Accessor];
      requireProof(result,primitive?.primitiveIndex===primitiveIndex&&stable(primitive?.texcoordChannels)===stable(['TEXCOORD_0','TEXCOORD_1'])&&primitive?.positionTexcoordAccessorsDistinct===true&&primitive?.positionTexcoordCountsMatch===true&&primitive?.uncompressedPrimitive===true&&primitive?.exactTexcoord01Only===true,'ACCESSOR_PRIMITIVE',record?.name+'/'+primitiveIndex);
      requireProof(result,new Set(accessors.map(accessor=>accessor?.index)).size===3&&accessors.every(accessor=>Number.isInteger(accessor?.index)&&accessor.index>=0&&accessor.indexInRange===true&&Number.isInteger(accessor?.bufferViewIndex)&&accessor.bufferViewIndex>=0&&accessor.bufferViewIndexInRange===true&&accessor.componentType===5126&&Number.isInteger(accessor.count)&&accessor.count>0&&accessor.float32===true&&accessor.storageWithinBufferView===true&&accessor.bufferViewWithinBinaryChunk===true&&accessor.uncompressedDenseAccessor===true&&accessor.passed===true)&&accessors[0].type==='VEC3'&&accessors[0].expectedType==='VEC3'&&accessors[1].type==='VEC2'&&accessors[1].expectedType==='VEC2'&&accessors[2].type==='VEC2'&&accessors[2].expectedType==='VEC2'&&accessors[0].count===accessors[1].count&&accessors[1].count===accessors[2].count,'ACCESSOR_STORAGE',record?.name+'/'+primitiveIndex);
    }
  }
  requireProof(result,exactJsProofHash(proof),'ACCESSOR_CANONICAL_HASH');
  return result;
}

function rawAccessorProofMatches(parsed,proof){
  if(!parsed||!proof)return false;const document=parsed.document,accessors=document.accessors||[],views=document.bufferViews||[],records=[];
  const evidence=(index,expectedType)=>{const accessor=Number.isInteger(index)?accessors[index]:null,view=accessor&&Number.isInteger(accessor.bufferView)?views[accessor.bufferView]:null,componentCount={VEC2:2,VEC3:3}[accessor?.type]||0,elementSize=accessor?.componentType===5126?4*componentCount:0,stride=view?.byteStride??elementSize,accessorOffset=accessor?.byteOffset??0,viewOffset=view?.byteOffset??0,lastEnd=accessor&&view&&Number.isInteger(accessor.count)&&accessor.count>0?accessorOffset+(accessor.count-1)*stride+elementSize:Infinity;return{index,indexInRange:!!accessor,bufferViewIndex:accessor?.bufferView,bufferViewIndexInRange:!!view,componentType:accessor?.componentType,type:accessor?.type,count:accessor?.count,float32:accessor?.componentType===5126,expectedType,storageWithinBufferView:!!view&&lastEnd<=view.byteLength,bufferViewWithinBinaryChunk:!!view&&(view.buffer??0)===0&&viewOffset+view.byteLength<=parsed.binary.length&&(document.buffers||[]).length===1&&(document.buffers?.[0]?.byteLength??Infinity)<=parsed.binary.length,uncompressedDenseAccessor:!!accessor&&!accessor.sparse&&!view?.extensions?.EXT_meshopt_compression,passed:false};};
  for(const node of document.nodes||[]){if(!proof.expectedRenderMeshes.includes(String(node.name||'')))continue;const mesh=Number.isInteger(node.mesh)?document.meshes?.[node.mesh]:null,primitives=[];for(const[primitiveIndex,primitive]of(mesh?.primitives||[]).entries()){const position=evidence(primitive.attributes?.POSITION,'VEC3'),texcoord0=evidence(primitive.attributes?.TEXCOORD_0,'VEC2'),texcoord1=evidence(primitive.attributes?.TEXCOORD_1,'VEC2'),channels=sorted(Object.keys(primitive.attributes||{}).filter(key=>key.startsWith('TEXCOORD_'))),distinct=new Set([position.index,texcoord0.index,texcoord1.index]).size===3,counts=position.count===texcoord0.count&&texcoord0.count===texcoord1.count,uncompressed=!primitive.extensions?.KHR_draco_mesh_compression&&!primitive.extensions?.EXT_meshopt_compression;for(const item of[position,texcoord0,texcoord1])item.passed=item.indexInRange&&item.bufferViewIndexInRange&&item.componentType===5126&&item.type===item.expectedType&&Number.isInteger(item.count)&&item.count>0&&item.storageWithinBufferView&&item.bufferViewWithinBinaryChunk&&item.uncompressedDenseAccessor;primitives.push({primitiveIndex,texcoordChannels:channels,positionAccessor:position,texcoord0Accessor:texcoord0,texcoord1Accessor:texcoord1,positionTexcoordAccessorsDistinct:distinct,positionTexcoordCountsMatch:counts,uncompressedPrimitive:uncompressed,exactTexcoord01Only:stable(channels)===stable(['TEXCOORD_0','TEXCOORD_1'])&&position.passed&&texcoord0.passed&&texcoord1.passed&&distinct&&counts&&uncompressed});}records.push({name:String(node.name||''),meshIndex:node.mesh,primarySemantic:node.extras?.mf_uv_primary,secondarySemantic:node.extras?.mf_uv_secondary,primitiveCount:primitives.length,primitives});}
  records.sort((a,b)=>a.name.localeCompare(b.name));return stable(records)===stable(proof.records);
}

function tiledUvAggregateProofResult(proof,{postExport,artifact,artifactSha256,expectedMeshes,preMeshRecords,parsed}){
  const result=proofResult(),perMesh=proof?.perMesh||[],names=perMesh.map(item=>item?.name);
  requireProof(result,proof?.schema==='MassfrontStage10TiledUvAggregateProofV1'&&proof?.method===TILED_UV_AGGREGATE_METHOD&&proof?.metricMethod===TILED_UV_METRIC_METHOD&&proof?.anchorMethod===TILED_UV_ANCHOR_METHOD,'AGGREGATE_SCHEMA_METHOD');
  requireProof(result,isSortedUniqueStrings(proof?.expectedRenderMeshes)&&stable(proof.expectedRenderMeshes)===stable(expectedMeshes)&&stable(proof?.actualRenderMeshes)===stable(expectedMeshes)&&proof?.exactRenderMeshMembership===true&&proof?.renderMeshCount===expectedMeshes.length&&stable(names)===stable(expectedMeshes),'AGGREGATE_MEMBERSHIP');
  requireProof(result,proof?.scope===(postExport?'POST_EXPORT_FRESH_BLENDER_REIMPORT':'PRE_EXPORT_BLENDER_SCENE')&&proof?.sceneConstruction===(postExport?'FACTORY_EMPTY_SCENE_IMPORT_OF_EXACT_REPAIR_GLB':'IN_MEMORY_DERIVED_SCENE_BEFORE_GLTF_EXPORT'),'AGGREGATE_SCOPE');
  if(postExport)requireProof(result,proof?.artifact===artifact&&proof?.artifactSha256===artifactSha256&&proof?.artifactSha256AfterProof===artifactSha256&&proof?.artifactSha256Stable===true&&proof?.artifactSha256Bound===true,'AGGREGATE_ARTIFACT_BINDING');
  else requireProof(result,proof?.artifact===null&&proof?.artifactSha256===null&&proof?.artifactSha256Bound===true&&proof?.artifactSha256AfterProof===undefined&&proof?.artifactSha256Stable===undefined&&proof?.exportedTexcoordChannelProof===undefined,'PRE_AGGREGATE_ARTIFACT_EMPTY');
  let zero=0,exactZero=0,nonfinite=0;
  for(const mesh of perMesh){
    requireProof(result,stable(mesh?.uvLayers)===stable(['UV_GEN','UVMap_Tile'])&&mesh?.tiledUvLayerIndex===1&&mesh?.tiledUvLayerSemantic==='TEXCOORD_1','AGGREGATE_LAYER',mesh?.name);
    const anchor=tiledUvAnchorProofResult(mesh?.tiledUvAnchorProof,postExport),metric=tiledUvMetricResult(mesh?.tiledUvMetrics);if(!anchor.ok)for(const reason of anchor.reasons)rejectProof(result,'ANCHOR',reason);if(!metric.ok)for(const reason of metric.reasons)rejectProof(result,'METRIC',reason);zero+=mesh?.tiledUvMetrics?.zeroUvAreaTriangles||0;exactZero+=mesh?.tiledUvMetrics?.exactZeroUvAreaTriangles||0;nonfinite+=mesh?.tiledUvMetrics?.nonFiniteTriangles||0;
  }
  if(!postExport&&Array.isArray(preMeshRecords)){const expected=preMeshRecords.map(mesh=>({name:mesh.name,uvLayers:mesh.uvLayers,tiledUvLayerIndex:mesh.tiledUvLayerIndex,tiledUvLayerSemantic:mesh.tiledUvLayerSemantic,tiledUvAnchorProof:mesh.tiledUvAnchorProof,tiledUvMetrics:mesh.tiledUvMetrics})).sort((a,b)=>a.name.localeCompare(b.name));requireProof(result,stable(perMesh)===stable(expected),'PRE_AGGREGATE_PER_MESH_BINDING');}
  requireProof(result,proof?.zeroUvAreaTriangles===zero&&proof?.exactZeroUvAreaTriangles===exactZero&&proof?.nonFiniteTriangles===nonfinite&&zero===0&&exactZero===0&&nonfinite===0,'AGGREGATE_FAILURE_TOTALS');
  requireProof(result,['allAnchorsValid','allTiledUvLayersAreTexcoord1','allFinite','allNonZero','allScaleConsistent','allWithinCubicStretchBound','allMetricValid','allValid'].every(field=>proof?.[field]===true),'AGGREGATE_NOT_GREEN');
  requireProof(result,proofHashHasPythonFormat(proof),'AGGREGATE_HASH_FORMAT');
  if(postExport){const accessor=exportedAccessorProofResult(proof?.exportedTexcoordChannelProof);if(!accessor.ok)for(const reason of accessor.reasons)rejectProof(result,'ACCESSOR',reason);requireProof(result,rawAccessorProofMatches(parsed,proof?.exportedTexcoordChannelProof),'ACCESSOR_RAW_GLB_MISMATCH');}
  return result;
}

function mergeProofReasons(target,prefix,result){for(const reason of result.reasons)target.push(typeof reason==='string'?prefix+':'+reason:{code:prefix+':'+reason.code,details:reason.details});}
function repairProofDisposition(report,outputParsed,textureParsed){
  const reasons=[],contract=tiledUvConstructionContractResult(report?.tiledUvConstructionContract),meshes=(report?.meshes||[]),expected=sorted(meshes.map(mesh=>mesh.name)),pre=tiledUvAggregateProofResult(report?.preExportTiledUvProof,{postExport:false,expectedMeshes:expected,preMeshRecords:meshes}),post=tiledUvAggregateProofResult(report?.postExportTiledUvProof,{postExport:true,artifact:report?.output,artifactSha256:report?.outputSha256,expectedMeshes:expected,parsed:outputParsed}),textureProof=report?.postExportTextureSourceTiledUvProof,textureExpected=textureProof?.expectedRenderMeshes,texture=tiledUvAggregateProofResult(textureProof,{postExport:true,artifact:report?.textureSource,artifactSha256:report?.textureSourceSha256,expectedMeshes:Array.isArray(textureExpected)?textureExpected:[],parsed:textureParsed});
  if(!materialSemanticItemGreen(report))reasons.push('MATERIAL_SEMANTIC:NOT_GREEN');if(!rasterCleanupProgressGreen(report?.rasterCleanup)||!lineageProofGreen(report?.rasterCleanup))reasons.push('RASTER_CLEANUP:NOT_GREEN');mergeProofReasons(reasons,'TILED_UV_CONSTRUCTION',contract);mergeProofReasons(reasons,'PRE_EXPORT_TILED_UV',pre);mergeProofReasons(reasons,'POST_EXPORT_OUTPUT_TILED_UV',post);mergeProofReasons(reasons,'POST_EXPORT_TEXTURE_SOURCE_TILED_UV',texture);
  if(report?.status==='RECONSTRUCTION_REQUIRED')return{accepted:false,reasons:Array.isArray(report.statusReasons)&&report.statusReasons.length?report.statusReasons:['RECONSTRUCTION_REQUIRED_WITHOUT_REASON'],reasonCode:'RECONSTRUCTION_REQUIRED'};
  if(!REPAIR_ELIGIBLE_STATUSES.includes(report?.status))reasons.push('STATUS:NOT_ELIGIBLE');return{accepted:REPAIR_ELIGIBLE_STATUSES.includes(report?.status)&&reasons.length===0,reasons,reasonCode:reasons.length?'REQUIRED_REPAIR_PROOF_NOT_GREEN':null};
}
function codePointCompare(left,right){return left<right?-1:left>right?1:0;}
function manifestRowCompare(left,right){return codePointCompare(String(left?.key||''),String(right?.key||''))||codePointCompare(String(left?.stage||''),String(right?.stage||''));}
function manifestHashGreen(summary,field){const rows=summary?.[field],hash=summary?.[field+'Sha256'];return Array.isArray(rows)&&isSha(hash)&&canonicalHash(rows)===hash&&stable(rows)===stable([...rows].sort(manifestRowCompare));}
function manifestCommonRowGreen(row){return row&&typeof row.key==='string'&&row.key.length>0&&typeof row.stage==='string'&&['REPAIR','PBR','AUDIT','RENDER'].includes(row.stage)&&typeof row.status==='string'&&row.status.length>0&&typeof row.disposition==='string'&&Array.isArray(row.reasons)&&typeof row.report==='string'&&isSha(row.reportSha256)&&typeof row.output==='string'&&isSha(row.outputSha256);}
function verifyStageManifestContract(summary,stage,inputRows,inheritedQuarantine,productionKeys){
  equal(summary.productionSetPolicy,PRODUCTION_SET_POLICY,stage.toLowerCase()+'.summary.production-policy');
  for(const field of['inputProductionManifest','productionManifest','quarantineManifest'])check(manifestHashGreen(summary,field),stage.toLowerCase()+'.summary.'+field+'-hash');
  equal(summary.inputProductionManifest,inputRows,stage.toLowerCase()+'.summary.input-production-manifest');
  equal(summary.items,summary.productionManifest,stage.toLowerCase()+'.summary.items-production-manifest');
  const production=summary.productionManifest||[],quarantine=summary.quarantineManifest||[];check(production.every(manifestCommonRowGreen)&&quarantine.every(manifestCommonRowGreen),stage.toLowerCase()+'.summary.manifest-row-shape');
  check(production.every(row=>row.stage===stage&&row.disposition==='PRODUCTION_READY'&&row.reasons.length===0),' '+stage.toLowerCase()+'.summary.production-row-contract');
  setEqual(production.map(row=>row.key),productionKeys,stage.toLowerCase()+'.summary.production-membership');
  check(quarantine.slice(0,inheritedQuarantine.length).length===inheritedQuarantine.length,' '+stage.toLowerCase()+'.summary.inherited-quarantine-count');
  const inheritedByIdentity=new Map(inheritedQuarantine.map(row=>[row.key+'\0'+row.stage,row]));for(const row of quarantine){const inherited=inheritedByIdentity.get(row.key+'\0'+row.stage);if(inherited)equal(row,inherited,stage.toLowerCase()+'.summary.inherited-quarantine-row.'+row.key);}
  const currentFailures=quarantine.filter(row=>row.stage===stage);check(currentFailures.every(row=>row.disposition==='QUARANTINED_NO_PROMOTION'&&row.status===stage+'_FAILED'&&row.reasons.length>0&&row.reasons.every(reason=>typeof reason==='string'&&reason.length>0)),stage.toLowerCase()+'.summary.failure-row-contract');
  const inputKeys=inputRows.map(row=>row.key),failureKeys=currentFailures.map(row=>row.key);check(new Set([...productionKeys,...failureKeys]).size===inputKeys.length,stage.toLowerCase()+'.summary.partition-unique');setEqual([...productionKeys,...failureKeys],inputKeys,stage.toLowerCase()+'.summary.partition-membership');
  const expectedQuarantine=[...inheritedQuarantine,...currentFailures].sort(manifestRowCompare);equal(quarantine,expectedQuarantine,stage.toLowerCase()+'.summary.quarantine-union');
  check(summary.finalProductionCount===production.length&&summary.quarantinedCount===quarantine.length&&summary.status===(quarantine.length?'PASS_WITH_QUARANTINE':'PASS'),stage.toLowerCase()+'.summary.partition-counts');
  return{productionRows:production,productionKeys:production.map(row=>row.key),quarantine};
}

function verifyCatalog(){
  const catalog=readJson(CATALOG_FILE,'catalog');if(!catalog)return null;
  check(catalog.schema==='MassfrontStage10ModelReviewCatalogV1','catalog.schema',catalog.schema);check(catalog.lifecycle==='REVIEW_ONLY_RUNTIME_INACTIVE','catalog.lifecycle',catalog.lifecycle);check(catalog.allRuntimeActive===false&&catalog.allRuntimeRegistered===false,'catalog.runtime-inactive');
  equal(catalog.sourceRootsReadOnly,['modules/space_exploration/assets/source/blender/world-kits','modules/space_exploration/assets/source/spline'],'catalog.source-roots');
  const entries=[];
  for(const family of catalog.worldKits||[]){check(typeof family.id==='string'&&family.id.length>0,'catalog.family-id');check(family.moduleCount===(family.modules||[]).length,'catalog.family-count',family.id);for(const module of family.modules||[])entries.push({key:module.key,id:module.id,family:family.id,category:module.category??family.label??family.id,kind:'world-kit',lifecycle:module.lifecycle,repairLocked:module.repairLocked===true,metadataBlocked:false,runtimeActive:module.runtimeActive,runtimeRegistered:module.runtimeRegistered,model:module.model});}
  for(const model of catalog.splineExports||catalog.splineModels||[])entries.push({key:model.key,id:model.id,family:'spline-world-prefabs',category:model.category??'Spline world prefabs',kind:'spline',lifecycle:model.lifecycle,repairLocked:model.repairLocked===true,metadataBlocked:model.metadataBlocked===true,runtimeActive:model.runtimeActive,runtimeRegistered:model.runtimeRegistered,model:model.model});
  const worldCount=entries.filter(item=>item.kind==='world-kit').length,splineCount=entries.filter(item=>item.kind==='spline').length;
  check(worldCount===EXPECTED.world,'catalog.world-count',worldCount);check(splineCount===EXPECTED.spline,'catalog.spline-count',splineCount);check(entries.length===EXPECTED.candidates,'catalog.candidate-count',entries.length);check(new Set(entries.map(item=>item.key)).size===entries.length,'catalog.unique-keys');check(new Set(entries.map(item=>item.model?.path)).size===entries.length,'catalog.unique-model-paths');
  setEqual(entries.filter(item=>item.repairLocked).map(item=>item.key),REPAIR_LOCKED.keys(),'catalog.exact-zero-repair-locks');equal(entries.filter(item=>item.metadataBlocked).map(item=>item.key),[],'catalog.exact-metadata-block');equal(catalog.repairLockedIds,[],'catalog.repair-locked-ids');
  for(const item of entries){const prefix=item.kind==='world-kit'?'modules/space_exploration/assets/source/blender/world-kits/':'modules/space_exploration/assets/source/spline/';check(item.model?.path?.startsWith(prefix),'catalog.model-source-root',item.key);verifyFileRecord(item.model?.path,item.model?.sha256,item.model?.bytes,ROOT,'catalog.model.'+item.key);check(item.runtimeActive===false&&item.runtimeRegistered===false,'catalog.item-runtime-inactive',item.key);}
  const upperKeys=entries.map(item=>(item.key+' '+item.id+' '+item.model.path).toUpperCase()),splineSource=path.join(ROOT,'modules','space_exploration','assets','source','spline'),splineFiles=walkFiles(splineSource).map(file=>posix(file).toUpperCase());for(const deleted of DELETED_SPLINE_PROPS){check(!upperKeys.some(value=>value.includes(deleted)),'catalog.deleted-prop-absent',deleted);check(!splineFiles.some(file=>file.includes(deleted)),'source.deleted-prop-tree-absent',deleted);const source=path.join(ROOT,'modules','space_exploration','assets','source','spline','world-prefabs','exports',deleted+'.glb');check(!fs.existsSync(source),'source.deleted-prop-absent',posix(source));const slug=deleted.toLowerCase().replaceAll('_','-'),processed=path.join(ROOT,'modules','space_exploration','assets','source','spline','processed',slug+'-v1','source');check(!fs.existsSync(path.join(processed,slug+'-source-v1.glb'))&&!fs.existsSync(path.join(processed,slug+'-source-v1.intake.json')),'source.deleted-prop-processed-absent',deleted);}
  equal(catalog.counts,{worldKitFamilies:8,worldKitModules:320,worldKitProcessingEligible:320,repairLocked:0,roadQaGlbs:31,splineExports:10,splineRendered:10,splineMetadataBlocked:1},'catalog.counts');
  const included=entries.filter(item=>!EXCLUSIONS.has(item.key));check(included.length===EXPECTED.included,'catalog.included-count',included.length);setEqual(entries.filter(item=>EXCLUSIONS.has(item.key)).map(item=>item.key),EXCLUSIONS.keys(),'catalog.exclusion-set');
  return{catalog,catalogHash:sha256File(CATALOG_FILE),entries,included,byKey:new Map(entries.map(item=>[item.key,item]))};
}
function expectedExclusionRecords(contract,includeLifecycle){return[...EXCLUSIONS].map(([key,reason])=>{const item=contract.byKey.get(key),record={key,id:item.id,family:item.family,kind:item.kind,reason,source:item.model.path,sourceSha256:item.model.sha256};if(includeLifecycle)record.catalogLifecycle=item.lifecycle;return record;}).sort((a,b)=>a.key.localeCompare(b.key));}
function stripExclusionOutputState(record){const copy={...record};delete copy.processingOutputCount;delete copy.processingOutputs;return copy;}

function verifyRepair(contract){
  check(sha256File(REPAIR_SCRIPT)===FROZEN_REPAIR_SCRIPT_SHA256,'repair.script.frozen-hash');check(sha256File(RASTER_CLEANUP_SCRIPT)===FROZEN_RASTER_CLEANUP_SCRIPT_SHA256,'repair.raster-script.frozen-hash');
  const summaryFile=path.join(OUT,'summary.json'),summary=readJson(summaryFile,'repair.summary');
  const loaded=loadReports(REPAIR_REPORT_ROOT,'MassfrontStage10ModelRepairV3','repair.reports'),expectedKeys=contract.included.map(item=>item.key);
  setEqual(loaded.paths.keys(),expectedKeys,'repair.report-membership');check(loaded.files.length===EXPECTED.included,'repair.report-count',loaded.files.length);
  const byKey=new Map(),productionReports=[],productionRows=[],repairDispositionManifest=[],quarantine=[];if(!summary)return{summary,summaryFile,reports:loaded.reports,paths:loaded.paths,byKey,productionReports,productionRows,repairDispositionManifest,quarantine,productionKeys:[]};
  check(summary.schema==='MassfrontStage10ModelRepairSummaryV3','repair.summary.schema',summary.schema);check(summary.pipelineVersion===EXPECTED.repairPipeline,'repair.summary.pipeline-version',summary.pipelineVersion);check(summary.pipelineMode==='DERIVED_SAME_WINDING_RASTER_CLEAN_AND_UV','repair.summary.pipeline-mode',summary.pipelineMode);
  check(summary.sourcePolicy==='CANONICAL_MODEL_BYTES_LOCKED_DERIVED_RASTER_CLEAN_AND_UV_ONLY','repair.summary.source-policy');check(summary.geometryPolicy==='SIGNED_SAME_WINDING_PAIR_LOCAL_CLEANUP_OPPOSITE_WINDING_PRESERVED','repair.summary.geometry-policy');equal(summary.repairDependencyContract,RASTER_DEPENDENCY,'repair.summary.dependencies');equal(summary.rasterAcceptanceContract,RASTER_ACCEPTANCE,'repair.summary.raster-contract');check(summary.pipelineScript===posix(REPAIR_SCRIPT)&&summary.pipelineScriptSha256===sha256File(REPAIR_SCRIPT),'repair.summary.pipeline-script-binding');check(summary.rasterCleanupScript===posix(RASTER_CLEANUP_SCRIPT)&&summary.rasterCleanupScriptSha256===sha256File(RASTER_CLEANUP_SCRIPT),'repair.summary.raster-script-binding');check(summary.rasterCleanup?.passed===true&&summary.rasterCleanup?.acceptedPairsAfter===0&&summary.rasterCleanup?.exactDuplicatePairsAfter===0&&summary.rasterCleanup?.remainingPairs===0&&Number.isInteger(summary.rasterCleanup?.modelsMutated)&&summary.rasterCleanup.modelsMutated>=0&&summary.rasterCleanup.modelsMutated<=330&&Number.isInteger(summary.rasterCleanup?.maximumStabilizationPasses)&&summary.rasterCleanup.maximumStabilizationPasses<=RASTER_ACCEPTANCE.maximumCleanupPasses,'repair.summary.raster-cleanup');check(summary.catalog===posix(CATALOG_FILE)&&summary.catalogSha256===contract.catalogHash,'repair.summary.catalog-binding');
  equal(summary.catalogCounts,{worldModules:320,splineModels:10,catalogCandidates:330,repairLocked:0,metadataBlocked:1,pipelineExcluded:3},'repair.summary.catalog-counts');equal(summary.selection,{families:[],only:[],includeSpline:true,limit:0,fullCatalog:true},'repair.summary.full-selection');setEqual(summary.processedStatusContract||[],REPAIR_STATUSES,'repair.summary.status-contract');check(summary.pipelineExclusionCount===3,'repair.summary.exclusion-count');
  equal((summary.pipelineExclusions||[]).map(stripExclusionOutputState).sort((a,b)=>a.key.localeCompare(b.key)),expectedExclusionRecords(contract,true),'repair.summary.exclusions');
  for(const exclusion of summary.pipelineExclusions||[]){check(exclusion.processingOutputCount===0,'repair.exclusion.output-count',exclusion.key);equal(exclusion.processingOutputs,[],'repair.exclusion.outputs.'+exclusion.key);}
  equal(summary.staleExcludedOutputsRemaining,[],'repair.summary.no-stale-excluded');check(summary.totalScheduled===327,'repair.summary.scheduled',summary.totalScheduled);check(summary.processed===327,'repair.summary.processed',summary.processed);check(summary.failed===0&&Array.isArray(summary.failures)&&summary.failures.length===0,'repair.summary.no-failures');check(fs.existsSync(REPAIR_SCRIPT),'repair.script.exists');check(fs.existsSync(RASTER_CLEANUP_SCRIPT),'repair.raster-script.exists');
  const referencedModels=[],referencedTextures=[];
  for(const report of loaded.reports){
    const item=contract.byKey.get(report.key);if(!item)continue;byKey.set(report.key,report);
    const reportFile=loaded.paths.get(report.key),expectedPath=path.join(REPAIR_REPORT_ROOT,item.family,safeId(item.id)+'.json');check(path.resolve(reportFile)===path.resolve(expectedPath),'repair.report-path',report.key);
    check(report.pipelineVersion===EXPECTED.repairPipeline&&report.pipelineMode==='DERIVED_SAME_WINDING_RASTER_CLEAN_AND_UV','repair.item.pipeline',report.key);check(report.pipelineScript===posix(REPAIR_SCRIPT)&&report.pipelineScriptSha256===sha256File(REPAIR_SCRIPT)&&report.rasterCleanupScript===posix(RASTER_CLEANUP_SCRIPT)&&report.rasterCleanupScriptSha256===sha256File(RASTER_CLEANUP_SCRIPT),'repair.item.script-bindings',report.key);equal([report.id,report.family,report.category,report.kind],[item.id,item.family,item.category,item.kind],'repair.item.identity.'+report.key);check(report.catalogSha256===contract.catalogHash,'repair.item.catalog-hash',report.key);check(report.source===item.model.path&&report.sourceSha256===item.model.sha256,'repair.item.source-binding',report.key);verifyFileRecord(report.source,report.sourceSha256,item.model.bytes,ROOT,'repair.source.'+report.key);
    check(report.sourceUntouched===true&&report.canonicalGeometryLocked===true&&report.canonicalTopologyUntouched===true&&report.modelGenerationPerformed===false&&report.derivedUvRefreshOnly===false&&report.uvGeometryPreserved===true,'repair.item.locked-source-policy',report.key);check(report.catalogLifecycle===item.lifecycle&&report.catalogRepairLocked===false&&report.catalogMetadataBlocked===false,'repair.item.catalog-flags',report.key);check(REPAIR_STATUSES.has(report.status),'repair.item.status',{key:report.key,status:report.status});check(Array.isArray(report.statusReasons)&&(report.status==='RECONSTRUCTION_REQUIRED'?report.statusReasons.length>0:report.statusReasons.length===0),'repair.item.status-reasons',report.key);
    const unfinished=report.status==='RECONSTRUCTION_REQUIRED',modelPrefix='tmp/stage10-model-repair/'+(unfinished?'unfinished-models/':'models/')+item.family+'/',texturePrefix='tmp/stage10-model-repair/'+(unfinished?'unfinished-texture-sources/':'texture-sources/')+item.family+'/';
    check(report.output?.startsWith(modelPrefix)&&path.posix.basename(report.output)===path.posix.basename(item.model.path),'repair.item.output-path',report.key);check(report.textureSource?.startsWith(texturePrefix)&&path.posix.basename(report.textureSource)===path.posix.parse(item.model.path).name+'-LOD0-UVGEN.glb','repair.item.texture-path',report.key);
    const output=verifyFileRecord(report.output,report.outputSha256,undefined,OUT,'repair.output.'+report.key),texture=verifyFileRecord(report.textureSource,report.textureSourceSha256,undefined,OUT,'repair.texture-source.'+report.key);referencedModels.push(output);referencedTextures.push(texture);
    const outputParsed=parseGlbSemantic(output),textureParsed=parseGlbSemantic(texture),disposition=repairProofDisposition(report,outputParsed,textureParsed),row={key:report.key,status:report.status,disposition:disposition.accepted?'ELIGIBLE_FOR_PBR':'QUARANTINED_NO_PROMOTION',stage:'REPAIR',reasons:disposition.reasons,report:posix(reportFile),reportSha256:sha256File(reportFile),output:report.output,outputSha256:report.outputSha256};repairDispositionManifest.push(row);
    if(disposition.accepted){productionReports.push(report);productionRows.push(row);}else quarantine.push(row);
    if(!disposition.accepted)continue;
    const raster=report.rasterCleanup||{};equal(report.repairDependencyContract,RASTER_DEPENDENCY,'repair.item.dependencies.'+report.key);equal(report.rasterAcceptanceContract,RASTER_ACCEPTANCE,'repair.item.raster-contract.'+report.key);equal(raster.dependencyContract,RASTER_DEPENDENCY,'repair.item.raster-dependencies.'+report.key);equal(raster.contract,RASTER_ACCEPTANCE,'repair.item.raster-cleanup-contract.'+report.key);check(raster.canonicalSourceMutated===false&&raster.canonicalTopologyUntouched===true&&finiteNumber(raster.maxBoundsDriftM)&&raster.maxBoundsDriftM<=RASTER_BOUNDS_DRIFT_LIMIT,'repair.item.raster-cleanup',report.key);check(report.derivedTopologyChanged===raster.mutated&&report.derivedRasterCleanupPerformed===raster.mutated&&report.topologyPreserved===!raster.mutated,'repair.item.derived-topology-state',report.key);check(finiteNumber(report.maxDimensionDriftM)&&report.maxDimensionDriftM<=RASTER_BOUNDS_DRIFT_LIMIT,'repair.item.bounds-drift',{key:report.key,drift:report.maxDimensionDriftM});
    check(report.overlapFaces===0&&report.overlapLoops===0&&report.allRenderMeshOverlapFaces===0&&report.allRenderMeshOverlapLoops===0,'repair.item.zero-overlap',report.key);equal(report.uvOverlapProof,{method:'BLENDER_UV_SELECT_OVERLAP',scope:'PER_RENDER_MESH_PACKED_ATLAS',passed:true},'repair.item.overlap-proof.'+report.key);check(report.uvContract?.UV_GEN?.gltfTexcoord===0&&report.uvContract?.UVMap_Tile?.gltfTexcoord===1,'repair.item.uv-contract',report.key);check(report.uvContract?.UV_GEN?.purpose==='per-render-mesh per-face bake atlas inside 0-1 with measured internal zero overlap'&&report.uvContract?.UVMap_Tile?.purpose==='world-space cubic projection at 4 metres per tile for later PBR binding','repair.item.uv-purpose',report.key);
    const aggregate=report.tiledUvMetrics||{};check(aggregate.method===TILED_UV_METRIC_METHOD&&aggregate.valid===true&&aggregate.worldDoubleAreaFloor===TILED_UV_WORLD_DOUBLE_AREA_FLOOR&&aggregate.projectedDoubleAreaFloorMethod===TILED_UV_PROJECTED_FLOOR_METHOD&&aggregate.projectedFloorIsDimensionalConversionNotToleranceRelaxation===true,'repair.item.cubic-metrics-valid',report.key);check(finiteNumber(aggregate.maxStretchRatio)&&aggregate.maxStretchRatio<=CUBIC_STRETCH_LIMIT,'repair.item.cubic-stretch',{key:report.key,value:aggregate.maxStretchRatio});check(finiteNumber(aggregate.maxAreaScaleRelativeError)&&aggregate.maxAreaScaleRelativeError<=TEXEL_SCALE_ERROR_LIMIT,'repair.item.texel-scale',{key:report.key,value:aggregate.maxAreaScaleRelativeError});
    check(Array.isArray(report.meshes)&&report.meshes.length===report.renderMeshCount&&report.meshes.length>0,'repair.item.mesh-count',report.key);let overlapFaces=0,overlapLoops=0;
    for(const mesh of report.meshes||[]){const cleanup=mesh.cleanup||{},packed=mesh.packedUv||{},tiled=mesh.tiledUv||{};check(cleanup.mode==='DERIVED_SAME_WINDING_RASTER_CLEAN_AND_UV','repair.mesh.cleanup-mode',report.key+'/'+mesh.name);check(cleanup.canonicalTopologyUntouched===true&&cleanup.uvGeometryPreserved===true&&cleanup.derivedTopologyChanged===!cleanup.topologyPreserved,'repair.mesh.cleanup-state',report.key+'/'+mesh.name);check(SHA_PATTERN.test(cleanup.geometrySignatureBefore||'')&&SHA_PATTERN.test(cleanup.geometrySignatureAfterRasterCleanup||'')&&cleanup.geometrySignatureAfterRasterCleanup===cleanup.geometrySignatureAfter,'repair.mesh.geometry-signature',report.key+'/'+mesh.name);if(cleanup.topologyPreserved)equal(cleanup.before,cleanup.after,'repair.mesh.unmutated-topology-counts.'+report.key+'/'+mesh.name);check(mesh.collapsedUvsRepaired===0,'repair.mesh.no-collapsed-manual-repair',report.key+'/'+mesh.name);equal(mesh.uvLayers,['UV_GEN','UVMap_Tile'],'repair.mesh.uv-order.'+report.key+'/'+mesh.name);check(packed.present===true&&packed.finite===true&&packed.insideUnitSquare===true&&packed.zeroAreaFaces===0&&packed.overlapFaces===0&&packed.overlapLoops===0,'repair.mesh.packed-uv',report.key+'/'+mesh.name);check(tiled.present===true&&tiled.finite===true&&tiled.zeroAreaFaces===0,'repair.mesh.tiled-uv',report.key+'/'+mesh.name);overlapFaces+=packed.overlapFaces||0;overlapLoops+=packed.overlapLoops||0;}
    check(overlapFaces===report.allRenderMeshOverlapFaces&&overlapLoops===report.allRenderMeshOverlapLoops,'repair.item.overlap-recomputed',report.key);verifyUvGlb(output,'repair.glb.'+report.key);verifyUvGlb(texture,'repair.texture-glb.'+report.key);
  }
  check(materialSemanticSummaryGreen(summary.materialSemanticContract,contract,loaded.reports),'repair.summary.material-semantic-contract');const repairCounts=Object.fromEntries([...REPAIR_STATUSES].map(status=>[status,0]));for(const report of loaded.reports)repairCounts[report.status]=(repairCounts[report.status]||0)+1;equal(summary.statusCounts,repairCounts,'repair.summary.status-counts-recomputed');check(sum(Object.values(summary.statusCounts||{}))===327,'repair.summary.status-total');equal(summary.families,familyRepairCounts(loaded.reports),'repair.summary.family-counts-recomputed');
  const actualModels=walkFiles(path.join(OUT,'models'),file=>file.toLowerCase().endsWith('.glb')).concat(walkFiles(path.join(OUT,'unfinished-models'),file=>file.toLowerCase().endsWith('.glb'))),actualTextures=walkFiles(path.join(OUT,'texture-sources'),file=>file.toLowerCase().endsWith('.glb')).concat(walkFiles(path.join(OUT,'unfinished-texture-sources'),file=>file.toLowerCase().endsWith('.glb')));
  setEqual(actualModels.map(posix),referencedModels.map(posix),'repair.output-files-exact');setEqual(actualTextures.map(posix),referencedTextures.map(posix),'repair.texture-files-exact');for(const key of EXCLUSIONS.keys())check(!byKey.has(key),'repair.excluded-key-absent',key);
  repairDispositionManifest.sort(manifestRowCompare);productionRows.sort(manifestRowCompare);quarantine.sort(manifestRowCompare);check(repairDispositionManifest.length===EXPECTED.included&&productionRows.length+quarantine.length===EXPECTED.included,'repair.production-partition');setEqual(repairDispositionManifest.map(row=>row.key),expectedKeys,'repair.disposition-membership');check(productionRows.every(row=>row.disposition==='ELIGIBLE_FOR_PBR'&&REPAIR_ELIGIBLE_STATUSES.includes(row.status)&&row.reasons.length===0)&&quarantine.every(row=>row.disposition==='QUARANTINED_NO_PROMOTION'&&Array.isArray(row.reasons)&&row.reasons.length>0),'repair.disposition-contract');
  const tiledSummary=summary.tiledUvProof||{};check(tiledSummary.schema==='MassfrontStage10TiledUvSummaryProofV1'&&tiledSummary.metricMethod===TILED_UV_METRIC_METHOD&&tiledSummary.anchorMethod===TILED_UV_ANCHOR_METHOD&&tiledSummary.modelsProven===EXPECTED.included&&exactJsProofHash(tiledSummary),'repair.summary.tiled-proof-binding');
  return{summary,summaryFile,reports:loaded.reports,paths:loaded.paths,byKey,productionReports,productionRows,repairDispositionManifest,quarantine,productionKeys:productionRows.map(row=>row.key)};
}

const ZFIGHT_AUDIT_MODE = 'READ_ONLY_SAME_LOD_SAME_WINDING_RASTER_RISK_V1';
const ZFIGHT_BROAD_PHASE_METHOD = 'WORLD_TRIANGLE_BVH_NEAREST_RANGE_V1';
const ZFIGHT_DUPLICATE_AUTHORITY = 'RAW_GLB_NONDEGENERATE_RENDER_NODE_SAME_WINDING';
function verifyZFightingV3(contract,repair,pbr){
  const summaryFile=path.join(ZFIGHT_ROOT,'summary.json'),summary=readJson(summaryFile,'zfighting.summary'),loaded=loadReports(ZFIGHT_ROOT,'MassfrontStage10ZFightingAuditV3','zfighting.reports',true),expectedKeys=pbr.productionKeys;
  check([...loaded.paths.keys()].every(key=>expectedKeys.includes(key)),'zfighting.report-input-subset');check(loaded.files.length<=expectedKeys.length,'zfighting.report-count',loaded.files.length);check(fs.existsSync(ZFIGHT_SCRIPT)&&fs.statSync(ZFIGHT_SCRIPT).isFile(),'zfighting.script.exists');check(fs.existsSync(RASTER_CLEANUP_SCRIPT)&&fs.statSync(RASTER_CLEANUP_SCRIPT).isFile(),'zfighting.raster-script.exists');const scriptHash=sha256File(ZFIGHT_SCRIPT);
  if(!summary)return{summary,summaryFile,reports:loaded.reports,paths:loaded.paths,productionRows:[],productionKeys:[],quarantine:pbr.quarantine};
  check(summary.schema==='MassfrontStage10ZFightingAuditSummaryV3'&&summary.pipelineVersion===EXPECTED.zFightingAudit&&summary.auditMode===ZFIGHT_AUDIT_MODE&&summary.broadPhaseMethod===ZFIGHT_BROAD_PHASE_METHOD&&summary.exactDuplicateAuthority===ZFIGHT_DUPLICATE_AUTHORITY,'zfighting.summary.contract');check(summary.rawGlbAuditMethod===RAW_GLTF_AUDIT_METHOD&&summary.sourceBindingPolicy==='CATALOG_REPAIR_AND_EXACT_PBR_REPORT_OUTPUT_SHA256','zfighting.summary.source-contract');check(summary.sourcePolicy==='READ_ONLY_NO_MODEL_MUTATION'&&summary.catalogSha256===contract.catalogHash,'zfighting.summary.source-binding');equal(summary.repairDependencyContract,RASTER_DEPENDENCY,'zfighting.summary.dependencies');equal(summary.rasterAcceptanceContract,RASTER_ACCEPTANCE,'zfighting.summary.raster-contract');check(summary.repairPipelineScript===posix(REPAIR_SCRIPT)&&summary.repairPipelineScriptSha256===sha256File(REPAIR_SCRIPT)&&summary.pbrPipelineScript===posix(PBR_SCRIPT)&&summary.pbrPipelineScriptSha256===sha256File(PBR_SCRIPT)&&summary.rasterCleanupScript===posix(RASTER_CLEANUP_SCRIPT)&&summary.rasterCleanupScriptSha256===sha256File(RASTER_CLEANUP_SCRIPT),'zfighting.summary.script-bindings');equal(summary.pbrRasterPolicy,PBR_RASTER_POLICY,'zfighting.summary.pbr-policy');check(summary.pbrRasterPolicySha256===canonicalHash(PBR_RASTER_POLICY),'zfighting.summary.pbr-policy-hash');
  equal(summary.selection,{families:[],only:[],includeSpline:true,limit:0,fullCatalog:true},'zfighting.summary.full-selection');equal(summary.catalogCounts,{worldModules:320,splineModels:10,repairLocked:0,metadataBlocked:1,pipelineExcluded:3},'zfighting.summary.catalog-counts');equal((summary.exclusions||[]).sort((a,b)=>a.key.localeCompare(b.key)),expectedExclusionRecords(contract,false),'zfighting.summary.exclusions');
  const greenReports=loaded.reports.filter(report=>report.status==='PASS'&&report.passed===true&&report.geometryMutationPerformed===false&&report.alternateLodComparisonPerformed===false&&report.findings?.total===0&&report.rawGlbAudit?.passed===true&&report.rawGlbAudit?.exactDuplicateTrianglesWithinRenderNode===0&&report.rasterAudit?.passed===true&&report.rasterAudit?.pairs===0),greenKeys=greenReports.map(report=>report.key),partition=verifyStageManifestContract(summary,'AUDIT',pbr.productionRows,pbr.quarantine,greenKeys),summaryRows=summary.items||[],items=new Map(summaryRows.map(item=>[item.key,item]));check(summaryRows.length===greenReports.length&&items.size===greenReports.length,'zfighting.summary.item-count',summaryRows.length);setEqual(items.keys(),greenKeys,'zfighting.summary.item-membership');let rawTotal=0,rasterTotal=0;
  for(const report of greenReports){
    const item=contract.byKey.get(report.key),repairReport=repair.byKey.get(report.key),pbrReport=pbr.byKey.get(report.key);if(!item||!repairReport||!pbrReport)continue;const reportFile=loaded.paths.get(report.key),expectedReport=path.join(ZFIGHT_ROOT,item.family,safeId(item.id)+'.json'),repairFile=repair.paths.get(report.key),pbrFile=pbr.paths.get(report.key);check(path.resolve(reportFile)===path.resolve(expectedReport),'zfighting.item.report-path',report.key);check(report.pipelineVersion===EXPECTED.zFightingAudit&&report.auditMode===ZFIGHT_AUDIT_MODE&&report.broadPhaseMethod===ZFIGHT_BROAD_PHASE_METHOD&&report.exactDuplicateAuthority===ZFIGHT_DUPLICATE_AUTHORITY&&report.auditScriptSha256===scriptHash&&report.status==='PASS'&&report.passed===true&&report.geometryMutationPerformed===false&&report.alternateLodComparisonPerformed===false,'zfighting.item.contract',report.key);equal([report.id,report.family,report.category,report.kind],[item.id,item.family,item.category,item.kind],'zfighting.item.identity.'+report.key);check(report.catalogSha256===contract.catalogHash,'zfighting.item.catalog-hash',report.key);
    check(report.repairReport===posix(repairFile)&&report.repairReportSha256===sha256File(repairFile)&&report.repairPipelineVersion===EXPECTED.repairPipeline&&report.repairOutput===repairReport.output&&report.repairOutputSha256===repairReport.outputSha256,'zfighting.item.repair-binding',report.key);check(report.repairPipelineScript===posix(REPAIR_SCRIPT)&&report.repairPipelineScriptSha256===sha256File(REPAIR_SCRIPT)&&report.rasterCleanupScript===posix(RASTER_CLEANUP_SCRIPT)&&report.rasterCleanupScriptSha256===sha256File(RASTER_CLEANUP_SCRIPT)&&report.repairRasterCleanupSha256===pbrReport.repairRasterCleanupSha256&&report.repairLineageProofSha256===pbrReport.repairLineageProofSha256&&report.repairMaterialSemanticContractSha256===pbrReport.repairMaterialSemanticContractSha256,'zfighting.item.repair-script-bindings',report.key);check(report.pbrReport===posix(pbrFile)&&report.pbrReportSha256===sha256File(pbrFile)&&report.pbrPipelineVersion===EXPECTED.pbrPipeline&&report.pbrPipelineScriptSha256===sha256File(PBR_SCRIPT)&&report.pbrOutput===pbrReport.output&&report.pbrOutputSha256===pbrReport.outputSha256&&report.source===pbrReport.output&&report.sourceSha256===pbrReport.outputSha256&&report.pbrContractSha256===pbrReport.contractSha256&&report.pbrResumeContractSha256===pbrReport.resumeContractSha256&&report.pbrFinalArtifactIdentitySha256===pbrReport.postExportArtifactIdentitySha256&&report.pbrFinalUvProofSha256===pbrReport.finalUvProofSha256,'zfighting.item.pbr-binding',report.key);verifyFileRecord(report.source,report.sourceSha256,undefined,PBR_MODEL_ROOT,'zfighting.source.'+report.key);
    equal(report.repairDependencyContract,RASTER_DEPENDENCY,'zfighting.item.dependencies.'+report.key);equal(report.rasterAcceptanceContract,RASTER_ACCEPTANCE,'zfighting.item.raster-contract.'+report.key);equal(report.tolerances,RASTER_ACCEPTANCE,'zfighting.item.tolerances.'+report.key);equal(report.pbrRasterBinding?.policy,PBR_RASTER_POLICY,'zfighting.item.pbr-policy.'+report.key);check(report.pbrRasterBinding?.policySha256===canonicalHash(PBR_RASTER_POLICY)&&report.pbrRasterBinding?.passed===true&&report.pbrRasterBinding?.renderPrimitiveCount===pbrReport.fullPbrCoverage?.renderPrimitives&&report.pbrRasterBinding?.opaqueSingleSidedMaterialCount===pbrReport.fullPbrCoverage?.opaqueMaterialCount,'zfighting.item.pbr-raster-binding',report.key);
    const raster=report.rasterAudit||{};equal(raster.contract,RASTER_ACCEPTANCE,'zfighting.item.shared-contract.'+report.key);equal(raster.dependencyContract,RASTER_DEPENDENCY,'zfighting.item.shared-dependencies.'+report.key);check(raster.schema==='MassfrontStage10RasterCleanupV2'&&raster.pairs===0&&raster.passed===true&&finiteNumber(raster.riskAreaM2)&&raster.riskAreaM2===0&&raster.metricsByLod&&typeof raster.metricsByLod==='object','zfighting.item.shared-zero.'+report.key);rasterTotal+=raster.pairs||0;
    const raw=report.rawGlbAudit||{};check(raw.method===RAW_GLTF_AUDIT_METHOD&&raw.sourceSha256===report.sourceSha256&&raw.passed===true&&raw.exactDuplicateTrianglesWithinRenderNode===0&&(raw.duplicateSamples||[]).length===0&&SHA_PATTERN.test(report.rawGlbAuditSha256||''),'zfighting.item.raw-zero.'+report.key);rawTotal+=raw.exactDuplicateTrianglesWithinRenderNode||0;check(report.findings?.total===0&&report.findings?.rawExactDuplicateFacesWithinRenderNode===0,'zfighting.item.finding-zero.'+report.key);
    const summaryItem=items.get(report.key);check(summaryItem?.stage==='AUDIT'&&summaryItem?.disposition==='PRODUCTION_READY'&&Array.isArray(summaryItem?.reasons)&&summaryItem.reasons.length===0&&summaryItem?.status==='PASS'&&summaryItem?.passed===true&&summaryItem?.findings===0&&summaryItem?.rasterPairs===0&&summaryItem?.rawExactDuplicateFaces===0&&summaryItem?.pbrReportSha256===sha256File(pbrFile)&&summaryItem?.pbrOutputSha256===pbrReport.outputSha256&&summaryItem?.report===posix(reportFile)&&summaryItem?.reportSha256===sha256File(reportFile),'zfighting.summary.item-binding',report.key);
  }
  check(rawTotal===0,'zfighting.summary.production-raw-zero');check(rasterTotal===0,'zfighting.summary.production-raster-zero');equal(summary.statusCounts,countsBy(loaded.reports,'status'),'zfighting.summary.status-recomputed');const stageFailures=partition.quarantine.filter(row=>row.stage==='AUDIT');check(summary.totalScheduled===expectedKeys.length&&summary.processed===loaded.reports.length&&summary.failed===stageFailures.length,'zfighting.summary.complete');setEqual((summary.failures||[]).map(failure=>failure.key),stageFailures.map(row=>row.key),'zfighting.summary.failure-membership');return{summary,summaryFile,reports:greenReports,allReports:loaded.reports,paths:loaded.paths,...partition};
}

function verifyPbrGlb(file,report,code){
  const parsed=verifyUvGlb(file,code,true);if(!parsed)return null;const{document}=parsed,primitives=renderPrimitives(document,true),textures=document.textures||[],samplers=document.samplers||[],materials=document.materials||[],images=glbImages(parsed,file,OUT,code),imageHashes=new Set(images.map(image=>image.sha256)),requiredMapHashes=new Set(),requiredEmissive=new Set((report.materials||[]).filter(material=>material.emissiveRequired).map(material=>material.outputMaterial));
  check(materials.length>0&&materials.every(material=>material.alphaMode==='OPAQUE'&&material.doubleSided===false),code+'.exact-raster-policy');
  const coverage={renderPrimitives:0,baseColorTexture:0,normalTexture:0,metallicRoughnessTexture:0,occlusionTexture:0,emissiveTextureWhereRequired:0};
  function textureInfo(material,semantic){return semantic==='baseColorTexture'||semantic==='metallicRoughnessTexture'?material.pbrMetallicRoughness?.[semantic]:material[semantic];}
  const usedImages=new Set(),usedMaterialNames=new Set(),singleSidedMaterials=new Set();let triangleCount=0;
  function validateTexture(info,semantic,materialName){check(info&&Number.isInteger(info.index)&&info.texCoord===1,code+'.binding.'+semantic,materialName);if(!info||!Number.isInteger(info.index))return;const texture=textures[info.index];check(!!texture,code+'.texture-index',{semantic,materialName});if(!texture)return;const sampler=Number.isInteger(texture.sampler)?samplers[texture.sampler]:{};check((sampler?.wrapS??GL_REPEAT)===GL_REPEAT&&(sampler?.wrapT??GL_REPEAT)===GL_REPEAT,code+'.sampler-repeat',materialName);const source=texture.source;check(Number.isInteger(source)&&images[source]&&!texture.extensions?.KHR_texture_basisu,code+'.core-png-texture-image',{semantic,materialName});if(Number.isInteger(source))usedImages.add(source);coverage[semantic]++;}
  for(const materialReport of report.materials||[])for(const map of Object.values(materialReport.maps||{}))requiredMapHashes.add(map.sha256);
  for(const{primitive,mesh}of primitives){coverage.renderPrimitives++;check((primitive.mode??4)===4,code+'.triangle-mode',mesh.name);const accessor=(document.accessors||[])[primitive.indices];check(Number.isInteger(primitive.indices)&&accessor&&accessor.count%3===0,code+'.triangle-indices',mesh.name);if(accessor)triangleCount+=accessor.count/3;check(Number.isInteger(primitive.material)&&materials[primitive.material],code+'.primitive-material',mesh.name);const material=materials[primitive.material];if(!material)continue;const name=material.name||'';check(material.alphaMode==='OPAQUE'&&material.doubleSided===false,code+'.opaque-single-sided',name);singleSidedMaterials.add(primitive.material);usedMaterialNames.add(name);for(const semantic of['baseColorTexture','normalTexture','metallicRoughnessTexture','occlusionTexture'])validateTexture(textureInfo(material,semantic),semantic,name);if(requiredEmissive.has(name))validateTexture(material.emissiveTexture,'emissiveTextureWhereRequired',name);}
  check(images.length>0&&images.every(image=>image.storage==='BUFFER_VIEW'&&image.mimeType==='image/png'),code+'.embedded-core-png-images');setEqual(imageHashes,requiredMapHashes,code+'.exact-reported-maps-embedded');setEqual(usedImages,images.map(image=>image.index),code+'.all-images-used');check(singleSidedMaterials.size===materials.length,code+'.all-materials-used',{used:singleSidedMaterials.size,defined:materials.length});setEqual(usedMaterialNames,(report.materials||[]).map(material=>material.outputMaterial),code+'.material-manifest');const expected=report.fullPbrCoverage||{};setEqual(expected.materialNames||[],usedMaterialNames,code+'.reported-material-names');for(const[field,value]of Object.entries(coverage))check(expected[field]===value,code+'.coverage.'+field,{actual:value,reported:expected[field]});check(expected.totalBound===primitives.length&&expected.passed===true,code+'.coverage-total');check(expected.requiredEmissiveMaterials===requiredEmissive.size,code+'.emissive-material-count');check(expected.opaqueMaterialCount===singleSidedMaterials.size&&expected.singleSidedMaterialCount===singleSidedMaterials.size&&expected.doubleSidedMaterialCount===0,code+'.opaque-single-sided-material-count');check(expected.renderTriangleCount===triangleCount,code+'.triangle-count',{actual:triangleCount,reported:expected.renderTriangleCount});
  const reportedImages=(expected.embeddedImages||[]).map(image=>({...image})).sort((a,b)=>a.index-b.index),actualImages=images.map(image=>({index:image.index,name:image.name,sha256:image.sha256,width:image.width,height:image.height,bitDepth:image.bitDepth,channels:image.channels})).sort((a,b)=>a.index-b.index);equal(reportedImages,actualImages,code+'.embedded-images');return{parsed,images,primitiveCount:primitives.length};
}
function pbrFamilySummary(reports){const result={};for(const report of reports){const family=result[report.family]||={total:0,semantics:{}};family.total++;for(const material of report.materials||[])family.semantics[material.semantic]=(family.semantics[material.semantic]||0)+1;}return result;}
function semanticUse(reports){const result={};for(const report of reports)for(const material of report.materials||[])result[material.semantic]=(result[material.semantic]||0)+1;return Object.fromEntries(Object.entries(result).sort(([a],[b])=>a.localeCompare(b)));}
function verifyPbrRebindProvenance(report,repairReport,library,artifact,finalUv,coverage){
  const disposition=report.artifactDisposition,proof=report.contentEquivalentRebind;
  if(disposition==='REBUILT'){
    check(proof===null&&report.contentEquivalentRebindSha256===null,'pbr.rebind.rebuilt-empty',report.key);
    return null;
  }
  check(disposition==='CONTENT_EQUIVALENT_REBOUND','pbr.rebind.disposition',report.key);
  if(disposition!=='CONTENT_EQUIVALENT_REBOUND'||!proof){check(false,'pbr.rebind.missing',report.key);return;}
  check(proof.schema===CONTENT_EQUIVALENT_REBIND_SCHEMA&&proof.method===CONTENT_EQUIVALENT_REBIND_METHOD&&proof.passed===true,'pbr.rebind.contract',report.key);
  const legacy=proof.legacyReport||{},currentPipeline=proof.currentValidationPipeline||{},currentInput=proof.currentRepairInput||{},reused=proof.reusedPbrOutput||{},fresh=proof.freshValidation||{};
  const expectedLegacyPrefix='tmp/stage10-model-repair/pbr-reports/rebind-provenance/legacy/'+report.family+'/',expectedLegacyName=safeId(report.id)+'.'+legacy.sha256+'.legacy.json.provenance';check(legacy.path?.startsWith(expectedLegacyPrefix)&&path.posix.basename(legacy.path)===expectedLegacyName,'pbr.rebind.legacy-path',report.key);
  const legacyFile=verifyFileRecord(legacy.path,legacy.sha256,legacy.bytes,path.join(PBR_REPORT_ROOT,'rebind-provenance'),'pbr.rebind.legacy.'+report.key);
  let legacyReport=null;
  try{legacyReport=JSON.parse(fs.readFileSync(legacyFile,'utf8'));}catch(error){check(false,'pbr.rebind.legacy-json',report.key+': '+error.message);}
  check(legacy.pipelineScriptSha256===CONTENT_EQUIVALENT_REBIND_LEGACY_PIPELINE_SHA256&&legacyReport?.pipelineScriptSha256===legacy.pipelineScriptSha256&&legacyReport?.schema==='MassfrontStage10ModelPbrV1'&&legacyReport?.status==='PBR_TEXTURED'&&stable([legacyReport?.key,legacyReport?.id,legacyReport?.family,legacyReport?.category,legacyReport?.kind])===stable([report.key,report.id,report.family,report.category,report.kind]),'pbr.rebind.legacy-producer',report.key);
  check(currentPipeline.path===posix(PBR_SCRIPT)&&currentPipeline.sha256===sha256File(PBR_SCRIPT)&&currentPipeline.sha256===report.pipelineScriptSha256&&currentPipeline.blenderVersion===report.blenderVersion,'pbr.rebind.current-pipeline',report.key);
  check(legacy.sourceSha256===repairReport.outputSha256&&legacyReport?.source===currentInput.path&&legacyReport?.sourceSha256===legacy.sourceSha256&&legacy.outputSha256===report.outputSha256&&legacyReport?.output===reused.path&&legacyReport?.outputSha256===legacy.outputSha256,'pbr.rebind.legacy-byte-binding',report.key);
  verifyFileRecord(currentInput.path,currentInput.sha256,currentInput.bytes,OUT,'pbr.rebind.input.'+report.key);verifyFileRecord(reused.path,reused.sha256,reused.bytes,PBR_MODEL_ROOT,'pbr.rebind.output.'+report.key);
  check(Number.isInteger(currentInput.bytes)&&currentInput.bytes>0&&currentInput.path===repairReport.output&&currentInput.sha256===repairReport.outputSha256&&currentInput.sha256===legacy.sourceSha256,'pbr.rebind.current-input',report.key);
  check(Number.isInteger(reused.bytes)&&reused.bytes>0&&reused.path===report.output&&reused.sha256===report.outputSha256&&reused.sha256===legacy.outputSha256,'pbr.rebind.reused-output',report.key);
  const legacyMapCount=(legacyReport?.materials||[]).reduce((total,material)=>total+Object.keys(material.maps||{}).length,0),currentMapCount=(report.materials||[]).reduce((total,material)=>total+Object.keys(material.maps||{}).length,0);
  check(proof.byteIdenticalRepairInput===true&&proof.byteIdenticalPbrOutput===true&&Number.isInteger(proof.legacyMapRecordCountRevalidated)&&proof.legacyMapRecordCountRevalidated>0&&proof.legacyMapRecordCountRevalidated===legacyMapCount&&proof.legacyMapRecordCountRevalidated===currentMapCount&&proof.previousReportUsedAsCurrentAcceptanceEvidence===false&&proof.outputReexported===false&&proof.reportRewritten===true,'pbr.rebind.no-stale-evidence',report.key);
  check(proof.legacyMaterialManifestSha256===canonicalHash(legacyReport?.materials)&&proof.currentMaterialManifestSha256===canonicalHash(report.materials)&&proof.legacyMaterialManifestSha256===proof.currentMaterialManifestSha256,'pbr.rebind.material-manifest',report.key);
  check(proof.legacyLibraryContractSha256===legacyReport?.library?.contractSha256&&proof.currentLibraryContractSha256===library.contractSha256&&proof.legacyLibraryContractSha256===proof.currentLibraryContractSha256,'pbr.rebind.library-contract',report.key);
  check(fresh.sourceImported===true&&fresh.pbrOutputImported===true&&fresh.glbMapCoverageValidated===true&&fresh.opaqueSingleSidedValidated===true&&fresh.topologyIndexWindingPositionNormalUvIdentityValidated===true&&fresh.materialSlotCountsAndFaceAssignmentsValidated===true&&fresh.packedUvOverlapValidated===true&&fresh.tiledUvWorldJacobianValidated===true,'pbr.rebind.fresh-validations',report.key);
  check(fresh.sourceSceneIdentitySha256===artifact.sourceSceneIdentitySha256&&fresh.finalSceneIdentitySha256===artifact.finalSceneIdentitySha256&&fresh.sourceSceneIdentitySha256===fresh.finalSceneIdentitySha256&&fresh.fullPbrCoverageSha256===canonicalHash(coverage)&&fresh.finalUvProofSha256===canonicalHash(finalUv),'pbr.rebind.fresh-proof-hashes',report.key);
  check(report.contentEquivalentRebindSha256===canonicalHash(proof),'pbr.rebind.provenance-hash',report.key);
  return legacyFile;
}
function verifyPbr(contract,repair){
  const summaryFile=path.join(PBR_REPORT_ROOT,'summary.json'),libraryFile=path.join(PBR_REPORT_ROOT,'library','library.json'),summary=readJson(summaryFile,'pbr.summary'),libraryReport=readJson(libraryFile,'pbr.library'),loaded=loadReports(PBR_REPORT_ROOT,'MassfrontStage10ModelPbrV1','pbr.reports',true,file=>isWithin(file,path.join(PBR_REPORT_ROOT,'library'))),expectedKeys=repair.productionKeys,byKey=new Map(),glbByKey=new Map();check([...loaded.paths.keys()].every(key=>expectedKeys.includes(key)),'pbr.report-input-subset');check(loaded.files.length<=expectedKeys.length,'pbr.report-count',loaded.files.length);
  if(libraryReport){check(libraryReport.schema==='MassfrontStage10PbrTileLibraryV1'&&libraryReport.pipelineVersion===1&&libraryReport.mappingVersion==='MASSFRONT_LIVE_MAT_SEMANTIC_V1','pbr.library.contract');check(libraryReport.registry?.grid===11&&libraryReport.registry?.tileSize===256&&libraryReport.registry?.materialCount>0,'pbr.library.registry');const registryFile=verifyFileRecord(libraryReport.registry?.path,libraryReport.registry?.sha256,undefined,ROOT,'pbr.library.registry-source');for(const[role,atlas]of Object.entries(libraryReport.atlases||{})){const atlasFile=verifyFileRecord(atlas.path,atlas.sha256,undefined,ROOT,'pbr.library.atlas.'+role);check(atlas.width===2816&&atlas.height===2816&&stable(pngDimensions(atlasFile))===stable([2816,2816]),'pbr.library.atlas-dimensions',role);}const sourceContract={mappingVersion:libraryReport.mappingVersion,registrySource:{path:posix(registryFile),sha256:sha256File(registryFile)},grid:libraryReport.registry?.grid,tileSize:libraryReport.registry?.tileSize,atlases:libraryReport.atlases};check(libraryReport.sourceContractSha256===canonicalHash(sourceContract),'pbr.library.source-contract-hash');check(libraryReport.cells&&Object.keys(libraryReport.cells).length>0,'pbr.library.cells');}
  if(!summary)return{summary,summaryFile,libraryFile,libraryReport,reports:loaded.reports,paths:loaded.paths,byKey,glbByKey,productionRows:[],productionKeys:[],quarantine:repair.quarantine};
  check(summary.schema==='MassfrontStage10ModelPbrSummaryV1'&&summary.pipelineVersion===1&&summary.pipelineMode==='DERIVED_ATLAS_PBR_ONLY'&&summary.mappingVersion==='MASSFRONT_LIVE_MAT_SEMANTIC_V1','pbr.summary.contract');check(summary.sourcePolicy==='CANONICAL_GEOMETRY_LOCKED_DERIVED_PBR_OUTPUT_ONLY'&&summary.runtimePromotionPerformed===false,'pbr.summary.locked-policy');check(summary.contentEquivalentRebindRequested===true,'pbr.summary.rebind-requested');equal(summary.contentEquivalentRebindPolicy,CONTENT_EQUIVALENT_REBIND_POLICY,'pbr.summary.rebind-policy');check(summary.catalogSha256===contract.catalogHash,'pbr.summary.catalog-hash');check(summary.repairSummary?.path===posix(repair.summaryFile)&&summary.repairSummary?.sha256===sha256File(repair.summaryFile)&&summary.repairSummary?.schema==='MassfrontStage10ModelRepairSummaryV3'&&summary.repairSummary?.pipelineVersion===EXPECTED.repairPipeline,'pbr.summary.repair-binding');equal(summary.repairMaterialSemanticContract,repair.summary?.materialSemanticContract,'pbr.summary.material-semantic-contract');check(summary.repairMaterialSemanticContractSha256===canonicalHash(repair.summary?.materialSemanticContract)&&canonicalTextBinding(summary.repairSummary?.materialSemanticContractCanonicalJson,repair.summary?.materialSemanticContract,summary.repairSummary?.materialSemanticContractSha256),'pbr.summary.material-semantic-hash');equal(summary.selection,{families:[],only:[],includeSpline:true,limit:0,fullCatalog:true},'pbr.summary.full-selection');equal(summary.catalogCounts,{worldModules:320,splineModels:10,repairLocked:0,metadataBlocked:1,pipelineExcluded:3},'pbr.summary.catalog-counts');check(summary.repairCensusCount===EXPECTED.included&&summary.repairAcceptedCount===repair.productionRows.length&&summary.repairQuarantinedCount===repair.quarantine.length,'pbr.summary.repair-census-partition');check(manifestHashGreen(summary,'repairDispositionManifest'),'pbr.summary.repair-disposition-hash');equal(summary.repairDispositionManifest,repair.repairDispositionManifest,'pbr.summary.repair-disposition');equal((summary.exclusions||[]).sort((a,b)=>a.key.localeCompare(b.key)),expectedExclusionRecords(contract,false),'pbr.summary.exclusions');check(fs.existsSync(PBR_SCRIPT),'pbr.script.exists');const pipelineHash=sha256File(PBR_SCRIPT),referencedOutputs=[],referencedLegacyArchives=[];
  for(const report of loaded.reports){
    const item=contract.byKey.get(report.key),repairReport=repair.byKey.get(report.key);if(!item||!repairReport)continue;byKey.set(report.key,report);const reportFile=loaded.paths.get(report.key),expectedReport=path.join(PBR_REPORT_ROOT,item.family,safeId(item.id)+'.json');check(path.resolve(reportFile)===path.resolve(expectedReport),'pbr.item.report-path',report.key);check(report.pipelineVersion===1&&report.pipelineMode==='DERIVED_ATLAS_PBR_ONLY'&&report.mappingVersion==='MASSFRONT_LIVE_MAT_SEMANTIC_V1'&&report.status==='PBR_TEXTURED','pbr.item.contract',report.key);equal([report.id,report.family,report.category,report.kind],[item.id,item.family,item.category,item.kind],'pbr.item.identity.'+report.key);check(report.runtimePromotionPerformed===false&&report.modelGenerationPerformed===false&&report.canonicalGeometryLocked===true,'pbr.item.locked-policy',report.key);check(report.pipelineScriptSha256===pipelineHash,'pbr.item.pipeline-hash',report.key);check(report.repairPipelineScript===posix(REPAIR_SCRIPT)&&report.repairPipelineScriptSha256===sha256File(REPAIR_SCRIPT)&&report.rasterCleanupScript===posix(RASTER_CLEANUP_SCRIPT)&&report.rasterCleanupScriptSha256===sha256File(RASTER_CLEANUP_SCRIPT),'pbr.item.repair-script-bindings',report.key);check(report.catalogSha256===contract.catalogHash,'pbr.item.catalog-hash',report.key);check(report.canonicalSource===item.model.path&&report.canonicalSourceSha256===item.model.sha256,'pbr.item.canonical-source',report.key);check(report.repairReport===posix(repair.paths.get(report.key))&&report.repairReportSha256===sha256File(repair.paths.get(report.key))&&report.repairPipelineVersion===EXPECTED.repairPipeline,'pbr.item.repair-report-binding',report.key);equal(report.repairDependencyContract,RASTER_DEPENDENCY,'pbr.item.repair-dependencies.'+report.key);equal(report.rasterAcceptanceContract,RASTER_ACCEPTANCE,'pbr.item.raster-contract.'+report.key);check(canonicalTextBinding(report.repairRasterCleanupCanonicalJson,repairReport.rasterCleanup,report.repairRasterCleanupSha256)&&canonicalTextBinding(report.repairLineageProofCanonicalJson,repairReport.rasterCleanup?.lineageProof,report.repairLineageProofSha256)&&canonicalTextBinding(report.repairMaterialSemanticContractCanonicalJson,repairReport.materialSemanticContract,report.repairMaterialSemanticContractSha256)&&materialSemanticItemGreen(repairReport)&&lineageProofGreen(repairReport.rasterCleanup),'pbr.item.repair-proof-hashes',report.key);check(report.source===repairReport.output&&report.sourceSha256===repairReport.outputSha256,'pbr.item.repair-output-binding',report.key);verifyFileRecord(report.source,report.sourceSha256,undefined,OUT,'pbr.source.'+report.key);check(report.output?.startsWith('tmp/stage10-model-repair/pbr-models/'+item.family+'/')&&path.posix.basename(report.output)===path.posix.basename(repairReport.output),'pbr.item.output-path',report.key);const output=verifyFileRecord(report.output,report.outputSha256,undefined,PBR_MODEL_ROOT,'pbr.output.'+report.key);referencedOutputs.push(output);
    const library=report.library||{};check(SHA_PATTERN.test(library.sourceContractSha256||'')&&library.sourceContractSha256===libraryReport?.sourceContractSha256&&library.grid===11&&library.tileSize===256&&library.cells&&Object.keys(library.cells).length>0,'pbr.item.library',report.key);const libraryCopy={...library};delete libraryCopy.contractSha256;check(library.contractSha256===canonicalHash(libraryCopy),'pbr.item.library-hash',report.key);for(const[semantic,cell]of Object.entries(library.cells||{}))equal(cell,libraryReport?.cells?.[semantic],'pbr.item.library-cell-binding.'+report.key+'/'+semantic);const expectedResume={pipelineVersion:1,pipelineMode:'DERIVED_ATLAS_PBR_ONLY',pipelineScriptSha256:pipelineHash,mappingVersion:'MASSFRONT_LIVE_MAT_SEMANTIC_V1',contentEquivalentRebindPolicy:CONTENT_EQUIVALENT_REBIND_POLICY,blenderVersion:report.blenderVersion,catalogSha256:contract.catalogHash,repairReportSha256:sha256File(repair.paths.get(report.key)),repairPipelineScriptSha256:repairReport.pipelineScriptSha256,rasterCleanupScriptSha256:repairReport.rasterCleanupScriptSha256,repairRasterCleanupSha256:report.repairRasterCleanupSha256,repairLineageProofSha256:report.repairLineageProofSha256,repairMaterialSemanticContractSha256:report.repairMaterialSemanticContractSha256,sourceSha256:repairReport.outputSha256,librarySourceContractSha256:library.sourceContractSha256};check(stable(report.resumeContract)===stable(expectedResume)&&canonicalTextBinding(report.resumeContractCanonicalJson,expectedResume,report.resumeContractSha256),'pbr.item.resume-contract-hash',report.key);check(Array.isArray(report.materials)&&report.materials.length>0,'pbr.item.materials',report.key);const outputMaterials=new Set();
    for(const material of report.materials||[]){
      check(typeof material.sourceName==='string'&&Number.isInteger(material.sourceIndex)&&Array.isArray(material.sourceObjects)&&material.sourceObjects.length>0,'pbr.material.source',report.key+'/'+material.outputMaterial);check(typeof material.classificationRule==='string'&&typeof material.semantic==='string'&&typeof material.outputMaterial==='string','pbr.material.semantic',report.key);check(!outputMaterials.has(material.outputMaterial),'pbr.material.unique-output-name',report.key+'/'+material.outputMaterial);outputMaterials.add(material.outputMaterial);const cell=library.cells?.[material.semantic];check(!!cell&&cell.materialId===material.materialId&&stable(cell.atlasCell)===stable(material.atlasCell)&&cell.emissiveRequired===material.emissiveRequired,'pbr.material.library-cell',report.key+'/'+material.outputMaterial);equal(material.rasterization,PBR_RASTER_POLICY,'pbr.material.rasterization.'+report.key+'/'+material.outputMaterial);equal(material.maps,cell?.maps,'pbr.material.maps-library.'+report.key+'/'+material.outputMaterial);for(const role of['baseColor','normal','orm'])check(!!material.maps?.[role],'pbr.material.required-map.'+role,report.key+'/'+material.outputMaterial);check((!!material.maps?.emissive)===(material.emissiveRequired===true),'pbr.material.emissive-map',report.key+'/'+material.outputMaterial);
      for(const[role,map]of Object.entries(material.maps||{})){const mapFile=verifyFileRecord(map.path,map.sha256,undefined,path.join(PBR_REPORT_ROOT,'library','cells'),'pbr.map.'+report.key+'/'+material.outputMaterial+'/'+role);check(map.width===256&&map.height===256&&stable(pngDimensions(mapFile))===stable([256,256]),'pbr.map.dimensions',map.path);check(map.sampler==='REPEAT'&&map.wrap&&Object.values(map.wrap).every(finiteNumber),'pbr.map.repeat',map.path);}
      equal(material.bindings,{baseColorTexture:1,normalTexture:1,metallicRoughnessTexture:1,occlusionTexture:1,emissiveTexture:material.emissiveRequired?1:null,preservedPackedUv:{name:'UV_GEN',gltfTexcoord:0},tiledUv:{name:'UVMap_Tile',gltfTexcoord:1}},'pbr.material.bindings.'+report.key+'/'+material.outputMaterial);
    }
    const preservation=report.preservation||{};check(preservation.topology===true&&preservation.winding===true&&preservation.normals===true&&preservation.transforms===true&&preservation.materialSlotFaceAssignments===true,'pbr.item.preservation',report.key);equal(preservation.sourceGeometrySignatures,preservation.boundGeometrySignatures,'pbr.item.geometry-signatures.'+report.key);equal(preservation.sourceMaterialAssignmentSignatures,preservation.boundMaterialAssignmentSignatures,'pbr.item.material-assignments.'+report.key);const cleanup=report.cleanup||{};check(cleanup.removedRenderGeometry===0&&cleanup.changedTopology===false&&cleanup.changedNormals===false&&cleanup.changedTransforms===false&&Array.isArray(cleanup.previewHelperMeshesFound)&&cleanup.previewHelperMeshesFound.length===0,'pbr.item.no-geometry-cleanup',report.key);
    const uv=report.uv||{};check(uv.packed?.name==='UV_GEN'&&uv.packed?.gltfTexcoord===0&&uv.packed?.preserved===true&&uv.tiled?.name==='UVMap_Tile'&&uv.tiled?.gltfTexcoord===1&&uv.tiled?.metresPerTile===4,'pbr.item.uv-contract',report.key);check(uv.overlapFaces===0&&uv.allRenderMeshOverlapFaces===0&&finiteNumber(uv.maxStretchRatio)&&uv.maxStretchRatio<=CUBIC_STRETCH_LIMIT&&finiteNumber(uv.maxAreaScaleRelativeError)&&uv.maxAreaScaleRelativeError<=TEXEL_SCALE_ERROR_LIMIT&&uv.passed===true,'pbr.item.uv-proof',report.key);equal(uv.meshMetrics,(repairReport.meshes||[]).map(mesh=>mesh.tiledUvMetrics),'pbr.item.uv-metrics-binding.'+report.key);
    const artifact=preservation.postExportArtifactIdentity||{},sourceIdentity=artifact.sourceMeshIdentity||{},finalIdentity=artifact.finalMeshIdentity||{},finalUv=uv.finalPbrArtifact||{},renderNames=artifact.renderNodeNames||{},expectedRenderNames=Object.entries(sourceIdentity).filter(([,record])=>record?.renderNode===true).map(([name])=>name).sort();check(artifact.method==='BLENDER_REIMPORT_FLOAT32_TOPOLOGY_INDEX_WINDING_POSITION_NORMAL_UV_IDENTITY_V1'&&artifact.passed===true&&artifact.topologyIndexWindingPositionNormalUvIdentity===true&&stable(artifact.permittedChanges)===stable(['MATERIAL_DEFINITIONS','EMBEDDED_IMAGES'])&&stable(sourceIdentity)===stable(finalIdentity)&&Array.isArray(renderNames)&&renderNames.length>0&&stable(renderNames)===stable([...new Set(renderNames)].sort())&&stable(renderNames)===stable(expectedRenderNames)&&artifact.sourceSceneIdentitySha256===canonicalHash(sourceIdentity)&&artifact.finalSceneIdentitySha256===canonicalHash(finalIdentity)&&report.postExportArtifactIdentitySha256===canonicalHash(artifact),'pbr.item.post-export-identity',report.key);for(const[name,record]of Object.entries(sourceIdentity)){const copy={...record};delete copy.identitySha256;check(record.identitySha256===canonicalHash(copy),'pbr.item.mesh-identity-hash',report.key+'/'+name);}const packedByMesh=finalUv.packedOverlapByMesh||{},tiledByMesh=finalUv.tiledMetricsByMesh||{};check(finalUv.method==='FINAL_PBR_GLB_REIMPORT_OVERLAP_AND_WORLD_JACOBIAN_V1'&&finalUv.passed===true&&stable(Object.keys(packedByMesh).sort())===stable(renderNames)&&stable(Object.keys(tiledByMesh).sort())===stable(renderNames)&&finalUv.packedOverlapFaces===sum(Object.values(packedByMesh).map(value=>value.overlapFaces||0))&&finalUv.packedOverlapFaces===0&&finalUv.packedOverlapLoops===sum(Object.values(packedByMesh).map(value=>value.overlapLoops||0))&&finalUv.allTiledMetricsValid===true&&Object.keys(tiledByMesh).length>0&&Object.values(tiledByMesh).every(metrics=>metrics.valid===true&&metrics.finite===true&&metrics.nonZero===true&&metrics.scaleConsistent===true&&metrics.withinCubicStretchBound===true&&metrics.zeroUvAreaTriangles===0&&metrics.nonFiniteTriangles===0&&finiteNumber(metrics.maxStretchRatio)&&metrics.maxStretchRatio<=CUBIC_STRETCH_LIMIT&&finiteNumber(metrics.maxAreaScaleRelativeError)&&metrics.maxAreaScaleRelativeError<=TEXEL_SCALE_ERROR_LIMIT)&&canonicalTextBinding(report.finalUvProofCanonicalJson,finalUv,report.finalUvProofSha256),'pbr.item.final-uv-proof',report.key);const coverage=report.fullPbrCoverage||{},contractPayload={resume:report.resumeContractSha256,library:library.contractSha256,materials:report.materials,preservation,uv,coverage:Object.fromEntries(Object.entries(coverage).filter(([key])=>key!=='embeddedImages'))};check(stable(report.contract)===stable(contractPayload)&&canonicalTextBinding(report.contractCanonicalJson,contractPayload,report.contractSha256),'pbr.item.contract-hash',report.key);const glb=verifyPbrGlb(output,report,'pbr.glb.'+report.key);if(glb)glbByKey.set(report.key,glb);check(coverage.passed===true&&coverage.renderPrimitives>0&&coverage.totalBound===coverage.renderPrimitives&&coverage.opaqueMaterialCount===coverage.singleSidedMaterialCount&&coverage.doubleSidedMaterialCount===0&&['baseColorTexture','normalTexture','metallicRoughnessTexture','occlusionTexture'].every(field=>coverage[field]===coverage.renderPrimitives),'pbr.item.full-core-coverage',report.key);check(glb&&glb.primitiveCount===coverage.renderPrimitives,'pbr.item.glb-coverage',report.key);const legacyArchive=verifyPbrRebindProvenance(report,repairReport,library,artifact,finalUv,coverage);if(legacyArchive)referencedLegacyArchives.push(legacyArchive);
  }
  equal(summary.statusCounts,countsBy(loaded.reports,'status'),'pbr.summary.status-recomputed');equal(summary.artifactDispositionCounts,countsBy(loaded.reports,'artifactDisposition'),'pbr.summary.dispositions-recomputed');equal(summary.families,pbrFamilySummary(loaded.reports),'pbr.summary.families-recomputed');equal(summary.semanticMaterialUse,semanticUse(loaded.reports),'pbr.summary.semantics-recomputed');const summaryRows=summary.items||[],summaryItems=new Map(summaryRows.map(item=>[item.key,item]));check(summary.itemManifestSha256===canonicalHash(summaryRows)&&summary.productionManifestSha256===summary.itemManifestSha256,'pbr.summary.item-manifest-hash');check(summaryRows.length===loaded.reports.length&&summaryItems.size===loaded.reports.length,'pbr.summary.item-count',summaryRows.length);setEqual(summaryItems.keys(),loaded.paths.keys(),'pbr.summary.item-membership');for(const report of loaded.reports){const item=summaryItems.get(report.key),reportFile=loaded.paths.get(report.key);check(item?.stage==='PBR'&&item?.disposition==='PRODUCTION_READY'&&Array.isArray(item?.reasons)&&item.reasons.length===0&&item?.status===report.status&&item?.artifactDisposition===report.artifactDisposition&&item?.contentEquivalentRebindSha256===report.contentEquivalentRebindSha256&&item?.report===posix(reportFile)&&item?.reportSha256===sha256File(reportFile)&&item?.output===report.output&&item?.outputSha256===report.outputSha256,'pbr.summary.item-binding',report.key);}const partition=verifyStageManifestContract(summary,'PBR',repair.productionRows,repair.quarantine,[...loaded.paths.keys()]),stageFailures=partition.quarantine.filter(row=>row.stage==='PBR');check(summary.totalScheduled===expectedKeys.length&&summary.processed===loaded.reports.length&&summary.failed===stageFailures.length&&(summary.failures||[]).length===stageFailures.length,'pbr.summary.complete');setEqual((summary.failures||[]).map(failure=>failure.key),stageFailures.map(row=>row.key),'pbr.summary.failure-membership');setEqual(walkFiles(PBR_MODEL_ROOT,file=>file.toLowerCase().endsWith('.glb')).map(posix),referencedOutputs.map(posix),'pbr.output-files-exact');check(new Set(referencedLegacyArchives.map(posix)).size===referencedLegacyArchives.length,'pbr.rebind.archive-unique');setEqual(walkFiles(path.join(PBR_REPORT_ROOT,'rebind-provenance')).map(posix),referencedLegacyArchives.map(posix),'pbr.rebind.archive-files-exact');return{summary,summaryFile,libraryFile,libraryReport,reports:loaded.reports,paths:loaded.paths,byKey,glbByKey,...partition};
}

// Python's sorted(key=str.lower) uses code-point ordering; localeCompare puts
// the real spline hyphen/underscore keys in a different order on Node/Windows.
function manifest(reports,paths){const entries=[...reports].sort((a,b)=>{const left=a.key.toLowerCase(),right=b.key.toLowerCase();return left<right?-1:left>right?1:0;}).map(report=>({key:report.key,path:posix(paths.get(report.key)),sha256:sha256File(paths.get(report.key)),bytes:fs.statSync(paths.get(report.key)).size}));return{count:entries.length,sha256:canonicalHash(entries)};}
function verifyInspectionIdentity(item,repairReport,code){const inspections=item.inspections||{},source=inspections.canonicalSource,repair=inspections.repairOutput,pbr=inspections.pbrOutput;check(source&&repair&&pbr,code+'.present');if(!source||!repair||!pbr)return;const tolerance=Math.max(1e-5,(repairReport.rasterCleanup?.maxBoundsDriftM||0)+1e-5);check(nearArray(source.boundsMin,repair.boundsMin,tolerance)&&nearArray(source.boundsMax,repair.boundsMax,tolerance),code+'.canonical-cleanup-bounds');check(nearArray(repair.boundsMin,pbr.boundsMin,1e-5)&&nearArray(repair.boundsMax,pbr.boundsMax,1e-5),code+'.repair-pbr-bounds');for(const field of['meshCount','polygons','triangles'])check(repair.selection?.[field]===pbr.selection?.[field],code+'.repair-pbr-count.'+field);for(const field of['surfaceArea','signedVolume'])check(nearNumber(repair.selection?.[field],pbr.selection?.[field],1e-6,1e-7),code+'.repair-pbr-scalar.'+field);equal(repair.selection,pbr.selection,code+'.repair-pbr-exact-geometry');check(source.trueRenderMeshCount>0&&source.trueRenderMeshCount===repair.trueRenderMeshCount&&source.trueRenderMeshCount===pbr.trueRenderMeshCount,code+'.mesh-count');check(SHA_PATTERN.test(repair.selection?.packTopologyWindingSha256||'')&&repair.selection?.packTopologyWindingSha256===pbr.selection?.packTopologyWindingSha256,code+'.repair-pbr-winding-hash');}
function verifyRender(contract,repair,pbr,audit){
  const summaryFile=path.join(RENDER_ROOT,'summary.json'),summary=readJson(summaryFile,'render.summary'),loaded=loadReports(path.join(RENDER_ROOT,'reports'),'MassfrontStage10ModelPbrRenderPairV2','render.reports'),expectedKeys=audit.productionKeys;check([...loaded.paths.keys()].every(key=>expectedKeys.includes(key)),'render.report-input-subset');check(loaded.files.length<=expectedKeys.length,'render.report-count',loaded.files.length);if(!summary)return{summary,summaryFile,reports:loaded.reports,paths:loaded.paths,productionRows:[],productionKeys:[],quarantine:audit.quarantine};
  check(summary.schema==='MassfrontStage10ModelPbrRenderSummaryV2'&&summary.rendererVersion===EXPECTED.renderer&&['PASS','PASS_WITH_QUARANTINE'].includes(summary.status),'render.summary.contract');check(summary.outputBoundary==='tmp/stage10-model-repair/renders'&&summary.sourcePolicy==='READ_ONLY_GLTF_IMPORTS_ORIGINALS_UNTOUCHED','render.summary.policy');check(summary.selection?.selectedCount===expectedKeys.length&&summary.selection?.completeCatalog===(expectedKeys.length===EXPECTED.included),'render.summary.selection');equal(summary.resolution,[768,768],'render.summary.resolution');check(summary.expectedFullRenderPairCount===expectedKeys.length&&summary.repairReportCount===expectedKeys.length,'render.summary.expected-count');const selectedRepairReports=expectedKeys.map(key=>repair.byKey.get(key)).filter(Boolean),renderCounts={byFamily:countsBy(selectedRepairReports,'family'),byKind:countsBy(selectedRepairReports,'kind'),byRepairStatus:countsBy(selectedRepairReports,'status')};equal(summary.selectionCounts,renderCounts,'render.summary.selection-counts');
  check(summary.rendererScript?.path===posix(RENDER_SCRIPT)&&summary.rendererScript?.sha256===sha256File(RENDER_SCRIPT)&&summary.rendererScript?.bytes===fs.statSync(RENDER_SCRIPT).size,'render.summary.renderer-binding');check(summary.repairPipeline?.path===posix(REPAIR_SCRIPT)&&summary.repairPipeline?.sha256===sha256File(REPAIR_SCRIPT)&&summary.repairPipeline?.bytes===fs.statSync(REPAIR_SCRIPT).size,'render.summary.repair-pipeline-binding');check(summary.rasterCleanupPipeline?.path===posix(RASTER_CLEANUP_SCRIPT)&&summary.rasterCleanupPipeline?.sha256===sha256File(RASTER_CLEANUP_SCRIPT)&&summary.rasterCleanupPipeline?.bytes===fs.statSync(RASTER_CLEANUP_SCRIPT).size,'render.summary.raster-pipeline-binding');check(summary.pbrPipeline?.path===posix(PBR_SCRIPT)&&summary.pbrPipeline?.sha256===sha256File(PBR_SCRIPT)&&summary.pbrPipeline?.bytes===fs.statSync(PBR_SCRIPT).size,'render.summary.pbr-pipeline-binding');check(summary.repairSummary?.path===posix(repair.summaryFile)&&summary.repairSummary?.sha256===sha256File(repair.summaryFile)&&summary.repairSummary?.bytes===fs.statSync(repair.summaryFile).size&&summary.repairSummary?.pipelineVersion===EXPECTED.repairPipeline&&summary.repairSummary?.totalScheduled===EXPECTED.included&&summary.repairSummary?.processed===EXPECTED.included&&summary.repairSummary?.failed===0,'render.summary.repair-summary-binding');check(summary.pbrSummary?.path===posix(pbr.summaryFile)&&summary.pbrSummary?.sha256===sha256File(pbr.summaryFile)&&summary.pbrSummary?.bytes===fs.statSync(pbr.summaryFile).size&&summary.pbrSummary?.pipelineVersion===1&&summary.pbrSummary?.itemManifestSha256===canonicalHash(pbr.summary?.items||[])&&summary.pbrSummary?.productionManifestSha256===pbr.summary?.productionManifestSha256,'render.summary.pbr-summary-binding');check(summary.pbrLibraryReport?.path===posix(pbr.libraryFile)&&summary.pbrLibraryReport?.sha256===sha256File(pbr.libraryFile)&&summary.pbrLibraryReport?.bytes===fs.statSync(pbr.libraryFile).size,'render.summary.pbr-library-binding');const selectedPbrReports=expectedKeys.map(key=>pbr.byKey.get(key)).filter(Boolean);equal(summary.repairReportCatalog,manifest(repair.reports,repair.paths),'render.summary.repair-manifest');equal(summary.selectedRepairReports,manifest(selectedRepairReports,repair.paths),'render.summary.selected-repair-manifest');equal(summary.pbrReportCatalog,manifest(pbr.reports,pbr.paths),'render.summary.pbr-manifest');equal(summary.selectedPbrReports,manifest(selectedPbrReports,pbr.paths),'render.summary.selected-pbr-manifest');check(canonicalTextBinding(summary.renderContractCanonicalJson,summary.renderContract,summary.renderContractSha256),'render.summary.contract-hash');
  const renderContract=summary.renderContract||{};check(canonicalTextBinding(summary.renderContractCanonicalJson,renderContract,summary.renderContractSha256),'render.contract.hash');check(renderContract.schema==='MassfrontStage10PbrRenderContractV2'&&['BLENDER_EEVEE','BLENDER_EEVEE_NEXT'].includes(renderContract.engine),'render.contract.engine');check(renderContract.camera==='MATCHED_UNION_BOUNDS_ORTHOGRAPHIC_ISOMETRIC'&&renderContract.frameMargin===1.18,'render.contract.camera');check(renderContract.materialPolicy?.after==='AUTHORITATIVE_DERIVED_PBR_MATERIALS_NO_FALLBACK'&&renderContract.materialPolicy?.backfaceCullingEnforced===true&&renderContract.materialPolicy?.alphaMode==='OPAQUE'&&renderContract.materialPolicy?.doubleSided===false&&renderContract.materialPolicy?.geometryProof==='CANONICAL_LOCK_PLUS_SHARED_RASTER_CLEANUP_AND_REPAIR_PBR_IDENTITY','render.contract.pbr-policy');equal(renderContract.uvContract,{'UV_GEN/TEXCOORD_0':'PER_RENDER_MESH_PER_FACE_NON_OVERLAP_BAKE_ATLAS','UVMap_Tile/TEXCOORD_1':'VISIBLE_WORLD_CUBIC_4M_PBR_PATH'},'render.contract.uv');check(renderContract.worldKitSelection==='LOD0_TRUE_RENDER_MESHES_ONLY'&&renderContract.splineSelection==='ALL_TRUE_RENDER_MESHES'&&renderContract.lights?.length===3,'render.contract.selection-lighting');
  const partition=verifyStageManifestContract(summary,'RENDER',audit.productionRows,audit.quarantine,[...loaded.paths.keys()]),summaryRows=summary.items||[],items=new Map(summaryRows.map(item=>[item.key,item]));check(summaryRows.length===loaded.reports.length&&items.size===loaded.reports.length,'render.summary.item-count',summaryRows.length);setEqual(items.keys(),loaded.paths.keys(),'render.summary.item-membership');const stageFailures=partition.quarantine.filter(row=>row.stage==='RENDER');check(summary.counts?.selected===expectedKeys.length&&(summary.counts.rendered||0)+(summary.counts.resumed||0)===loaded.reports.length&&summary.counts.failed===stageFailures.length&&(summary.failures||[]).length===stageFailures.length,'render.summary.complete');setEqual((summary.failures||[]).map(failure=>failure.key),stageFailures.map(row=>row.key),'render.summary.failure-membership');const beforeFiles=[],afterFiles=[];
  for(const report of loaded.reports){
    const catalogItem=contract.byKey.get(report.key),repairReport=repair.byKey.get(report.key),pbrReport=pbr.byKey.get(report.key);if(!catalogItem||!repairReport||!pbrReport)continue;const reportFile=loaded.paths.get(report.key),expectedReport=path.join(RENDER_ROOT,'reports',catalogItem.family,safeId(catalogItem.id)+'.json');check(path.resolve(reportFile)===path.resolve(expectedReport),'render.item.report-path',report.key);check(report.rendererVersion===EXPECTED.renderer&&report.status==='RENDERED'&&report.error===null,'render.item.contract',report.key);equal([report.id,report.family,report.kind,report.repairStatus],[catalogItem.id,catalogItem.family,catalogItem.kind,repairReport.status],'render.item.identity.'+report.key);
    const binding=report.binding||{},repairReportFile=repair.paths.get(report.key),pbrReportFile=pbr.paths.get(report.key);check(report.resumeKey===canonicalHash(binding),'render.item.resume-key',report.key);check(binding.sourceSha256===catalogItem.model.sha256&&binding.sourceBytes===catalogItem.model.bytes&&binding.repairOutputSha256===repairReport.outputSha256&&binding.repairOutputBytes===fs.statSync(resolveRef(repairReport.output,OUT,'render.repair-size')).size&&binding.pbrOutputSha256===pbrReport.outputSha256&&binding.pbrOutputBytes===fs.statSync(resolveRef(pbrReport.output,PBR_MODEL_ROOT,'render.pbr-size')).size,'render.item.model-bindings',report.key);check(binding.repairPipelineVersion===EXPECTED.repairPipeline&&binding.pbrPipelineVersion===1&&binding.repairPipelineSha256===sha256File(REPAIR_SCRIPT)&&binding.repairPipelineBytes===fs.statSync(REPAIR_SCRIPT).size&&binding.rasterCleanupScriptSha256===sha256File(RASTER_CLEANUP_SCRIPT)&&binding.rasterCleanupScriptBytes===fs.statSync(RASTER_CLEANUP_SCRIPT).size&&binding.pipelineScriptSha256===sha256File(PBR_SCRIPT)&&binding.pbrPipelineSha256===sha256File(PBR_SCRIPT)&&binding.pbrPipelineBytes===fs.statSync(PBR_SCRIPT).size&&binding.rendererScriptSha256===sha256File(RENDER_SCRIPT)&&binding.rendererScriptBytes===fs.statSync(RENDER_SCRIPT).size,'render.item.pipeline-bindings',report.key);check(binding.repairReportSha256===sha256File(repairReportFile)&&binding.repairReportBytes===fs.statSync(repairReportFile).size&&binding.pbrReportSha256===sha256File(pbrReportFile)&&binding.pbrReportBytes===fs.statSync(pbrReportFile).size&&binding.pbrContractSha256===pbrReport.contractSha256&&binding.pbrResumeContractSha256===pbrReport.resumeContractSha256&&binding.repairRasterCleanupSha256===pbrReport.repairRasterCleanupSha256&&binding.repairLineageProofSha256===pbrReport.repairLineageProofSha256&&binding.repairMaterialSemanticContractSha256===pbrReport.repairMaterialSemanticContractSha256&&binding.pbrFinalArtifactIdentitySha256===pbrReport.postExportArtifactIdentitySha256&&binding.pbrFinalUvProofSha256===pbrReport.finalUvProofSha256,'render.item.report-bindings',report.key);check(binding.pbrLibrarySha256===canonicalHash(pbrReport.library)&&binding.pbrLibraryReportSha256===sha256File(pbr.libraryFile)&&binding.pbrLibraryReportBytes===fs.statSync(pbr.libraryFile).size&&canonicalTextBinding(binding.pbrMaterialManifestCanonicalJson,pbrReport.materials,binding.pbrMaterialManifestSha256),'render.item.pbr-manifest-bindings',report.key);equal(binding.resolution,[768,768],'render.item.resolution.'+report.key);check(binding.renderContractSha256===summary.renderContractSha256,'render.item.contract-hash',report.key);equal(report.renderContract,summary.renderContract,'render.item.contract-match.'+report.key);check(canonicalTextBinding(report.renderContractCanonicalJson,report.renderContract,binding.renderContractSha256),'render.item.contract-canonical-hash',report.key);check(report.source?.path===catalogItem.model.path&&report.source?.sha256===catalogItem.model.sha256&&report.source?.bytes===catalogItem.model.bytes,'render.item.source',report.key);check(report.repairOutput?.path===repairReport.output&&report.repairOutput?.sha256===repairReport.outputSha256&&report.repairOutput?.bytes===binding.repairOutputBytes,'render.item.repair-output',report.key);check(report.pbrReport?.path===posix(pbrReportFile)&&report.pbrReport?.sha256===sha256File(pbrReportFile)&&report.pbrReport?.bytes===binding.pbrReportBytes,'render.item.pbr-report',report.key);check(report.pbrOutput?.path===pbrReport.output&&report.pbrOutput?.sha256===pbrReport.outputSha256&&report.pbrOutput?.bytes===binding.pbrOutputBytes,'render.item.pbr-output',report.key);
    verifyInspectionIdentity(report,repairReport,'render.inspection.'+report.key);
    const inspections=report.inspections||{},sourceSelection=inspections.canonicalSource?.selection||{},repairSelection=inspections.repairOutput?.selection||{},pbrSelection=inspections.pbrOutput?.selection||{},proof=report.geometryProof||{},raster=repairReport.rasterCleanup||{};
    equal(proof.countFields,['meshCount','polygons','triangles'],'render.geometry-proof-count-fields.'+report.key);equal(proof.scalarFields,['surfaceArea','signedVolume'],'render.geometry-proof-scalar-fields.'+report.key);
    const proofFields=[...(proof.countFields||[]),...(proof.scalarFields||[])],selectionProof=selection=>Object.fromEntries(proofFields.map(field=>[field,selection[field]]));
    equal(proof.canonical,selectionProof(sourceSelection),'render.geometry-proof-canonical.'+report.key);equal(proof.repair,selectionProof(repairSelection),'render.geometry-proof-repair.'+report.key);equal(proof.pbr,selectionProof(pbrSelection),'render.geometry-proof-pbr.'+report.key);equal(proof.derivedDelta,Object.fromEntries(proofFields.map(field=>[field,repairSelection[field]-sourceSelection[field]])),'render.geometry-proof-derived-delta.'+report.key);
    check(nearNumber(proof.boundsToleranceM,Math.max(1e-5,(raster.maxBoundsDriftM||0)+1e-5),1e-12)&&proof.canonicalBoundsWithinCleanupProof===true&&proof.repairPbrBoundsMatch===true,'render.geometry-proof-bounds.'+report.key);
    check(proof.repairReportCleanupProof===true&&proof.artifactIndependentAcceptance===true&&repairReport.sourceUntouched===true&&repairReport.canonicalTopologyUntouched===true&&repairReport.uvGeometryPreserved===true&&rasterCleanupProgressGreen(raster)&&lineageProofGreen(raster)&&(repairReport.meshes||[]).length>0&&(repairReport.meshes||[]).every(mesh=>mesh.cleanup?.canonicalTopologyUntouched===true&&mesh.cleanup?.uvGeometryPreserved===true&&SHA_PATTERN.test(mesh.cleanup?.geometrySignatureAfterRasterCleanup||'')&&mesh.cleanup?.geometrySignatureAfterRasterCleanup===mesh.cleanup?.geometrySignatureAfter),'render.geometry-proof-repair-binding',report.key);
    check(proof.pbrReportPreservation===true&&['topology','winding','normals','transforms','materialSlotFaceAssignments'].every(field=>pbrReport.preservation?.[field]===true)&&(pbrReport.materials||[]).length>0&&(pbrReport.materials||[]).every(material=>stable(material.rasterization)===stable(PBR_RASTER_POLICY)),'render.geometry-proof-pbr-binding',report.key);check(proof.contract==='CANONICAL_LOCK_PLUS_SHARED_RASTER_CLEANUP_AND_REPAIR_PBR_IDENTITY'&&proof.passed===true,'render.geometry-proof-pass',report.key);
    const frame=report.frame||{},sourceBounds={min:inspections.canonicalSource?.boundsMin,max:inspections.canonicalSource?.boundsMax},pbrBounds={min:inspections.pbrOutput?.boundsMin,max:inspections.pbrOutput?.boundsMax};
    check(frame.camera?.type==='ORTHO'&&stable(frame.camera.direction)===stable(summary.renderContract.cameraDirection)&&finiteNumber(frame.camera.orthoScale)&&frame.camera.orthoScale>0&&finiteNumber(frame.camera.clipStart)&&finiteNumber(frame.camera.clipEnd)&&frame.camera.clipStart>0&&frame.camera.clipEnd>frame.camera.clipStart,'render.item.matched-camera',report.key);equal(frame.canonicalSourceBounds,sourceBounds,'render.item.source-frame.'+report.key);equal(frame.pbrOutputBounds,pbrBounds,'render.item.pbr-frame.'+report.key);
    const union={min:sourceBounds.min?.map((value,index)=>Math.min(value,pbrBounds.min[index])),max:sourceBounds.max?.map((value,index)=>Math.max(value,pbrBounds.max[index]))},dimensions=union.min?.map((value,index)=>union.max[index]-value),translation=union.min?.map((value,index)=>index<2?-(value+union.max[index])/2:-value);
    check(nearArray(frame.unionBounds?.min,union.min,1e-6)&&nearArray(frame.unionBounds?.max,union.max,1e-6)&&nearArray(frame.dimensions,dimensions,1e-6)&&nearArray(frame.translation,translation,1e-6),'render.item.frame-geometry',report.key);
    for(const[side,expectedRoot]of[['before',path.join(RENDER_ROOT,'before')],['after',path.join(RENDER_ROOT,'after')]]){const evidence=report[side]||{},file=verifyFileRecord(evidence.path,evidence.sha256,evidence.bytes,expectedRoot,'render.'+side+'.'+report.key);check(stable(evidence.dimensions)===stable([768,768])&&stable(pngDimensions(file))===stable([768,768]),'render.'+side+'.dimensions',report.key);equal(evidence.settings?.resolution,[768,768],'render.'+side+'.settings-resolution.'+report.key);check(evidence.settings?.engine===summary.renderContract.engine,'render.'+side+'.engine',report.key);if(side==='before'){equal(evidence.selection,report.inspections?.canonicalSource?.selection,'render.before.selection.'+report.key);check(evidence.materials?.policy==='SOURCE'&&(evidence.materials?.materials||[]).length>0&&(evidence.materials?.materials||[]).every(material=>material.backfaceCulling===true),'render.before.source-materials',report.key);beforeFiles.push(file);}else{equal(evidence.selection,report.inspections?.pbrOutput?.selection,'render.after.selection.'+report.key);check(evidence.materials?.policy==='PBR'&&(evidence.materials?.neutralFallbackMeshes||[]).length===0&&(evidence.materials?.materials||[]).length>0,'render.after.no-flat-fallback',report.key);for(const material of evidence.materials?.materials||[])check(material.backfaceCulling===true&&material.usesNodes===true&&(material.imageNodes||[]).length>0,'render.after.material-proof',report.key+'/'+material.name);afterFiles.push(file);}}
    equal(report.before?.settings,report.after?.settings,'render.item.matched-settings.'+report.key);for(const[side,bounds]of[['before',sourceBounds],['after',pbrBounds]]){const translated={min:bounds.min?.map((value,index)=>value+frame.translation[index]),max:bounds.max?.map((value,index)=>value+frame.translation[index])};check(nearArray(report[side]?.boundsAfterPairTranslation?.min,translated.min,1e-5)&&nearArray(report[side]?.boundsAfterPairTranslation?.max,translated.max,1e-5),'render.item.matched-translated-bounds.'+report.key+'/'+side);}const expectedMapEvidence=[];for(const material of pbrReport.materials||[])for(const semantic of['baseColor','normal','orm','emissive']){const map=material.maps?.[semantic];if(!map)continue;const file=resolveRef(map.path,path.join(PBR_REPORT_ROOT,'library','cells'),'render.pbr-map');expectedMapEvidence.push({semantic,path:map.path,sha256:map.sha256,bytes:fs.existsSync(file)?fs.statSync(file).size:null,dimensions:[map.width,map.height]});}equal(report.pbrMapEvidence,expectedMapEvidence,'render.item.pbr-map-evidence.'+report.key);const verifiedGlb=pbr.glbByKey.get(report.key),glbEvidence=report.pbrGlbEvidence||{},coverage=pbrReport.fullPbrCoverage||{},expectedBindingCounts=Object.fromEntries(['baseColorTexture','normalTexture','metallicRoughnessTexture','occlusionTexture','emissiveTextureWhereRequired'].map(field=>[field,coverage[field]]));check(SHA_PATTERN.test(glbEvidence.glbJsonSha256||'')&&glbEvidence.renderPrimitiveCount===coverage.renderPrimitives&&glbEvidence.opaqueSingleSidedMaterialCount===coverage.opaqueMaterialCount,'render.item.pbr-glb-evidence',report.key);equal(glbEvidence.rasterPolicy,PBR_RASTER_POLICY,'render.item.pbr-glb-raster-policy.'+report.key);equal(glbEvidence.bindingCounts,expectedBindingCounts,'render.item.pbr-glb-bindings.'+report.key);if(verifiedGlb){equal(glbEvidence.images,verifiedGlb.images.map(image=>({index:image.index,name:image.name,mimeType:image.mimeType,storage:image.storage,sha256:image.sha256,bytes:image.bytes,dimensions:image.dimensions})),'render.item.pbr-glb-images.'+report.key);equal(glbEvidence.usedImageIndices,verifiedGlb.images.map(image=>image.index),'render.item.pbr-glb-used-images.'+report.key);}setEqual(glbEvidence.expectedReportedMapHashes||[],new Set(expectedMapEvidence.map(map=>map.sha256)),'render.item.pbr-glb-map-hashes.'+report.key);const summaryItem=items.get(report.key);check(summaryItem?.stage==='RENDER'&&summaryItem?.disposition==='PRODUCTION_READY'&&Array.isArray(summaryItem?.reasons)&&summaryItem.reasons.length===0&&summaryItem?.status==='PASS'&&(summaryItem?.itemReport||summaryItem?.report)===posix(reportFile)&&(summaryItem?.itemReportSha256||summaryItem?.reportSha256)===sha256File(reportFile)&&summaryItem?.before===report.before?.sha256&&summaryItem?.after===report.after?.sha256&&!summaryItem?.error,'render.summary.item-binding',report.key);
  }
  setEqual(walkFiles(path.join(RENDER_ROOT,'before'),file=>file.toLowerCase().endsWith('.png')).map(posix),beforeFiles.map(posix),'render.before-files-exact');setEqual(walkFiles(path.join(RENDER_ROOT,'after'),file=>file.toLowerCase().endsWith('.png')).map(posix),afterFiles.map(posix),'render.after-files-exact');return{summary,summaryFile,reports:loaded.reports,paths:loaded.paths,...partition};
}

let finalAccounting=null;
function main(){const contract=verifyCatalog();if(!contract)throw new Error('authoritative model-review catalog is unavailable');const repair=verifyRepair(contract);const pbr=verifyPbr(contract,repair);const audit=verifyZFightingV3(contract,repair,pbr),render=verifyRender(contract,repair,pbr,audit);finalAccounting={repair,pbr,audit,render};}
try{main();}catch(error){check(false,'verifier.exception',error.stack||error.message);}
const accounting=finalAccounting?{productionSetPolicy:PRODUCTION_SET_POLICY,repair:{censusCount:finalAccounting.repair.reports.length,productionManifest:finalAccounting.repair.productionRows,quarantineManifest:finalAccounting.repair.quarantine},pbr:{productionManifest:finalAccounting.pbr.productionRows,quarantineManifest:finalAccounting.pbr.quarantine},audit:{productionManifest:finalAccounting.audit.productionRows,quarantineManifest:finalAccounting.audit.quarantine},render:{productionManifest:finalAccounting.render.productionRows,quarantineManifest:finalAccounting.render.quarantine},pipelineExclusions:[...EXCLUSIONS].map(([key,reason])=>({key,reason})).sort(manifestRowCompare),discardedSplinePropsPoi:[...DELETED_SPLINE_PROPS]}:null;
if(accounting){check(accounting.repair.censusCount===EXPECTED.included,'accounting.repair-census');check(accounting.pipelineExclusions.length===EXPECTED.excluded,'accounting.exclusion-count');check(accounting.discardedSplinePropsPoi.length===12&&new Set(accounting.discardedSplinePropsPoi).size===12,'accounting.discard-count');}
const stages=Object.fromEntries(Object.entries(stageCounts).map(([stage,counts])=>[stage,{checks:counts.checks,passed:counts.checks-counts.failed,failed:counts.failed}]));
const failures=Object.values(failureSamples).flat().slice(0,64);
const quarantineCount=accounting?.render?.quarantineManifest?.length||0;
const result={schema:'MassfrontStage10ModelPreparationVerificationV2',status:failedCount?'FAIL':quarantineCount?'PASS_WITH_QUARANTINE':'PASS',checks:checkCount,passed:checkCount-failedCount,failed:failedCount,catalogCandidates:EXPECTED.candidates,pipelineExcluded:EXPECTED.excluded,pipelineIncluded:EXPECTED.included,repairPipelineVersion:EXPECTED.repairPipeline,zFightingAuditVersion:EXPECTED.zFightingAudit,pbrPipelineVersion:EXPECTED.pbrPipeline,rendererVersion:EXPECTED.renderer,repairCensusCount:accounting?.repair?.censusCount??0,finalProductionCount:accounting?.render?.productionManifest?.length??0,quarantinedCount:quarantineCount,discardedCount:accounting?.discardedSplinePropsPoi?.length??0,accounting,stages,failures,omittedFailureCount:Math.max(0,failedCount-failures.length)};
console.log(JSON.stringify(result,null,2));if(failedCount)process.exitCode=1;
